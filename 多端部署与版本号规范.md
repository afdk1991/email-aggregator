# 邮箱聚合平台 · 多端部署与版本号规范

> 适用版本：**MALLV0.0.0**
> 权威版本源：仓库根 [`version.json`](./version.json)
> 最后更新：2026-09-16

---

## 1. 总览

本项目从"浏览器单一形态"升级为**六端可部署**，并且所有端的所有产物共享**同一个版本号**。

| 端 | 技术载体 | 产物形态 | 宿主要求 | 本地可构建 |
|----|----------|----------|----------|-----------|
| **Windows** | Electron + electron-builder | `…-win.exe`（多架构合体 NSIS）<br>`…-win-x64.exe` / `…-win-arm64.exe`（分架构 NSIS）<br>`…-win-x64-portable.exe`（免安装） | Windows | ✅ 已构建验证 |
| **macOS** | Electron + electron-builder | `EmailAggregator-MALLV0.0.0-mac-{x64,arm64}.dmg` / `.zip` | macOS | ⛔ 需 macOS 宿主 |
| **Linux** | Electron + electron-builder | `EmailAggregator-MALLV0.0.0-linux-{x64,arm64}.AppImage` / `.deb`、x64 `.rpm` | Linux | ⛔ 需 Linux 宿主 |
| **Android** | Capacitor + Gradle | `app-release.apk` / `app-release.aab` | JDK 17+ / Android SDK | ⛔ 需 JDK + SDK |
| **iOS** | Capacitor + Xcode | `App.xcarchive`（未签名）；`.ipa` 需证书 | macOS + Xcode 15+ | ⛔ 需 macOS + Xcode |
| **HarmonyOS** | ArkTS + Web 容器 | `entry-default-unsigned.hap` | DevEco Studio 5.0+ / API 12 | ⛔ 需 DevEco Studio |
| *（附加）* **Web / 容器 / 云函数** | React SPA + Go 主干 | 静态站点、容器镜像、Serverless 函数 | 无 | ✅ 已完成 |

**设计原则**：多端不是"同一份代码复制六次"，而是**一份 SPA + 一份 Go 主干**，由六个很薄的"壳"承载：

```
                    ┌──────────────────────────────┐
                    │   email-aggregator-web/dist  │  ← 唯一前端产物
                    │   （vite build，base='./'）   │
                    └──────────────┬───────────────┘
                                   │ scripts/sync-web-assets.mjs
        ┌──────────────┬───────────┼───────────┬──────────────┐
        ▼              ▼           ▼           ▼              ▼
   go webroot/    edgeone-app  desktop/    mobile/       harmony/
   （embed）      （云端）      resources/  www/          rawfile/www/
                                app-dist    （Capacitor） （ArkTS Web）
```

---

## 2. 版本号规范

### 2.1 唯一权威源

**`version.json`** 是全项目唯一的版本号定义处：

```json
{
  "$comment": "全项目唯一版本源……",
  "prefix": "MALLV",
  "major": 0,
  "minor": 0,
  "patch": 0,
  "channel": "dev",
  "updatedAt": "2026-09-16"
}
```

任何其它位置的版本号都是**生成物**，禁止手工编辑（生成的 Go / TS / CJS / ETS 文件头部均带 `DO NOT EDIT`）。

### 2.2 三种派生形态

| 形态 | 表达式 | 当前值 | 用途 |
|------|--------|--------|------|
| **canonical** | `MALLV<major>.<minor>.<patch>` | `MALLV0.0.0` | 用户可见版本、**安装包文件名**、容器镜像 tag、git tag、文档标注、`versionName` |
| **semver** | `<major>.<minor>.<patch>` | `0.0.0` | 有格式约束的场合：npm `package.json`、iOS `CFBundleShortVersionString` |
| **code** | `major*10000 + minor*100 + patch` | `0` | 必须是单调递增整数的场合：Android `versionCode`、HarmonyOS `versionCode` |

> **为什么要有 code**：Android / HarmonyOS 都要求 `versionCode` 是整数且新版本必须大于旧版本。直接用 `0.0.0` 无法比较。`major*10000+minor*100+patch` 保证 `0.0.1 → 1`、`0.1.0 → 100`、`1.0.0 → 10000`，各段有独立进位空间，patch 段可容纳 100 个版本、minor 段 100 个。

### 2.3 递增规则

```powershell
# 在仓库根目录执行，一条命令会同步全部 18 个落点
node scripts/version.mjs bump patch    # MALLV0.0.0 -> MALLV0.0.1   （修 bug、样式调整）
node scripts/version.mjs bump minor    # MALLV0.0.1 -> MALLV0.1.0   （新增功能，向后兼容）
node scripts/version.mjs bump major    # MALLV0.1.0 -> MALLV1.0.0   （破坏性变更）
```

**发布流程（推荐顺序）**：

```
① node scripts/version.mjs bump patch      ← 唯一改版本号的动作
② node scripts/version.mjs verify          ← 确认 18 个落点全部一致
③ git add -A && git commit -m "chore(release): MALLV0.0.1"
④ git tag MALLV0.0.1 && git push origin master --tags
⑤ 标签推送后 .github/workflows/release-multiplatform.yml 自动出六端产物
```

第 ⑤ 步的 `version-guard` job 会强制校验 **标签名 == `version.json` 的 canonical**，不一致直接失败——这堵死了"tag 打 MALLV0.0.2、包里写 MALLV0.0.1"这类最常见的漂移。

### 2.4 18 个版本落点矩阵

`node scripts/version.mjs sync` 一次性下发到下列全部位置：

| # | 落点文件 | 形态 | 说明 |
|---|----------|------|------|
| 1 | `email-aggregator-web/package.json` | semver | 前端包 |
| 2 | `email-aggregator/package.json` | semver | TS PoC |
| 3 | `platforms/desktop/package.json` | semver | 桌面端（npm 生态不接受非 semver，故不写 MALLV 前缀） |
| 4 | `platforms/mobile/package.json` | semver | 移动端 |
| 5 | `VERSION` | canonical | 文本文件，供 shell / 构建脚本 `cat VERSION` |
| 6 | `email-aggregator-go/src/version/version.go` | 三者 | **整文件生成**，`var` 而非 `const`，允许 `-ldflags -X` 覆盖 |
| 7 | `email-aggregator-web/index.html` | canonical | `<meta name="app-version">` 等版本元信息 |
| 8 | `deploy/edgeone-app/index.html` | canonical | EdgeOne 静态入口（构建产物，schema 保持正确） |
| 9 | `deploy/edgeone-app/cloud-functions/api/[[default]].js` | canonical + code | 云函数常量；`/api/health` 直接回读 |
| 10 | `deploy/container/docker-compose.yml` | canonical | 镜像 tag `email-aggregator:MALLV0.0.0` |
| 11 | `deploy/container/Dockerfile` | canonical | OCI labels `org.opencontainers.image.version` |
| 12 | `email-aggregator-web/Dockerfile` | canonical | 同上（前端镜像） |
| 13 | `platforms/desktop/electron-builder.yml` | 模板 | `artifactName` 里的 `${env.MALLV_VERSION}` 宏 |
| 14 | `platforms/mobile/android/app/build.gradle` | canonical + code | `versionName` / `versionCode`（需先 `npx cap add android`） |
| 15 | `platforms/mobile/ios/App/App/Info.plist` | semver + code | `CFBundleShortVersionString` / `CFBundleVersion`（需先 `npx cap add ios`） |
| 16 | `email-aggregator-web/src/version.generated.ts` | 三者 | 前端运行时读取（页脚版本徽标） |
| 17 | `platforms/desktop/src/version.generated.cjs` | 三者 | Electron 主进程 / preload 读取 |
| 18 | `platforms/harmony/entry/src/main/ets/generated/AppVersion.ets` | 三者 | 鸿蒙 Web 容器通过 `javaScriptProxy` 桥接给页面 |

另有 `AppScope/app.json5`（HarmonyOS）的 `versionCode` / `versionName` 由 `harmony-app-json5` 落点维护。

**关于"可选落点"**：14 / 15 号落点标注为 `optional`。原生工程需要 `npx cap add <platform>` 生成后才存在；不存在时 `verify` 输出 `SKIP` 并给出补全命令，**不视为失败**——这是刻意的，否则 clone 后首次 CI 必然红。

### 2.5 版本号可验证性

版本号不只在文件名里，还进入**产物内容**，任一环节被篡改或漏传都能被检出：

| 端 | 校验点 | 校验方式 |
|----|--------|----------|
| Go 主干 | `GET /api/health` → `{"version":"MALLV0.0.0","versionCode":0}` | 运行时读取；另在 `prepare.mjs` 中对二进制做字符串搜索 |
| 前端 SPA | 页脚版本徽标 + `index.html` meta | 视觉可见 |
| 云函数 | `GET /api/health`（serverless-blob 形态） | 与 Go 主干同一契约 |
| 桌面端 | asar 内 `src/version.generated.cjs` | `scripts/verify-artifacts.mjs` 解包校验 |
| 安装包文件名 | `EmailAggregator-MALLV0.0.0-win-x64.exe` | 文件名正则 |
| Android | `versionName` / `versionCode` | `aapt dump badging` 或 `verify-artifacts.mjs` |
| HarmonyOS | `AppScope/app.json5` → `versionName` | `verify-artifacts.mjs` |

---

## 3. 目录结构

```
项目003/
├── version.json                    ★ 唯一版本源
├── VERSION                          （生成）纯文本版本号
├── scripts/
│   ├── version.mjs                  ★ 版本工具链（show/list/verify/sync/bump/set/artifacts）
│   ├── sync-web-assets.mjs          ★ 前端产物 5 处同步
│   └── make-icon.mjs                ★ 零依赖 PNG/ICO 生成器（桌面端 + 鸿蒙共用）
├── email-aggregator-web/            React + TS + Vite 前端（唯一 SPA）
├── email-aggregator-go/             Go 主干（REST + WebSocket + 内嵌 SPA）
├── email-aggregator/                TS PoC（契约对照）
├── deploy/
│   ├── container/                   Dockerfile + docker-compose
│   ├── edgeone-app/                 EdgeOne Makers（静态 + 云函数）
│   └── ...
└── platforms/                       ★ 新增：各端"壳"
    ├── desktop/                     Electron（Windows / macOS / Linux）
    │   ├── src/main.cjs             主进程：拉起 Go sidecar，失败则静态兜底
    │   ├── src/preload.cjs          exposeInMainWorld('eaDesktop')
    │   ├── scripts/prepare.mjs      版本门禁 + 同步 + 图标 + 交叉编译 sidecar
    │   ├── scripts/build.mjs        注入 MALLV_VERSION 后驱动 electron-builder
    │   ├── scripts/verify-artifacts.mjs  产物三层证据链校验
    │   └── electron-builder.yml     打包配置（win/mac/linux target）
    ├── mobile/                      Capacitor（Android / iOS）
    │   ├── capacitor.config.json    appId / webDir=www / https scheme
    │   ├── scripts/prepare.mjs      版本门禁 + 同步到 www
    │   └── scripts/build.mjs        cap sync → gradlew / xcodebuild
    └── harmony/                     HarmonyOS（ArkTS + Web 容器）
        ├── AppScope/app.json5       versionCode / versionName
        ├── entry/src/main/ets/
        │   ├── entryability/EntryAbility.ets
        │   ├── pages/Index.ets      Web 容器 + javaScriptProxy('eaHarmony')
        │   └── generated/AppVersion.ets  （生成）
        └── scripts/build.mjs        hvigorw assembleHap
```

---

## 4. 各端构建指南

### 4.0 通用前置（所有端）

```powershell
# 1) 构建前端 SPA（唯一前端产物）
cd email-aggregator-web
npm ci        # 或 npm install
npm run build

# 2) 分发到全部 5 个消费端（Go webroot / EdgeOne / desktop / mobile / harmony）
cd ..
node scripts/sync-web-assets.mjs

# 3) 版本门禁
node scripts/version.mjs sync
node scripts/version.mjs verify
```

> `sync-web-assets.mjs` 每次结束都会**复验 5 处哈希一致**，任何一处漏同步都会显式报错。这一步替代了历史上"手抄 dist 到两个目录"的做法——那个做法曾两次漏文件（漏 CSS、漏 webroot/dist）。

**为什么 `vite.config.ts` 必须 `base: './'`**：六个端的 origin 各不相同（`https://…`、`http://localhost:8082`、`file://`、`capacitor://localhost`、`rawfile://`）。绝对路径 `/assets/xxx.js` 在容器/云上正确，但在 Electron `file://` 与鸿蒙 `rawfile` 下会**漏掉 `www/` 这一层**导致 404。相对路径是六端唯一的公共解。

### 4.1 Windows（✅ 本机可构建）

```powershell
cd platforms/desktop
npm install
node scripts/build.mjs --win
node scripts/verify-artifacts.mjs
```

产物落在 `platforms/desktop/release/`：

| 文件 | 说明 | 体积 |
|------|------|------|
| `EmailAggregator-MALLV0.0.0-win.exe` | **多架构合体** NSIS 安装包（x64 + arm64，用户不确定架构时用这个） | ~214 MB |
| `EmailAggregator-MALLV0.0.0-win-x64.exe` | x64 专用 NSIS 安装包 | ~110 MB |
| `EmailAggregator-MALLV0.0.0-win-arm64.exe` | arm64 专用 NSIS 安装包 | ~104 MB |
| `EmailAggregator-MALLV0.0.0-win-x64-portable.exe` | 免安装绿色版 | ~110 MB |

> **portable 必须独立命名**：顶层 `artifactName` 用的 `${os}` 在 Windows 上求值为 `win`，于是 `nsis(x64)` 与 `portable(x64)` 会算出**同一个文件名** `…-win-x64.exe`。实测日志显示先建 nsis、后建 portable，落在同一路径 → **NSIS 安装包被 portable 静默覆盖**。故 `electron-builder.yml` 为 portable 单独设了 `artifactName`（带 `-portable` 后缀）。

国内网络需镜像（`build.mjs` 已默认注入）：

```powershell
$env:ELECTRON_MIRROR = 'https://npmmirror.com/mirrors/electron/'
$env:ELECTRON_BUILDER_BINARIES_MIRROR = 'https://npmmirror.com/mirrors/electron-builder-binaries/'
$env:CSC_IDENTITY_AUTO_DISCOVERY = 'false'   # 无证书时不翻钥匙串
```

**桌面端运行时模型**（`src/main.cjs`）：

```
Electron 启动
   ├─ 选取空闲端口
   ├─ spawn resources/bin/email-aggregator-<os>-<arch>[.exe]（Go 主干，webui 标签内嵌 SPA）
   │    成功 → 加载 http://127.0.0.1:<port>/     （完整功能：REST + WS + AI）
   └─ 失败 → 启动内置静态服务器托管 resources/app-dist/
        /api/* 返回 503 JSON（而不是回 index.html）—— 避免把"接口不可用"伪装成"页面正常"
```

这个"503 而非首页兜底"是刻意的：静态兜底页必须**诚实地**告诉用户后端没起来，不能让人以为功能正常。

### 4.2 macOS / Linux 桌面端

```bash
# macOS 宿主
cd platforms/desktop && npm install && node scripts/build.mjs --mac

# Linux 宿主（rpm 需先装 rpmbuild）
sudo apt-get install -y rpm
cd platforms/desktop && npm install && node scripts/build.mjs --linux
```

**必须在对应宿主系统上执行**，electron-builder 不做跨平台交叉打包（macOS 的 dmg 需要 `hdiutil`，Linux 的 AppImage 需要 `appimagetool`）。

### 4.3 Android

```powershell
# 前置：JDK 17+、Android SDK（ANDROID_HOME 已设置）
cd platforms/mobile
npm install
npx cap add android            # 仅在首次执行，生成原生工程
cd ../..
node scripts/version.mjs sync  # 把 versionCode/versionName 写进 build.gradle
cd platforms/mobile
node scripts/build.mjs android --debug      # 出 debug APK（可直装）
node scripts/build.mjs android              # 出 release APK + AAB（需签名才能分发）
node scripts/build.mjs android --apk-only   # 只出 APK
```

**顺序很关键**：必须先 `cap add android` 再 `version.mjs sync`。因为 `build.gradle` 落点是 `optional` 的——生成之前它不存在，sync 会跳过；生成之后再 sync 才能把版本号写进去。`build.mjs` 的 `prepare()` 会做一致性断言，顺序颠倒会直接失败而不是静默产出错误版本的包。

### 4.4 iOS

```bash
# 仅 macOS + Xcode 15+ + CocoaPods
cd platforms/mobile
npm install
npx cap add ios
cd ../.. && node scripts/version.mjs sync && cd platforms/mobile
node scripts/build.mjs ios          # xcodebuild archive（未签名）
```

产物为 `ios/build/App.xcarchive`。**产出可安装的 `.ipa` 需要 Apple 开发者证书**，在未配置签名时 CI 只归档不导出。

### 4.5 HarmonyOS

```powershell
# 前置：DevEco Studio 5.0+（提供 hvigor / ohpm）、HarmonyOS SDK (API 12)
#       环境变量 DEVECO_SDK_HOME 指向 SDK 根目录
cd platforms/harmony
node scripts/prepare.mjs
node scripts/build.mjs                 # Release HAP
node scripts/build.mjs --mode debug    # Debug HAP
# 或直接指定 hvigorw 路径
node scripts/build.mjs --hvigor "C:\DevEco\...\hvigorw.bat"
```

**鸿蒙为什么不是 Capacitor**：Capacitor 官方无 HarmonyOS 目标。因此本端是**手写的 ArkTS 壳**：

```
entry/src/main/ets/pages/Index.ets
   Web({ src: $rawfile('www/index.html') })    ← 加载 SPA（rawfile 目录）
     .javaScriptProxy({ object: eaHarmony, ... })  ← 把 AppVersion.ets 的版本号桥给页面
```

`prepare.mjs` 把 `email-aggregator-web/dist/` 同步到 `entry/src/main/resources/rawfile/www/`，与桌面端的 `app-dist`、移动端的 `www` 同源同内容。

**真机安装必须签名**：在 `AppScope/build-profile.json5` 的 `signingConfigs` 填入自己的 `.p12 + .cer + profile`。未配置时只能产出 unsigned HAP。

---

## 5. 命令速查表

| 目的 | 命令（仓库根） |
|------|---------------|
| 查看当前版本 | `node scripts/version.mjs show` |
| 机器可读版本 | `node scripts/version.mjs show --json` |
| 列出 18 个落点及当前值 | `node scripts/version.mjs list` |
| 校验全部落点一致 | `node scripts/version.mjs verify` |
| 下发版本到全部落点 | `node scripts/version.mjs sync` |
| 递增版本 | `node scripts/version.mjs bump <major\|minor\|patch>` |
| 直接设定版本 | `node scripts/version.mjs set MALLV0.0.3` |
| 列出各端预期产物名 | `node scripts/version.mjs artifacts` |
| 同步前端产物到 5 处 | `node scripts/sync-web-assets.mjs` |
| 生成图标 | `node scripts/make-icon.mjs --out platforms/desktop/build` |
| Windows 安装包 | `cd platforms/desktop && node scripts/build.mjs --win` |
| macOS / Linux 安装包 | `node scripts/build.mjs --mac` / `--linux` |
| Android / iOS | `cd platforms/mobile && node scripts/build.mjs android` / `ios` |
| HarmonyOS HAP | `cd platforms/harmony && node scripts/build.mjs` |
| 校验产物版本号 | `node scripts/verify-artifacts.mjs`（在各 `platforms/<端>/` 下） |
| 本地全量 CI | `powershell -ExecutionPolicy Bypass -File .\scripts\ci.ps1 -Integration` |

---

## 6. 持续集成

### 6.1 `ci.yml`（每次 push / PR）

新增 `version-guard` job，作为**第一个 job** 执行：

1. `version.mjs sync` 后 `git diff --quiet` —— 若 sync 产生改动，说明有人手工改了生成物或漏提交，直接失败；
2. `version.mjs verify` —— 18 个落点逐一比对；
3. 11 个构建脚本 `node --check` 语法自检；
4. 版本信息写入 Job Summary。

原有 `go` / `ts` / `web` / `integration` 四个 job 不变。

### 6.2 `release-multiplatform.yml`（推送 `MALLV*` 标签）

```
version-guard  ─┬─►  desktop (windows / macOS / ubuntu 矩阵)
                ├─►  android (ubuntu + JDK17 + Android SDK)
                └─►  ios     (macOS + xcodebuild)
                        │
                        ▼
                    release (gh-release，汇总全部产物)
```

两个硬性约束：

- **标签必须等于版本号**：`version-guard` 校验 `${GITHUB_REF#refs/tags/} == canonical`；
- **产物必须带版本号**：每个平台出包后都跑 `verify-artifacts.mjs`，不通过则不上传。

### 6.3 HarmonyOS 在 CI 中的位置

DevEco Studio（含 hvigor / ohpm / HarmonyOS SDK）**无法在 GitHub 托管 runner 上合法获取**，因此 HAP 不在托管流水线内构建。两条可行路径：

1. **本地构建**：用 DevEco Studio 打开 `platforms/harmony`，`Build > Build Hap(s)`；或命令行（先设 `DEVECO_SDK_HOME`）执行第 4.5 节命令。
2. **自建 runner**：在 Windows/macOS 机器上安装 DevEco Studio，注册为 self-hosted runner，添加 `runs-on: [self-hosted, harmony]` 的 job 复用同样命令。

---

## 7. 环境前置与当前阻断项

| 能力 | 状态 | 说明 |
|------|------|------|
| 版本工具链（18 落点） | ✅ 已验证 | `sync` / `verify` 均 EXIT=0 |
| Go 主干编译 | ✅ 已验证 | `go build ./...`、`go vet`、`gofmt -l src/version` 全通过 |
| 前端 SPA 构建 | ✅ 已验证 | `tsc --noEmit` + `vite build` 通过 |
| 前端产物 5 处同步 | ✅ 已验证 | 每处哈希一致 |
| **Windows 安装包** | ✅ 已验证 | NSIS + portable，文件名含 `MALLV0.0.0` |
| macOS / Linux 安装包 | ⛔ 宿主限制 | 需对应操作系统的构建机 |
| Android APK / AAB | ⛔ 环境缺失 | 本机无 JDK 17 / Android SDK；`npx cap add android` 后可交给 CI |
| iOS ipa / xcarchive | ⛔ 环境缺失 | 需 macOS + Xcode |
| HarmonyOS HAP | ⛔ 环境缺失 | 需 DevEco Studio 5.0+ 与 HarmonyOS SDK |
| 三 worker 容器 E2E | ⛔ **本机无 Docker** | 见下 |

> **关于 Docker**：本机未安装 Docker，因此 PG / MinIO / OpenSearch / Redpanda / Keycloak 的**运行时**验证无法进行；`go build -tags integration` 等 12 种标签组合的**编译级**验证已全部通过。补做需在有 Docker 的环境执行 `powershell -ExecutionPolicy Bypass -File .\scripts\ci.ps1 -Integration`，或依赖 `ci.yml` 的 `integration` job。

**这些阻断项不是"未实现"，而是"运行环境不满足"**。所有构建脚本在环境缺失时都会**明确报错并给出补全命令**，不会静默产出一个空包或错误版本的包。

---

## 8. 常见问题排查

### Q1：安装包文件名没有版本号，只有 `EmailAggregator Setup 0.0.0.exe`

**原因**：`electron-builder.yml` 的 `artifactName` 不在**顶层**（被缩进成上一条目的子键），YAML 解析时该键失效，electron-builder 退回默认命名。

**检查**：
```powershell
Select-String -Path platforms\desktop\electron-builder.yml -Pattern '^artifactName'
```
应输出**顶格**的 `artifactName: ${productName}-${env.MALLV_VERSION}-…`。若匹配不到，说明键被缩进了。

**修复**：`node scripts/version.mjs sync` 会把它纠正为顶格。该落点的正则刻意不加前导 `\s*`——因为 `dmg:` 段内也有一行缩进的 `artifactName`，允许前导空白会误命中那一行。

### Q2：`version.mjs verify` 报某个落点不一致

```powershell
node scripts/version.mjs verify   # 看是哪个落点
node scripts/version.mjs sync     # 重新下发
node scripts/version.mjs verify   # 复验
```

若 `sync` 后仍有落点不一致，说明该文件的**锚点被改动**（例如正则依赖的 `const DEFAULT_ACCOUNT = '…'` 被删）。这类落点带 `insertAfter` 自播种能力，锚点存在时能自动重建槽位。

### Q3：CI 的 `version-guard` 失败，提示 "sync 后有改动"

说明仓库里的生成物与 `version.json` 不匹配（有人手改了生成文件，或改了 `version.json` 但没跑 `sync` 就提交）。本地执行 `sync` 并提交即可。

### Q4：`prepare.mjs` 报 "桌面端版本模块与 version.json 不一致"

`platforms/desktop/src/version.generated.cjs` 落后了。执行 `node scripts/version.mjs sync`。

**注意**：该模块是 **CommonJS JS 对象字面量**（键名不带引号），不能用"正则剥前缀 + `JSON.parse`"读取——会抛 `Expected property name or '}'`。`prepare.mjs` 用 `createRequire` 直接 `require()` 载入，这也避免了正则抽取在生成格式微调时静默算错。

### Q5：Go sidecar 编译报 `undefined: version`

`src/api/server.go` 的 import 块缺少 `"email-aggregator-go/src/version"`。`version.go` 本身由 `sync` 生成，`package version`，`var Canonical/Semver/Code/Prefix`。

### Q6：多端页面白屏，控制台一片 404 指向 `/assets/...`

`vite.config.ts` 的 `base` 在生产构建时必须是 `'./'`（相对路径）。绝对路径在 Electron `file://` 与鸿蒙 `rawfile://` 下会找不到 `assets/`。

### Q7：改了前端却只有一个端更新了

`scripts/sync-web-assets.mjs` 覆盖 **5 处**：`email-aggregator-go/webroot/dist`、`deploy/edgeone-app`、`platforms/desktop/resources/app-dist`、`platforms/mobile/www`、`platforms/harmony/entry/src/main/resources/rawfile/www`。脚本结束时会复验 5 处一致；若只更新了一部分，说明有人手工拷贝而非走脚本。**JS 与 CSS 两个文件都要比对**，只看文件数或总大小会漏。

### Q8：`release/` 里少了一个安装包（NSIS 安装包消失，只剩免安装版）

**原因**：`nsis(x64)` 与 `portable(x64)` 共用顶层 `artifactName` 模板，而 `${os}` 在 Windows 上求值都是 `win` → 两者算出**同一个文件名** → 后构建的 portable **静默覆盖** nsis。

**检查**：
```powershell
Get-ChildItem platforms\desktop\release\*.exe | Select-Object Name
```
应看到 **4 个**：`…-win.exe`、`…-win-x64.exe`、`…-win-arm64.exe`、`…-win-x64-portable.exe`。
若只有 3 个且没有 `-portable`，说明 `electron-builder.yml` 的 `portable:` 段被删掉了。

**修复**：恢复 `portable.artifactName: ${productName}-${env.MALLV_VERSION}-${os}-${arch}-portable.${ext}`。

> 这类"两个 target 算出同一文件名"的坑不会报错——electron-builder 只是安静地把后一个写到同一路径。**只能靠核对 release 目录里的文件数发现**。

---

## 9. 附录：新增文件清单

| 文件 | 作用 |
|------|------|
| `version.json` | 唯一版本源 |
| `VERSION` | 生成：纯文本版本号 |
| `scripts/version.mjs` | 版本工具链（7 个子命令 + 18 落点注册表） |
| `scripts/sync-web-assets.mjs` | 前端产物 5 处同步 + 复验 |
| `scripts/make-icon.mjs` | 零依赖 PNG/ICO 生成器（手写 PNG 编码器 + 超采样抗锯齿 + ICO 容器） |
| `platforms/desktop/**` | Electron 桌面端（三平台 PC 安装包） |
| `platforms/mobile/**` | Capacitor 移动端（Android / iOS） |
| `platforms/harmony/**` | HarmonyOS ArkTS 壳 |
| `.github/workflows/release-multiplatform.yml` | 六端发布流水线 |
| `email-aggregator-go/src/version/version.go` | 生成：Go 版本包 |
| `email-aggregator-web/src/version.generated.ts` | 生成：前端版本模块 |
| `platforms/desktop/src/version.generated.cjs` | 生成：Electron 版本模块 |
| `platforms/harmony/entry/src/main/ets/generated/AppVersion.ets` | 生成：鸿蒙版本模块 |

**改动文件**：`email-aggregator-web/vite.config.ts`（`base:'./'`）、`email-aggregator-web/src/api/client.ts`（`resolveApiOrigin()` / `wsEndpoint()`）、`email-aggregator-web/src/App.tsx`（版本徽标 + WS 端点）、`email-aggregator-web/src/global.d.ts`（`ImportMetaEnv`）、`email-aggregator-go/src/api/server.go`（health 增加 version/versionCode）、`deploy/**`（版本注入）、`.github/workflows/ci.yml`（新增 version-guard job）。

---

**规范维护**：修改本文件前先确认 `version.json` 当前值，文中所有 `MALLV0.0.0` 字样随版本递增同步更新。
