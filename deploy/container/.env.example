# 复制为同目录 .env 后，docker compose 会自动读取（.env 已被 .gitignore 忽略，不入库）
# 宿主机访问端口（容器内固定 8080）。本机 8080 已被其他项目占用，故使用 18090。
APP_PORT=18090
# 容器时区
TZ=Asia/Shanghai

# 可选：真实 AI 模型（OpenAI 兼容）；留空则 AI 面板自动降级为本地演示回复
# AI_THIRD_PARTY_URL=
# AI_THIRD_PARTY_KEY=
# AI_SELF_HOSTED_URL=
# AI_SELF_HOSTED_KEY=
