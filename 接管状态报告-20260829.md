# 项目003 · 邮箱聚合平台 全面接管状态报告（2026-08-29）

> 本报告由接管本轮实测产出：**确认全项目三套实现 + 真实中间件全链路当前可运行、可验证、可交付**，
> 并记录实测证据、运行方式、文档修复与遗留建议。所有结论均为本机真实执行结果，无伪造。

---

## 一、接管范围

| 资产 | 路径 | 角色 |
|------|------|------|
| 架构设计 | `邮箱聚合平台系统架构设计.md`（v1.6，17 章 + ADR-001~010） | 设计蓝图 |
| TS PoC | `email-aggregator/` | BFF/前端/对照实现（Node，零依赖） |
| Go 主干 | `email-aggregator-go/` | 生产主干（Go 1.26.6） |
| React 前端 | `email-aggregator-web/` | 对接 Go REST/WS 契约的前端层 |
| 验证文档 | 根目录 3 份 + `email-aggregator-go/` 4 份报告 | 证据链 |

---

## 二、环境基线（2026-08-29 实测）

| 工具 | 状态 | 版本/说明 |
|------|------|-----------|
| Go | ✅ | `go1.26.6 windows/amd64`（全局 PATH） |
| Node | ⚠️ 注意 | 默认 `node` = v20.20.2（**不支持 `--experimental-strip-types`**）；须用托管 Node 22：`C:\Users\addk1\.workbuddy\binaries\node\versions\22.22.2-2\node.exe`（v22.22.2） |
| npm | ✅ | 10.8.2 |
| Docker（Windows） | ⚠️ | CLI 29.6.2；Desktop daemon 未运行 |
| Docker（WSL Ubuntu） | ✅ | 原生 dockerd 运行中，含邮箱平台中间件栈（deploy-postgres/minio/opensearch/kafka） |

> **关键前置**：跑 TS 代码必须显式用 Node 22 路径；跑 Go 集成/前端直接用现有 PATH 即可。

---

## 三、三套实现验证矩阵（本轮实测全绿）

### 3.1 TS PoC（`email-aggregator/`）
| 检查 | 命令 | 结果 |
|------|------|------|
| 端到端 demo | `node22 --experimental-strip-types src/demo.ts` | **8/8 PASS**（含 ADR-009 多租户 3 项） |
| 单元测试 | `node22 --test --experimental-strip-types tests/phase0.test.ts` | **19/19 通过**（含集成层装配断言 #ts6/#ts18/#ts19） |
| 集成层形态 | `src/integration/integration.ts` + `main.integration.ts` | 已存在（PG/MinIO/OpenSearch/Kafka/WS/TenantKms 全套真实适配器，Phase 1+ 物理隔离） |

### 3.2 Go 主干（`email-aggregator-go/`）
| 检查 | 命令 | 结果 |
|------|------|------|
| 基准构建 | `go build ./...` | ✅ RC=0 |
| 静态检查 | `go vet ./...` | ✅ RC=0 |
| 单元测试 | `go test ./...` | ✅ 全绿（aigateway/ingest/syncsvc/api/notify/security/store/events/connector/search/cmd 均 ok） |
| 端到端 demo | `go run ./cmd/demo.go` | ✅ 闭环 PASS（含 tenantA 多租户） |

### 3.3 React 前端（`email-aggregator-web/`）
| 检查 | 命令 | 结果 |
|------|------|------|
| 生产构建 | `npm run build`（tsc + vite build） | ✅ dist 产出（JS 155.6KB / CSS 8.3KB） |

---

## 四、真实中间件端到端验证（本轮实跑，服务 :8082）

> 中间件栈（WSL Docker）：PG 15432 / MinIO 9000 / OpenSearch 9200 / Kafka 9092；服务 = `bin/integ-webui.exe`（`integration + webui` 双标签，同端口提供 SPA + REST + WS）。

| # | 验证项 | 证据 |
|---|--------|------|
| 1 | 服务健康 | `GET /api/health` → `{"ok":true}` |
| 2 | 账户列表 | `GET /api/accounts` → `acc_demo`、`acc_kafka` |
| 3 | 邮件列表 | `GET /api/mails?accountId=acc_demo` → 3 封 |
| 4 | 全文检索 | `GET /api/search?q=演示&accountId=acc_demo` → hits=2（OpenSearch） |
| 5 | SPA | `GET /` → 200，标题「邮箱聚合平台 · 前端」；`/assets/*.js|css` → 200 |
| 6 | WebSocket 实时推送 | `ws://127.0.0.1:8082/ws?accountId=acc_kafka` 触发 `/api/demo/push` → **收到 `new-mail` 帧**（RESULT_PASS） |
| 7 | Kafka 事件驱动摄取（核心链路） | 向 `mail-ingested` 生产 `mk_takeover_20260829` → 消费落 PG（mails 增至 5 封含该邮件）→ OpenSearch 索引（`q=接管` hits=1） |
| 8 | Kafka 消费 LAG | `consumer-groups --describe --group ingest` → CURRENT=LOG-END=4，**LAG=0** |
| 9 | MinIO 内容寻址 | `agg-mail/mail/` 3 个 sha256 对象（含本轮摄取正文） |

**结论：授权 → 采集 → 事件驱动摄取 → 落库/对象存储 → 索引 → 实时推送 全链路当前真实可跑。**

---

## 五、本轮文档一致性修复

| 文件 | 问题 | 处置 |
|------|------|------|
| `email-aggregator-go/README.md` | §真实集成注「本开发机当前无 Go 工具链」与上文「已装 1.26.6」自相矛盾 | 改为「已装，可直接执行」并补充一键启动/停止脚本说明 |
| `统一契约与双实现等价性验证.md` | 多处称「TS 集成适配层缺失（唯一不对齐项）」——实际 `integration.ts` 已落地并含 Phase 1+ 隔离 | 顶部加更新声明；§1/§7/§8/Phase1 待办/结论逐处修正；单测数 17→19 |
| `本地运行可行性评估.md` | 「当前没有任何代码真正连接这些中间件」已失效 | 加 2026-08-28 更新说明（Go 集成层已实连真实中间件并验证） |

---

## 六、当前运行状态与操作

- **服务当前在跑**：`http://localhost:8082`（SPA + REST + WS 同端口），本轮验证后保持运行。
- 启停：
  ```powershell
  powershell -ExecutionPolicy Bypass -File .\deploy\start-local.ps1       # 启动（默认 8082）
  powershell -ExecutionPolicy Bypass -File .\deploy\start-local.ps1 -Rebuild  # 强制重编
  powershell -ExecutionPolicy Bypass -File .\deploy\stop-local.ps1        # 停止宿主服务（中间件保留）
  wsl -d Ubuntu -u root -- docker ps                                       # 查看中间件容器
  ```
- **端口占用提示**：8080/8081 被其他项目进程占用（返回 401），本项目用 8082 互不冲突。

---

## 七、遗留项执行记录（2026-08-29 第二批次，三项全部完成）

| 遗留项 | 状态 | 执行结果与证据 |
|--------|------|----------------|
| **1. 版本控制** | ✅ 完成 | 项目根 `git init` + 全局 `.gitignore`（排除 Go 缓存 850MB/构建产物/node_modules/密钥 `.env`，保留 `.env.example`）；首次基线提交 `7fc9fd5`（144 文件，无密钥/产物泄漏校验通过） |
| **2. TS 集成层真实联调** | ✅ 完成 | 新增 `tests/integration_real_e2e.ts` 对真实中间件实跑 **7/7 PASS**（Wire 装配 / MinIO 内容寻址 Phase1+ 前缀与租户内去重 / PG 落库回读 / OpenSearch 索引检索 / Kafka 发布）；服务端 `:8083` 经 REST 回读 TS 写入数据（`/mails`、`/search` 均命中） |
| **3. CI 固化** | ✅ 完成 | `scripts/ci.ps1` 本地一键 CI（TS demo+单测+typecheck / Go build+vet+test+`-tags integration` / Web build）实跑**全绿**；`.github/workflows/ci.yml` GitHub Actions 三 Job（Go/TS/Web）就绪 |

### 2.x 联调中修复的 3 个真实 Bug

| # | Bug | 根因 | 修复 |
|---|-----|------|------|
| 1 | `fromAppConfig` 忽略 `pgDsn/objectEndpoint/openSearchAddr/kafkaBrokers` 四个 override | 工厂函数直接取 `app.*` 未回退 override → 装配"成功"但全部指向默认错误地址 | 改为 `overrides.x ?? app.x` |
| 2 | OpenSearchIndex 无法连自签 TLS 的 OpenSearch | Node `fetch` 拒绝自签证书 | 引入 `undici` Agent（`rejectUnauthorized:false`），https 地址下发 dispatcher（对齐 Go `InsecureSkipVerify`） |
| 3 | OpenSearch ping 恒失败 | 安全插件对 `_cluster/health` 也要求鉴权，ping 未带 Authorization → 401 | `ping()` 补 `this.headers()` |

> 修复后：`tsc --noEmit` 0 错误、TS 单测 19/19 保持通过；`main.integration.ts` 启动 err 日志干净（无 ping 告警）。

### 3.x CI 交付物

- **本地**：`powershell -ExecutionPolicy Bypass -File .\scripts\ci.ps1`（可加 `-Integration`），日志落 `.ci-logs/`。
- **远端**：`.github/workflows/ci.yml`（push/PR 触发，Go/TS/Web 三 Job 并行）。
- **备注**：真实中间件端到端联调（Go 8082 / TS 8083）依赖 Docker 栈，未纳入 CI，由 `tests/integration_real_e2e.ts` 本地运行。

---

## 八、仍开放项（非本轮范围）

4. **杂物清理（低风险、可重生成）**：`email-aggregator-go/` 根目录散落构建产物（`cmd.exe`/`server.exe`/`emailagg-server.exe`/`integ_server*.exe`/`integ_run*.log` 等）可归档或移除；`_inbox` 为临时目录已消失，无需处理。
5. **Phase 1 开发**（多协议 POP3/Exchange/Gmail 适配器、事件化、账户服务）与 **Phase 2+**（检索拆分、扩展、HA/DR、企业版私有化）属长期路线，建议按里程碑分阶段立项推进。

---

## 九、Git 提交记录

| 提交 | 内容 |
|------|------|
| `7fc9fd5` | 基线提交：Phase 0 全部实现 + 架构文档 + 接管状态报告 |
| `b379378` | TS 集成层真实联调修复（3 Bug）+ CI 固化（本地脚本 + GitHub Actions）+ 版本控制完善 |

---

*接管完成时间：2026-08-29 ｜ 验证方式：本机真实命令执行 + 真实中间件实跑 ｜ 报告路径：`D:\网站全栈项目\项目003\接管状态报告-20260829.md`*
