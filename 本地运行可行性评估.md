# 邮箱聚合平台 · 本地（开发机）安装与运行可行性评估

> 评估对象：`D:\网站全栈项目\项目003` 整个工作区（架构文档 + TypeScript PoC `email-aggregator/` + Go 主干 PoC `email-aggregator-go/`）
> 评估目标：能否在**普通本地开发机器**完成安装与运行；明确运行环境、安装步骤、本地限制与前置条件、最终结论。
> 评估方式：静态分析 `package.json` / `go.mod` / 全部源码 import、docker-compose，并**在本机实测运行 TS PoC**。

---

## 1. 项目架构与依赖环境总览

本项目不是一套"已经能跑的生产系统"，而是**一个邮箱聚合平台的设计蓝图 + 两份 Phase 0 PoC 骨架代码**：

| 资产 | 性质 | 依赖特征 |
|------|------|----------|
| `邮箱聚合平台系统架构设计.md` + `docs/` | 生产架构设计（ADR-001~010、分层、K8s/多AZ/HA-DR） | 设计目标，非代码 |
| `email-aggregator/`（TypeScript） | Phase 0 PoC：**BFF/前端/对照实现** | **零运行时依赖**（仅 Node 内置模块） |
| `email-aggregator-go/`（Go） | Phase 0 PoC：**生产主干骨架** | 基准构建**零外部依赖**（`go.mod` 无 require） |

**关键事实（已逐文件核实）**：
- 架构文档描述的生产栈（Kafka / PostgreSQL+Citus / OpenSearch / MinIO / Redis / KMS / K8s+Istio）是 **Phase 2–4 的目标**，~~当前没有任何代码真正连接这些中间件~~ **（2026-08-28 更新：Go 集成层 `-tags integration webui` 已实连真实 PG/MinIO/OpenSearch/Kafka 并端到端验证通过，详见 `email-aggregator-go/集成验证报告-20260828.md`）**。
- 两份 PoC 全部用 **InMemory 桩**（事件总线、元数据、内容、索引、KMS）实现，因此**设计为"无需任何外部服务即可在笔记本上跑通"**。
- 外部中间件仅在 `//go:build integration` 标签下或 docker-compose 中"预留"，默认不参与构建/运行。

---

## 2. 依赖环境逐项分析

### 2.1 TypeScript 版 `email-aggregator/`

- **`package.json`**：`dependencies` 为空；仅 `devDependencies`：`typescript ^5.5.0`、`@types/node ^22.0.0`。`engines.node >= 22.6`。
- **运行方式**：`node --experimental-strip-types src/demo.ts`（Node 22.6+ 原生去类型，无需 `tsc` 编译）。
- **源码 import 核实**：所有 import 仅两类——
  - 本地相对路径（`./model/...`、`./connector/...` 等）；
  - Node 内置模块：`node:http`、`node:crypto`。
  - `kafkajs` 仅出现在 `events/bus.ts` 的注释示例里，**运行时无任何第三方模块**。
- **存储/中间件**：`InMemoryEventBus` / `InMemoryMetadataStore` / `InMemoryContentStore` / `InMemorySearchIndex` / `InMemoryKms` —— 全内存，无 DB / MQ / 对象存储 / Docker。
- **结论**：运行时**零 npm 安装**（连 `typescript` 都不是运行所需，只用于 `tsc --noEmit` 类型检查）。

**✅ 实测**：本机用托管 Node `v22.22.2` 直接执行 `node --experimental-strip-types src/demo.ts`，未执行 `npm install`，结果 **ALL PASS ✅（退出码 0）**：
```
PASS  初始全量：种子 3 封入库
PASS  增量：注入 1 封后总数为 4
PASS  检索：'Welcome' 至少命中 1
PASS  推送：至少收到 1 次 new-mail
PASS  幂等：mail-ingested 无重复入库
```

### 2.2 Go 版 `email-aggregator-go/`

- **`go.mod`**：`module email-aggregator-go` / `go 1.22`，**无 `require` 段** → 基准构建纯标准库。
- **源码 import 核实**：
  - 默认构建（`go build ./...`）下所有 `.go` 仅 import 标准库（`context`、`crypto/*`、`net/http`、`encoding/*`、`sync`、`errors`、`strings`、`os`、`io`、`fmt`、`time`）及内部包。
  - 唯一含第三方 import 的文件是 `src/integration/integration.go`（`gorilla/websocket`、`jackc/pgx/v5`、`minio-go/v7`、`opensearch-project/opensearch-go/v2`），**被 `//go:build integration` 保护**，默认构建排除。受影响的只有 `src/integration/integration.go` 与 `cmd/server_integration.go` 两文件。
- **结论**：`go run ./cmd/demo.go` / `go build ./...` / `go test ./...` **零外部依赖、零联网**。

### 2.3 真实集成 / 生产栈（docker-compose）

两份项目都带 `deploy/docker-compose.yml`（TS 版含 postgres / pgbouncer / minio / opensearch / redpanda；Go 版结构相同）。这是给"接真实中间件联调"用的，**Phase 0 内存桩 PoC 不需要启动它**。

---

## 3. 本机工具链现状（实测）

| 工具 | 是否可用 | 版本 |
|------|----------|------|
| Node（托管） | ✅ | v22.22.2 |
| Node（系统） | ✅ | v24.18.1 |
| Go | ❌ | `go: command not found`（本机未安装） |
| Docker | ✅ | v29.6.2 |

> 即：**TS 版现在就能跑；Go 版当前缺 Go 工具链；Docker 已具备（用于可选的真实集成栈）。**

---

## 4. 运行环境 / 安装步骤 / 本地限制对照表

| 形态 | 运行环境 | 是否需要联网拉包 | 是否需要外部中间件/Docker | 当前本机可跑？ |
|------|----------|------------------|---------------------------|----------------|
| **TS PoC（内存桩）** | Node ≥ 22.6 | 否（零依赖） | 否 | ✅ 已实测通过 |
| **Go 基准 PoC（内存桩）** | Go 1.22+ | 否 | 否 | ⚠️ 需先装 Go 1.22+ |
| **Go 集成 PoC** | Go 1.22+ | 是（`go get` 5 个库） | 是（docker-compose：PG/MinIO/OS/Redpanda） | ❌ 缺 Go + 需联网 |
| **设计目标生产栈** | Go + Node + K8s/Istio/Citus/Patroni… | 是 | 是（云/多节点） | ❌ 普通本地机器无法承载 |

---

## 5. 安装与运行步骤

### A. TypeScript PoC（本机可立即运行，无需安装任何东西）
```bash
cd D:/网站全栈项目/项目003/email-aggregator
# 1) 无头自检（已实测 ALL PASS）
node --experimental-strip-types src/demo.ts
# 2) 常驻服务（REST :8080 + WebSocket）
node --experimental-strip-types src/main.ts
# 可选：仅类型检查才需要 npm install
npm install && npm run typecheck
```
前置条件：**仅 Node ≥ 22.6**（本机已满足）。无需网络、无需数据库、无需 Docker。

### B. Go 基准 PoC（需先装 Go 1.22+）
```bash
# 先安装 Go 1.22+（如用本环境 install_binary 工具或官网安装包）
cd D:/网站全栈项目/项目003/email-aggregator-go
go run ./cmd/demo.go        # 零依赖端到端演示
go build ./...              # 基准编译校验（纯标准库）
go test ./...               # 单元测试（全 InMemory）
```
前置条件：**本机安装 Go 1.22+ 工具链**。装好后零依赖即可运行。

### C. Go 集成 PoC（接真实中间件，进阶）
```bash
cd D:/网站全栈项目/项目003/email-aggregator-go
go get github.com/segmentio/kafka-go@latest github.com/jackc/pgx/v5@latest \
        github.com/opensearch-project/opensearch-go/v2@latest \
        github.com/minio/minio-go/v7@latest github.com/gorilla/websocket@latest
cd deploy && docker compose up -d        # 起 PG/MinIO/OpenSearch/Redpanda
go build -tags integration ./...
go run -tags integration ./cmd/server_integration.go
```
前置条件：**Go 1.22+ + 公网 + Docker Desktop**。

---

## 6. 本地运行限制与前置条件（重点）

1. **TS 版**：唯一硬前置是 **Node ≥ 22.6**（本机满足）。无网络、无数据库、无 Docker 要求，是"最本地友好"的形态。
2. **Go 版基准**：需在**本机安装 Go 1.22+**——这是当前唯一的阻塞点（`go` 命令不存在）。装好后即可零依赖运行。
3. **Go 集成 / 生产栈**：需要①本机有 Go；②**联网** `go get` 5 个第三方库；③**Docker Desktop 跑 compose** 起 4 个中间件（PostgreSQL/MinIO/OpenSearch/Redpanda，约需 4–8 GB 内存）。本机 Docker 已就绪，但缺 Go 与联网拉包。
4. **设计目标的生产架构**（K8s + Istio + Citus 分片 + Patroni HA + 多 AZ + 混沌演练 + 多租户/私有化）**绝非普通本地机器能承载**，那是云/IDC 环境的目标，Phase 0/1 阶段根本不涉及。

---

## 7. 最终结论

**能否在普通本地机器上顺利部署启动？——分两层回答：**

- ✅ **作为 PoC（代码真正的交付物）：完全可以，且本地友好。**
  - **TS 版现在就能在本机直接跑通**（已实测 ALL PASS），连 `npm install` 都不用——它是为"笔记本零依赖验证"而生的。
  - **Go 版基准 PoC**在装上 Go 1.22+ 后也能零依赖运行；当前本机缺 Go 工具链是**唯一阻塞**，装上即解。

- ⚠️ **作为架构文档描述的"生产系统"：普通本地机器无法"顺利部署启动"。**
  - 完整 HA/DR/多租户/私有化生产栈需要 K8s 集群与云资源，远超本地机器能力。
  - 但 Phase 0/1 的"接真实中间件"子集，可通过**单机 Docker Compose**（PG/MinIO/OpenSearch/Redpanda）在 16 GB+ 内存的本地机器上跑起来——前提是安装 Go 1.22+、具备公网拉包能力、并启动 Docker。

**一句话判断**：本项目代码库（Phase 0 PoC）**刻意做成零依赖、可本地运行**；普通本地开发机（有 Node 22.6 / 装好 Go 1.22+）即可安装并启动 PoC 验证闭环。架构文档中的全量生产栈不在"普通本地机器"范畴，但那是设计远期目标，不是当前代码的要求。

---

*评估日期：2026-08-15 ｜ 方法：静态依赖分析 + 本机实测 TS PoC 运行*

---

## 更新（2026-08-16）

- **Go 工具链已全局安装并验证通过**：托管二进制目录 `C:\Users\addk1\.workbuddy\binaries\go\go` 实为 **go1.26.6**（此前"本机未安装"仅指未进全局 PATH）。已复制至 `C:\Program Files\Go` 并写入系统级 Machine PATH。
- 修正后本机工具链状态：

| 工具 | 当前状态 | 版本 |
|------|----------|------|
| Node（托管） | ✅ | v22.22.2 |
| Node（系统） | ✅ | v24.18.1 |
| Go（全局） | ✅ 已安装并验证 | v1.26.6 |
| Docker | ✅ | v29.6.2 |

- Go 版基准 PoC 现已可本地直接运行：`go run ./cmd/demo.go` 端到端跑通（编排器闭环采集 → 发布 mail-ingested → IngestWorker 落库/索引/通知/审计）；`go build ./...` / `go test ./...` 全绿。
