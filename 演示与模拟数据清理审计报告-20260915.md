# 演示/模拟内容排查与清理审计报告

项目：项目003 · 邮箱聚合平台　｜　日期：2026-09-15　｜　范围：全量前端 + 后端 + 部署链路

---

## 0. 结论速览

| 问题 | 结论 |
|------|------|
| 构建产物是否最新？ | ✅ 是。`dist/` → `deploy/edgeone-app/assets/` → `.edgeone/` 上传包三方哈希一致，**不存在旧产物残留** |
| 是否有本地 mock 拦截器被误打包？ | ❌ 否。依赖中**没有任何 mock 库**（无 msw / axios-mock-adapter / json-server）。但存在等价物：前端硬编码的 `FALLBACK_ACCOUNTS` 兜底假账户 |
| 环境变量开关是否未切换？ | 🔴 **是，主因**。此前项目**根本不存在演示开关**，所有演示逻辑无条件执行 |
| CDN / 浏览器缓存？ | 🟡 次要因素。`index.html` 无 hash，可能命中 CDN 旧版本；资源文件有 hash 不受影响 |
| 持久化残留？ | 🔴 **是，隐藏主因**。旧种子数据已写进 Blob，改代码不会自动清除 |

> **一句话结论**：不是缓存或构建问题，而是**演示内容本来就是作为产品功能写死在源码里的**，且没有任何开关；叠加 Blob 里已持久化的历史种子数据，导致"改了代码线上依旧有演示内容"。

---

## 1. 全量扫描清单

### 1.1 部署产物（🔴 直接影响线上）

| 文件 | 位置 | 内容 | 作用 |
|------|------|------|------|
| 云函数 | `deploy/edgeone-app/cloud-functions/api/[[default]].js:112-175` | `initState()` 种子：7 封假邮件（m1/m2/m3/w1/w2/p1/p2）+ 3 个假账户 | 线上全部数据的来源 |
| 云函数 | 同上 `:396-425` | `handleDemoPush()` → `/api/demo/push` | 演示推信路由，生成"演示推送邮件" |
| 云函数 | 同上 `:358-393` | `handleAIChat()` 兜底：`[demo] 这是本地演示后端…`、`backend:'local-demo'`、`model:'demo-echo'` | AI 无上游时伪造回复 |
| 云函数 | 同上 `:338` | 同步接口返回"演示形态：Serverless 无长驻同步进程" | 文案含"演示" |
| 云函数 | 同上 `:454` | 凭据接口"演示形态：凭据仅登记，不发起真实 IMAP 连接" | 文案含"演示" |
| 前端 | `email-aggregator-web/src/App.tsx`（原 `:590`） | 「模拟收信」按钮 → `demoPush()` | 演示入口按钮 |
| 前端 | `src/App.tsx`（原 `:38-42`） | `FALLBACK_ACCOUNTS` 硬编码 3 个兜底账户 | **打包进生产的假账户数据** |
| 前端 | `src/components/AiChat.tsx:26` | `tenantId: 'demo-tenant'` | 租户 ID 写死为 demo |
| 前端 | `src/components/AiChat.tsx:54` | "（未配置模型时为本地演示回复）" | 演示文案 |
| 前端 | `src/api/client.ts:178-183` | `demoPush()` → `/demo/push` | 演示接口调用 |
| 前端 | `src/App.tsx:27` | `DEFAULT_ACCOUNT = 'acc_demo'` | 默认账户 ID 含 demo |

### 1.2 Go 主干（🟡 不影响当前线上，但影响容器/Render 部署）

| 文件 | 位置 | 内容 | 影响 |
|------|------|------|------|
| `cmd/server/main.go` | `:47-118` | `demoMails` 种子 7 封假邮件 | `//go:build !integration` = **默认构建即带演示数据** |
| `cmd/server/main.go` | `:137-184` | 30 秒定时自动生成演示邮件的 goroutine | 长驻进程才触发 |
| `src/api/server.go` | `:144`、`:614-637` | `/api/demo/push` 路由与处理器 | 与前端契约绑定 |
| `src/aigateway/demo.go` | 全文 | `LocalDemoProvider` 本地演示回复 | AI 降级路径 |
| `cmd/demo.go` | 全文（33 处） | 一次性场景演示 | 独立命令，不进 server |
| `src/connector/imap_mock.go` | 全文（24 处） | 进程内 mock IMAP 服务端 | **无 build tag，会编进生产二进制** |
| `src/connector/gmail_mock.go` | 全文（26 处） | mock Gmail API 服务端 | 同上 |
| `src/connector/graph_mock.go` | 全文（31 处） | mock Graph API 服务端 | 同上 |
| `src/connector/pop3_mock.go` | 全文（15 处） | mock POP3 服务端 | 同上 |

> ⚠️ `connector` 包下 4 个 `*_mock.go` **没有任何 `//go:build` 约束**（已确认全包无 build tag），
> 会被编译进生产二进制。它们只在测试中被显式调用，不会自动注入假数据，但会增大产物体积。

### 1.3 TS PoC（🟢 不参与部署）

`email-aggregator/src/demo.ts`、`src/app.ts`、`src/main.ts`、`src/config.ts`、`src/connector/imap.ts` 等共 12 个文件含 demo/mock 引用，属 PoC 参考实现，不影响线上。

### 1.4 构建与部署配置

| 文件 | 问题 |
|------|------|
| `.github/workflows/deploy-edgeone.yml` | 🔴 `deploy/edgeone-app/` **整个目录未纳入 Git**（`git status` 显示 `??`），CI 的 `paths` 过滤与 `cp` 步骤在全新 clone 上必然失败 |
| `email-aggregator-web/.env.production` | 🆕 本轮新增，生产构建关闭演示模式 |
| `deploy/edgeone-app/.env` | 🆕 本轮新增 `ENABLE_DEMO=false` |

---

## 2. 部署后仍然生效的原因分析（逐项排查）

### 2.1 ✅ 已排除：构建产物不是最新的

证据（SHA-256 前 16 位，同一份 JS 在三个位置完全一致）：

```
源 dist            e507c81044ec63f0   168118B
部署区 assets      e507c81044ec63f0   168118B
上传包 .edgeone    e507c81044ec63f0   168118B
```

`dist/` 已被正确替换，不存在旧产物残留。

### 2.2 ✅ 已排除：本地 mock 拦截器被误打包

`email-aggregator-web/package.json` 的 `dependencies` 只有 `react` / `react-dom`，
`devDependencies` 只有 vite 工具链 —— **没有任何 mock 库**。
`vite.config.ts` 的 proxy 也只对 `npm run dev` 生效，不会进生产产物。

### 2.3 🔴 主因一：不存在演示开关，演示逻辑无条件执行

这是根本原因。改造前：

- 云函数 `initState()` **每次冷启动都无条件播种** 7 封假邮件 + 3 个假账户
- `/api/demo/push` 路由无条件挂载
- AI 无上游时无条件返回 `[demo] …` 伪造回复
- 前端「模拟收信」按钮无条件渲染
- `FALLBACK_ACCOUNTS` 无条件作为初始 state

**即：演示内容 = 产品本体，不是残留。**

### 2.4 🔴 主因二：Blob 持久化导致历史种子数据残留

平台无托管数据库，数据存在 Blob 的 `state.json` 里。
**改代码不会清除已写入 Blob 的旧数据** —— 即使种子逻辑删掉，线上读到的仍是历史假邮件。
这也解释了为什么"删了账户刷新又出现"（上一轮已定位为 Serverless 多实例缓存，本次再叠加此问题）。

**处置**：把 `STATE_KEY` 从 `state.json` 升到 `state.v2.json`，等价于把旧库整体作废，无需手工删库。

### 2.5 🟡 次要：CDN / 浏览器缓存

- `index.html` 文件名无 hash，EdgeOne CDN 可能缓存旧版（旧版引用旧 JS hash）
- 带 hash 的 `assets/*.js` 不受影响
- 本地浏览器可能缓存旧 `index.html`

**处置**：部署后在控制台执行一次「缓存刷新」，并用 `Ctrl+Shift+R` 强刷验证。

### 2.6 🟡 部署日志告警（需知悉）

```
[cli] Pulling environment variables...
[cli] No environment variables found.
```

平台侧环境变量面板为空，**实际生效的是构建期从本地 `.env` 内联进函数包的值**。
已验证新包内确实注入成功（`.edgeone/cloud-functions/api-node/index.mjs:2376` → `"ENABLE_DEMO": "false"`）。
这意味着：**改 `.env` 必须重新构建部署才生效**，在控制台改环境变量是无效的。

---

## 3. 分类处置：可直接删除 vs 需改造为真实逻辑

### 3.1 🟢 A 类：可直接删除（零风险，已完成）

| 项 | 处置 | 风险 |
|----|------|------|
| 前端「模拟收信」按钮 | `DEMO_MODE=false` 时整块不渲染 | 无，纯 UI |
| `FALLBACK_ACCOUNTS` 假账户 | 生产置空数组 | 无，仅后端不可用时的兜底 |
| `tenantId: 'demo-tenant'` | 生产改为 `'default-tenant'` | 无 |
| AI 面板"未配置模型时为本地演示回复"文案 | 删除 | 无 |
| `DEFAULT_ACCOUNT = 'acc_demo'` | 生产改为 `'default'` | 无，库空时不会命中 |
| Blob 旧键 `state.json` | 升位到 `state.v2.json` | 无，等价于清库 |
| `deploy/edgeone-app/assets/index.html` 重复文件 | 删除（与根 `index.html` 重复） | 无 |

### 3.2 🟡 B 类：需改造为真实逻辑（有风险，已用开关隔离）

| 项 | 为什么不能硬删 | 当前处置 |
|----|---------------|---------|
| `/api/demo/push` | 前端 WS 实时推送联调依赖它 | 生产返回 **404**，本地仍可用 |
| 种子数据 | 删了页面就全空，无内容可展示 | 生产**空库起步**，由用户自行添加账户 |
| AI `[demo]` 兜底 | 删了会让 AI 面板直接抛错 | 生产返回 **503 + 明确错误**，不伪造答案 |
| 账户同步接口 | Serverless 无长驻进程，物理上无法真同步 | 生产文案改为"当前部署形态未提供邮件同步服务" |
| 凭据登记接口 | 无 KMS / 无真实 IMAP 连接 | 生产文案去掉"演示"字样的中性描述 |

### 3.3 🔴 C 类：架构限制，删除会破坏功能（暂缓）

| 项 | 限制 | 出路 |
|----|------|------|
| 真实 IMAP/Gmail/Graph 同步 | Serverless 无长驻进程，30 秒定时推送不可能 | 改用容器化/长驻部署（`deploy/container/Dockerfile` 或 `deploy/render-go-native.yaml`） |
| 真实数据库 | 平台无托管数据库，Blob 即数据库 | 同上，或接外部 PG |
| 真实多租户鉴权 | 当前无登录体系 | 需新增认证模块 |

### 3.4 风险评估

| 风险 | 等级 | 说明 | 缓解 |
|------|------|------|------|
| 生产页面变空 | 🟡 中 | 关闭种子后首屏无账户无邮件 | 已保留「添加账户」入口；空态由 `MailList` 的 `empty` 文案承载 |
| AI 面板报错 | 🟡 中 | 无上游时返回 503 | 平台已注入 `AI_GATEWAY_*`，实际会走真实模型；仅在上游故障时才 503 |
| 本地联调失效 | 🟢 低 | 生产关闭了演示 | `npm run dev` 不加载 `.env.production`，保持 `DEMO_MODE=true` |
| CI 失效 | 🟡 中 | `deploy/edgeone-app/` 未入库 | 见 §5 checklist 第 1 项 |

---

## 4. 本轮已执行的清理

### 4.1 云函数（`deploy/edgeone-app/cloud-functions/api/[[default]].js`）

1. 新增演示开关 `ENABLE_DEMO`（缺省 `true`，生产置 `false`），**每次请求按 `context.env` 刷新**，避免 Serverless 实例复用导致开关失效
2. `STATE_KEY` 升位 `state.json` → `state.v2.json`，作废 Blob 里的历史种子数据
3. `initState()` 在关闭演示时返回空库，不播种任何邮件/账户
4. `/api/demo/push` 关闭时返回 **404**
5. AI 无上游时返回 **503 + 明确错误**，不再伪造 `[demo]` 回复；上游调用失败返回 502
6. 同步接口 / 凭据接口文案去掉"演示"字样
7. `/api/health` 新增 `demo` 字段，便于线上判别当前形态

### 4.2 前端（`email-aggregator-web/`）

1. 新增 **编译期常量** `__DEMO_MODE__`（`vite.config.ts` 的 `define`，从 `.env.production` 读 `VITE_DEMO_MODE`）
   —— 用编译期替换而非运行期判断，保证死分支被 minifier 折叠、演示代码被 tree-shaking 掉
2. 新增 `src/global.d.ts` 声明 `__DEMO_MODE__`
3. `App.tsx`：「模拟收信」按钮与 `demoPush()` 调用整体移入条件块；`FALLBACK_ACCOUNTS` 生产置空；`DEFAULT_ACCOUNT` 生产改 `'default'`
4. `AiChat.tsx`：租户 ID 生产改 `'default-tenant'`；删除演示文案；错误映射改为原样展示后端中文错误
5. 新增 `email-aggregator-web/.env.production`（`VITE_DEMO_MODE=false`）

### 4.3 部署与测试

1. `deploy/edgeone-app/.env` 新增 `ENABLE_DEMO=false`；`.env.example` 同步补充说明
2. `smoke.mjs` 新增 `--prod` 生产形态套件（7 条断言），与默认套件（23 条）并行
3. `deploy.sh` / `deploy.ps1` / `.github/workflows/deploy-edgeone.yml` 均改为跑两套冒烟

---

## 5. 上线 checklist

### 部署前

- [ ] **1. 把 `deploy/edgeone-app/` 纳入 Git**（当前整目录 `??` 未跟踪，CI 在全新 clone 上必失败）
      `git add deploy/edgeone-app && git commit -m "feat: EdgeOne 部署工作区"`
      确认 `.env` 与 `.edgeone/` 已被 `.gitignore` 排除（`git check-ignore -v` 验证）
- [ ] 2. `email-aggregator-web/.env.production` 已存在且 `VITE_DEMO_MODE=false`
- [ ] 3. `deploy/edgeone-app/.env` 已含 `ENABLE_DEMO=false`（改完**必须重新构建部署**才生效）
- [ ] 4. `cd email-aggregator-web && npm run build`（确认产物体积变小、无 `模拟收信` 字样）
- [ ] 5. 同步产物：`dist/index.html` → `deploy/edgeone-app/index.html`，`dist/assets/*` → `deploy/edgeone-app/assets/`
- [ ] 6. `node smoke.mjs` **且** `node smoke.mjs --prod`（预期 23 + 7 全绿）

### 部署

- [ ] 7. `cd deploy/edgeone-app && edgeone makers deploy -n email-aggregator-p003 --json`
      （或一键：`powershell -ExecutionPolicy Bypass -File deploy.ps1`）
- [ ] 8. 记录返回的 Deployment ID 与 URL

### 部署后验证

- [ ] 9. **控制台执行一次缓存刷新**（EdgeOne → 项目 → 缓存刷新），否则可能命中旧 `index.html`
- [ ] 10. 浏览器 `Ctrl+Shift+R` 强刷打开
- [ ] 11. `/api/health` 返回 `"demo": false` 且 `accounts: 0`、`mails: 0`
- [ ] 12. 页面：无「模拟收信」按钮；账户列表为空；无种子邮件
- [ ] 13. `POST /api/demo/push` → **404**
- [ ] 14. AI 面板提问 → 返回真实模型回答，响应中**不含** `[demo]`
- [ ] 15. 「添加账户」可用，创建的账户刷新后仍在（验证 Blob 持久化）

### 回滚

- [ ] 16. 控制台 → 部署历史 → 将上一版本（如 `dpm9soclkdcw`）设为生产版本
- [ ] 17. 或本地 `git revert` 后重新执行部署流程

---

## 6. 附：本轮部署记录

| 项 | 值 |
|----|----|
| 项目 ID | `makers-jnzfzsp1nlt6` |
| 部署 ID（本次） | `dpmbwjeyzinf` |
| 上一版 | `dpm9soclkdcw` |
| 冒烟 | 演示形态 23/23 + 生产形态 7/7 |
| 运行时注入 | `ENABLE_DEMO=false`、`AI_GATEWAY_API_KEY` / `AI_GATEWAY_BASE_URL` 均已内联 |

## 7. 附：安全提醒

构建器会把 `.env` 的值**明文内联进函数包**（`.edgeone/cloud-functions/api-node/index.mjs`）。
虽已被 `.gitignore` 排除、不会入库，但该 Key 已出现在构建产物与本次会话中，
**建议后续在平台控制台轮换一次 `AI_GATEWAY_API_KEY`**，并优先改用平台环境变量面板注入。
