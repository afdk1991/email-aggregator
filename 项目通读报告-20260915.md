# 项目003 · 邮箱聚合平台 — 项目通读报告（2026-09-15）

> 通读范围：根目录 5 份文档 + `docs/` 2 份 + 三套实现源码树（TS PoC / Go 主干 / React 前端）+ CI 与部署资产 + git 历史与工作树状态。
> 目的：把「这个项目是什么、代码长什么样、现在处于什么状态、坑在哪」一次说清，供后续指令直接接续。

---

## 一、项目是什么

**统一收件箱 SaaS**。把分散在多家邮件服务商的邮箱（IMAP / POP3 / Exchange-EWS / Microsoft Graph / Gmail）聚合为**单一全文检索入口**，提供 REST + WebSocket + Web UI 三端，支持实时新邮件推送、租户级隔离、AI 邮件摘要。

不是"能跑的生产系统"，而是**一份生产级架构设计 + 三套真实可运行的 PoC/主干代码**：
架构文档描述了 K8s + Citus + Patroni + 多 AZ 的远期目标；代码层则是「默认零依赖、构建标签开真实中间件」的分层实现，并已接**真实 139 邮箱（2392 封）**跑通端到端。

---

## 二、仓库结构

| 目录 | 角色 | 规模 |
|------|------|------|
| `email-aggregator/` | **TS PoC**：BFF/前端/契约对照实现 | 34 个 ts/md/json，零运行时依赖 |
| `email-aggregator-go/` | **Go 主干（生产主选）** | 62 个非测试 .go + 33 个测试 .go，约 1.18 万行 |
| `email-aggregator-web/` | **React 前端**（Vite + TS） | 12 个源文件，约 1372 行 |
| `scripts/ci.ps1` | 本地一键 CI（TS/Go/Web 三套） | — |
| `.github/workflows/ci.yml` | GitHub Actions：Go / TS / Web / integration 四 Job | — |
| 根目录 md | 架构设计 v1.10、可行性评估、契约验证、接管状态报告 | 约 170 KB |

### 文档清单

| 文档 | 内容 |
|------|------|
| `邮箱聚合平台系统架构设计.md` | **主文档 v1.10**：18 章 + ADR-001~010（约 1040 行） |
| `接管状态报告-20260829.md` | **20 章执行实录**（633 行）：真实邮箱接入、TLS 根因、KEK 事故、WSL 磁盘丢失恢复 |
| `统一契约与双实现等价性验证.md` | TS/Go 在模型/状态机/事件/API/安全五类契约上的对齐矩阵 |
| `本地运行可行性评估.md` | 环境前置与依赖分析 |
| `docs/overview.md` · `docs/邮箱聚合平台_架构深化设计.md` | 总览与深化设计 |

---

## 三、架构骨架

### 3.1 十个 ADR（决策记录，是理解代码的钥匙）

| ADR | 决策 |
|-----|------|
| 001 | 演进式架构：模块化单体 → 按边界拆分微服务 |
| 002 | Kafka 为主总线（分区/可重放），至少一次 + 幂等 + DLQ |
| 003 | 存储三权分立：PG 元数据 / 对象存储内容 / OpenSearch 索引 |
| 004 | KMS 信封加密凭据（AES-256-GCM + KEK 包裹 + 轮换），密钥按租户派生 |
| 005 | 适配器注册表 + 统一邮件模型 `CanonicalMail` |
| 006 | **技术线锁定 Go 主干** |
| 007 | 有界多语言：Go 默认 + Node/TS 做 BFF/前端 + Java 仅作边界连接器例外区 |
| 008 | 触发 Java 例外区：企业版 Exchange/EWS 以独立 Java 边界服务落地 |
| 009 | 多租户隔离与数据驻留：共享栈 / 专属栈 / 私有化三形态，同一二进制参数化 |
| 010 | AI 能力路由网关：企业/私有强制自托管不出境，公共 SaaS 同意+脱敏后可走第三方 |

### 3.2 统一领域模型

`CanonicalMail` 是所有协议的收敛点（Go：`src/model/canonical.go`）：

```
CanonicalMail{ TenantID, ID, AccountID, Provider, Folder,
               From, To[], Cc[], Bcc[], Subject, BodyText, BodyHTML, Snippet,
               HasAttachment, Attachments[], InternalDate, SizeBytes,
               Cursor, Read, RawObjectKey }
```

- **幂等键** = `accountId|folder|messageId`，贯穿整条事件链做去重。
- `SyncCursor` 归一化各协议游标：IMAP 的 `uidValidity/uidNext/lastUid/modseq`、Gmail 的 `historyId`、Graph 的 `deltaLink`、POP3 的 `uidlWatermark`（走 `ProviderSpecific`）。
- 同步状态机 7 态 8 事件：`UNCONNECTED → AUTHORIZING → INITIAL_FULL → INCREMENTAL ⇄ THROTTLED / PAUSED / ERROR`，非法迁移抛错。

### 3.3 五条 Kafka 主题

`sync-tasks`（编排→采集）、`mail-ingested`（采集→落库/索引/通知/审计，**唯一扇出源**）、`mail-index`、`notifications`、`audit`。
分区键 = `accountId`（单账户有序），消费侧 `idempotencyKey` 去重。

### 3.4 Go 主干包结构（`src/` + `cmd/`）

```
model/      领域模型（CanonicalMail / Account / Connector 契约）
connector/  imap / pop3 / gmail / graph / ews + registry（每个协议均含 *_real.go + *_mock.go）
syncsvc/    编排状态机 + Orchestrator（含游标持久化）
ingest/     IngestWorker（消费 mail-ingested → 落库/对象存储/索引/通知/审计）
store/      元数据 + 内容（内容寻址）+ 账户注册表
search/     OpenSearch 索引（per-tenant 物理索引 mail-<tid>）
notify/     WebSocket Hub
security/   KMS 信封加密 Vault（本地持久化 keyring）+ OAuth2
aigateway/  AIProvider 接口 / 路由策略 Decide / 脱敏 Redactor / HTTP 后端
auth/       RBAC 矩阵 + AuthMiddleware 跨租户阻断 + OIDC(go:build sso) + 审计 sink
tenant/     X-Tenant-Id 解析与全链路透传
observ/     指标 + 结构化日志 + Prometheus 导出
integration/ 真实中间件适配（PG/MinIO/OpenSearch/Kafka/WS），go:build integration
api/        REST + WS + 静态 SPA
cmd/        demo / server_integration / sync_worker / ingest_worker / search_service / reindex / syncer
```

### 3.5 构建标签（关键设计约束）

| 标签 | 作用 |
|------|------|
| `integration` | 引入 pgx / minio-go / opensearch-go / kafka-go / gorilla-websocket，接真实中间件 |
| `webui` | 把前端 dist 嵌入单二进制（SPA + REST + WS 同端口） |
| `sync_worker` / `ingest_worker` / `search_service` | 三 worker 独立编译（Phase 2 服务拆分） |
| `sso` | 引入 go-oidc + oauth2，启用 Keycloak 验签 |

**默认 `go build ./...` 保持零外部依赖** —— 这是硬性约束，不要破坏。

### 3.6 API 契约（Go 集成服务 :8082）

| 端点 | 权限动作 |
|------|---------|
| `GET /api/health` | 公开（含 goroutines + metrics） |
| `GET /api/mails`、`/api/mails/{id}`、`/api/search` | `mail.read` |
| `POST /api/mails/{id}/read` | `mail.write` |
| `GET/POST /api/accounts`、`GET/PUT/DELETE /api/accounts/{id}` | `account.create` / `account.manage` |
| `POST /api/accounts/{id}/credentials`、`POST/GET /api/accounts/{id}/sync` | `account.manage` |
| `POST /api/ai/chat` | AI 路由网关（租户/敏感度/同意/脱敏） |
| `WS /ws?accountId=` | 实时推送 `new-mail` / `sync-state` / `error` |
| `POST /api/demo/push`、`GET /api/metrics` | 演示 / 可观测 |

---

## 四、前端（`email-aggregator-web/`）

React 18 + TS + Vite，仅依赖 react/react-dom（无 UI 库）。`App.tsx`（620 行）是单页主体，组件包括 MailList / MailDetail / SearchBar / AiChat / NotificationFeed / HealthBadge / ErrorBoundary / Icon。

已实现：账户 chips（含 provider 徽标、暂停/错误态）、内联添加账户、凭据录入、立即同步、已读下移未读上移稳定排序、WS 实时通知、AI 摘要面板、明暗主题（主题初始化前置到 `index.html` 防 FOUC）。

构建产物拷入 `email-aggregator-go/webroot/dist`，由 `webui` 标签嵌入 Go 二进制 —— **改前端后必须重建 dist 再重编 Go 服务**。

---

## 五、部署与运维资产

- `deploy/docker-compose.yml`：PG16 + MinIO + OpenSearch2 + Kafka（外部网络 `mixmlaal_mixmlaal-network`）。
- `deploy/migrations/001~006`：init / Citus 分片（分布键 `tenant_id`）/ read 列 / 账户表 / server_host / Citus worker 注册。
- `deploy/observability/`：Prometheus + Grafana（3 个看板）+ Alertmanager + SLO 告警规则。
- `deploy/docker-compose.citus.yml`（1C+2W 真实集群）、`multiaz.yml`（双 AZ + RUNBOOK 11 章）、`keycloak.yml` / `keycloak-prod.yml`（IdP）。
- `scripts/`：start-local / stop-local / drill-chaos（RPO/RTO）/ verify-citus-sharding / verify-keycloak-oidc / verify-sso-pkce 等 10 个 PowerShell 脚本。
- `packaging/install-windows.ps1`：Windows 服务安装。

---

## 六、当前真实状态（2026-09-15 实测）

| 项 | 状态 |
|----|------|
| git 分支 | `master`，与 `origin/master` 同步，最新提交 `c1b3bfd`（架构文档 v1.10 / Phase 4） |
| 工作树 | **不干净：29 项未提交**（详见下） |
| Docker | **无任何运行容器** |
| :8082 服务 | **无响应（环境中止，非代码问题）** |

### 未提交内容 = 在制品（Keycloak 生产化 + PKCE 真实浏览器 E2E）

**代码/配置（有价值，需收尾）**
- 新增 `src/api/auth_handler.go`（OIDC 登录/回调/登出 handler）
- 新增 `deploy/docker-compose.keycloak-prod.yml`、`keycloak/Caddyfile.prod`（HTTPS 反向代理）
- 新增 `scripts/verify-keycloak-prod.ps1`、`verify-sso-pkce.ps1`、`start-e2e-sso.ps1`、`wait-keycloak-ready.ps1`、`import-realm.ps1`
- 新增 `deploy/.env.keycloak-prod`（凭据环境变量注入，**零明文入库**）
- 修改 `src/auth/middleware.go`、`src/api/server.go`、`cmd/oidc_assemble_sso.go`、`cmd/oidc_assemble_nosso.go`、`cmd/server_integration.go`、`deploy/keycloak/realm-export.json`

**调试残留（建议清理）**
`deploy/bisect_fields.py`、`bisect_realm.py`、`bisect_roles.py`、`bisect_v2.py`、`bisect_v2b.py`、`convert_and_import.py`、`roundtrip2.py`、`roundtrip_format.py`、`test_flat_groups.py`、`zz-test.json`、`kc-token.json`、`keycloak/master-ref.json`、`keycloak/realm-export.v2.json`、`scripts/probe-kc-8443.ps1`

---

## 七、踩过的坑（下次直接避开）

1. **TS 必须用 Node ≥22.6**（`--experimental-strip-types`）。系统默认 `node` 是 v20，跑 TS 会直接失败。
2. **Kafka advertised 是容器名** `deploy-kafka-1:9092`，宿主机不可解析 → 必须 `KAFKA_DIAL_ADDR=127.0.0.1:9092`（Go `WithDial`）或 `KAFKA_HOST_REWRITE`（TS `socketFactory`）。缺了就是事件永远不落库，且**不报错**。
3. **KEK 内存态**：不设 `KMS_KEYRING_FILE` 会回退随机 KEK，进程重启后已入库凭据全部拆封失败。必须指向 `bin/kms-keyring.json`。
4. **139 邮箱仅支持 RSA 密钥交换**，Go 1.22+ 默认禁用 → `registry_default.go` 的 `tlsCfgFrom` 已显式列出 RSA 套件，别删。
5. **139 服务器对单条 IMAP 命令大响应做 ~64KB 静默截断**（命令仍返回 OK）→ 全量同步必须按序列号分批（每批 50），否则 2411 封只回来 99 封。
6. **Go map 迭代顺序随机**：POP3 的 `for seq, uidl := range uidls` 造成拉取顺序不确定、CI 偶发 flaky，已加 `sortedUIDLKeys()`。
7. **Git Bash MSYS 路径转换**会把容器内 `/migrations/...` 误转成本机路径 → 需 `MSYS_NO_PATHCONV=1`，或走 `docker compose exec`。
8. **WSL Ubuntu 磁盘（ext4.vhdx）已于 2026-09-03 整体丢失**，不可恢复。中间件已迁至宿主机 Docker Desktop，`start-local.ps1` 改双引擎（Docker Desktop 优先 + WSL 回退）。

---

## 八、验证过的战绩（不是纸面架构）

- Go：`go build/vet/test` 全绿；含 `integration` / `sso` / 三 worker 标签构建均通过。
- TS：demo 8/8、单测 19/19、POP3 e2e 10/10、Gmail e2e 13/13、真实中间件联调 7/7。
- 真实 139 邮箱：**2392 封落库、2387 封有正文（99.8%）**，read 旗标与服务器 `\Seen` 一致，游标持久化后重启走增量（15s vs 全量 40 分钟）。
- Keycloak 26.0 真实 IdP 联调 7 步全绿，跨租户隔离（acme vs globex）经验证。
- 剩余已知边界：5 封超大同/服务器持久拒发邮件无正文（服务器侧硬性限制，第三方客户端同样取不到）。

---

## 九、下一步可选项

1. **收尾在制品**：Keycloak 生产化（KC_DB=postgres + Caddy HTTPS）+ PKCE 真实浏览器 E2E，然后清理调试脚本并提交。
2. **恢复运行环境**：跑 `start-local.ps1` 拉起中间件与服务（当前环境是停的）。
3. 架构文档已列 Phase 5 开放项：Patroni 自动选主、Kafka RF=3、跨 region DR、PITR 备份、AI 生产化。
