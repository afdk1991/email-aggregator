# 项目 003 · 文档与开发文件索引

- **清点日期**：2026-09-16
- **范围**：`D:\网站全栈项目\项目003` 全量（已排除 `node_modules/`、`.gopath/`、`.git/`）
- **统计**：**38 份有效 Markdown 文档** + 开发基础设施文件若干
- **说明**：`.gopath/`（根目录 181 份 + `email-aggregator-go/` 116 份）与 `node_modules/` 内的 md 均为第三方依赖自带，**非本项目文档**，已剔除

---

## 一、文档总览（按主题分类）

### 1. 核心架构与总览（4 份）

| 文件 | 体积 | 修改 | 说明 |
|------|------|------|------|
| `邮箱聚合平台系统架构设计.md` | 89.7 KB | 09-12 | **主架构文档 v1.10**，18 章 + ADR-001~010。全项目最大的单份文档 |
| `docs/邮箱聚合平台_架构深化设计.md` | 13.2 KB | 08-14 | 早期架构深化稿 |
| `docs/overview.md` | 9.1 KB | 08-18 | 项目概览 |
| `README.md` | 5.0 KB | 08-29 | 仓库入口 |

### 2. 契约一致性（2 份）

| 文件 | 体积 | 说明 |
|------|------|------|
| `统一契约与双实现等价性验证.md` | 13.0 KB | TS / Go 双实现契约对齐矩阵 |
| `项目通读报告-20260915.md` | 12.2 KB | 全项目通读结论 |

### 3. 验证报告（7 份）

| 文件 | 体积 | 说明 |
|------|------|------|
| `接管状态报告-20260829.md` | **55.2 KB** | 20 章执行记录，真实邮箱接入与事故恢复踩坑全集 |
| `邮箱聚合平台-全项目部署验证报告-20260916.md` | 11.0 KB | **本次产出**，三形态部署验证 |
| `email-aggregator-go/可观测性层推进报告-20260830.md` | 7.4 KB | 可观测性层 |
| `email-aggregator-go/集成验证报告-20260828.md` | 6.7 KB | 集成栈验证 |
| `email-aggregator/TS_PoC_验证报告.md` | 6.3 KB | TS PoC |
| `email-aggregator-go/Go_PoC_验证报告.md` | 3.9 KB | Go PoC |
| `email-aggregator-go/补全验证报告-20260818.md` | 2.6 KB | 早期补全 |

### 4. 审计报告（5 份）

| 文件 | 体积 | 说明 |
|------|------|------|
| `.workbuddy/analysis/问题修复总结.md` | **43.8 KB** | 问题修复汇总 |
| `.workbuddy/analysis/架构分析报告.md` | 21.1 KB | 架构分析 |
| `email-aggregator-web/主页面深度审计报告-20260904.md` | 18.6 KB | 前端深度审计 |
| `演示与模拟数据清理审计报告-20260915.md` | 14.3 KB | 演示/mock 数据清理审计 |
| `email-aggregator-web/主页面审计报告-20260830.md` | 3.7 KB | 前端首轮审计 |

### 5. UI 设计与重设计（2 份）

| 文件 | 体积 | 说明 |
|------|------|------|
| `邮箱聚合平台-UI设计规范.md` | 16.8 KB | 10 章设计系统（栅格/间距/圆角/字号/配色/组件变体/无障碍） |
| `邮箱聚合平台-UI重设计改动说明.md` | 20.3 KB | 13 节逐文件改动说明 |

### 6. 部署运维（4 份）

| 文件 | 体积 | 说明 |
|------|------|------|
| `DEPLOY.md` | 16.5 KB | **部署总纲** |
| `email-aggregator-go/INTEGRATION.md` | 10.3 KB | 集成栈接入说明 |
| `本地运行可行性评估.md` | 10.1 KB | 本机环境前置评估 |
| `email-aggregator-go/deploy/multiaz/RUNBOOK.md` | 8.9 KB | 多可用区运维手册 |

### 7. 模块 README（5 份）

| 文件 | 体积 |
|------|------|
| `email-aggregator-go/README.md` | 7.7 KB |
| `email-aggregator/README.md` | 4.5 KB |
| `email-aggregator-go/packaging/README.md` | 3.5 KB |
| `email-aggregator-web/README.md` | 2.2 KB |
| `email-aggregator-go/deploy/migrations/README.md` | 2.2 KB |

### 8. 阶段规划（2 份，位于 `.trae/documents/`）

| 文件 | 体积 | 说明 |
|------|------|------|
| `phase2_opensearch_citus_observability.md` | 15.3 KB | Phase 2 规划 |
| `phase3_multiaz_rbac_sso.md` | 11.6 KB | Phase 3 规划 |

### 9. 工作记忆（7 份，位于 `.workbuddy/`）

`memory/2026-08-14.md` · `08-15` · `08-19` · `08-29` · `09-15` · `09-16` · `MEMORY.md`（长期约定）

---

## 二、开发基础设施文件

### 2.1 CI / 工作流（2 份）

| 文件 | 说明 |
|------|------|
| `.github/workflows/ci.yml` | 主 CI |
| `.github/workflows/deploy-edgeone.yml` | EdgeOne 自动部署 |

### 2.2 构建与部署脚本

| 文件 | 说明 |
|------|------|
| `scripts/ci.ps1` | 本地全量 CI（`-Integration` 需 Docker） |
| `email-aggregator-go/build.ps1` / `build.sh` | Go 主干构建（含标签组合） |
| `email-aggregator-go/deploy/start-local.ps1` / `stop-local.ps1` | 本地一键启停（默认 :8082） |
| `deploy/edgeone-app/deploy.ps1` / `deploy.sh` | EdgeOne 部署脚本 |
| `deploy/edgeone-app/smoke.mjs` | EdgeOne 云函数冒烟（demo 23 / prod 7 项） |
| `shots/live-verify.sh` | **线上全接口验证（36 项，curl 驱动）** |
| `shots/a11y-check.mjs` · `responsive-check.mjs` · `modal-check.mjs` | 前端 CDP 验证（无障碍 / 5 断点 / 模态结构） |

### 2.3 集成与验证辅助脚本（`email-aggregator-go/scripts/`）

`setup-integration.ps1` · `drill-chaos.ps1` · `verify-citus-sharding.ps1` ·
`verify-keycloak-oidc.ps1` · `verify-keycloak-prod.ps1` · `verify-sso-pkce.ps1` ·
`start-e2e-sso.ps1` · `wait-keycloak-ready.ps1` · `probe-kc-8443.ps1`

> 这 9 个脚本**均需 Docker 环境**才可运行，本机属阻断项。

### 2.4 容器编排（11 份）

| 位置 | 文件 |
|------|------|
| `deploy/container/` | `docker-compose.yml` · `Dockerfile` · `.env.example` |
| `email-aggregator-go/` | `Dockerfile` · `deploy/docker-compose.yml` |
| `email-aggregator-go/deploy/` | `docker-compose.citus.yml` · `.multiaz.yml` · `.keycloak.yml` · `.keycloak-prod.yml` |
| `email-aggregator-web/` | `docker-compose.yml` · `Dockerfile` |
| `email-aggregator/` | `deploy/docker-compose.yml` |

### 2.5 部署平台配置

| 文件 | 说明 |
|------|------|
| `render.yaml` | Render 部署 |
| `deploy/render-go-native.yaml` | Render Go 原生部署 |
| `deploy/edgeone-app/edgeone-project.json` | EdgeOne 项目绑定（已修正为 `makers-1qp5qzh9qml7`） |
| `deploy/edgeone-app/.edgeone/project.json` | CLI 级项目绑定 |
| `deploy/edgeone-app/cloud-functions/api/[[default]].js` | 云函数入口（18.1 KB） |
| `deploy/edgeone-app/.edgeone/cloud-functions/api-node/index.mjs` | 云函数打包产物（190.6 KB） |

---

## 三、三套实现结构

| 目录 | 角色 | 关键文件 |
|------|------|----------|
| `email-aggregator/` | TS PoC / 契约对照 | `src/main.ts` · `demo.ts` · `main.integration.ts` · `tests/`（19 项） |
| `email-aggregator-go/` | **Go 主干（生产主选）** | `cmd/server/` · `src/{api,connector,syncsvc,ingest,search,store,aigateway,auth,security,tenant,notify,observ,events,model}/` |
| `email-aggregator-web/` | React + TS + Vite 前端 | `src/{App.tsx,styles.css,components/,pages/}` |

---

## 四、Git 版本控制现状

### 4.1 文档入库情况

| 类别 | 数量 | 明细 |
|------|------|------|
| ✅ 已入库 | 19 份 | 早期架构、各模块 README、早期验证报告、契约验证 |
| ❌ **未入库** | **6 份** | 见下表 |

**未入库的 6 份（含全部近期成果）**：

```
DEPLOY.md
演示与模拟数据清理审计报告-20260915.md
项目通读报告-20260915.md
邮箱聚合平台-UI设计规范.md
邮箱聚合平台-UI重设计改动说明.md
邮箱聚合平台-全项目部署验证报告-20260916.md
```

### 4.2 🔴 关键问题：EdgeOne 部署链路完全未纳入版本控制

```
?? .dockerignore
?? .github/workflows/deploy-edgeone.yml   ← CI 工作流本身未入库
?? deploy/                                 ← 整个部署目录未入库
```

**后果**：全新克隆的仓库里**既没有部署脚本，也没有自动部署工作流**。`deploy-edgeone.yml` 引用的路径全部不存在 → CI 必然失败。

### 4.3 ⚠️ 流程脆弱点：`webroot/dist/` 被 gitignore，但 `webui` 构建依赖它

`.gitignore` 含 `**/webroot/dist/`，而 `//go:build webui` 通过 `embed` 读取该目录。

**这解释了本次发现的"内嵌 SPA 滞后一整版"缺口** —— 它是构建产物、不受版本控制、且需要**手工拷贝**才能更新。一次手滑就漂移，且不会有任何构建报错（目录里有旧文件就能编译通过）。

### 4.4 已正确忽略（无需处理）

`.gopath/` · `.gocache/` · `bin/` · `release/` · `node_modules/` · `dist/` · `*.log` · `.env`（保留 `.env.example`）· `.workbuddy/` · `.ci-logs/`

另：`deploy/edgeone-app/.edgeone/` 已被该目录下的 `.gitignore` 忽略。经逐字节比对确认，
`.edgeone/assets/` 是 EdgeOne CLI 在上传前自动生成的**暂存镜像**，与部署根目录**8 个同名文件全部字节一致**
（`deploy.ps1` / `deploy.sh` / `smoke.mjs` / `index.html` / `edgeone-project.json` / `package.json` / JS / CSS）。
**属正常中间产物，非重复配置，无需处理。**

### 4.5 📌 产物同步覆盖面对照（`webroot/dist` 缺口的制度性成因）

| 目标位置 | 谁负责同步 | 是否自动 | 状态 |
|----------|-----------|---------|------|
| `deploy/edgeone-app/`（云上 EdgeOne） | `deploy.ps1` 第 **[2/4] 步**：`Copy-Item dist\* → assets\` | ✅ **自动** | 一直正常 |
| `email-aggregator-web/dist/` | `vite build` 自身 | ✅ 自动 | 正常 |
| `email-aggregator-go/webroot/dist/`（`webui` 内嵌） | `email-aggregator-go/build.ps1` / `build.sh` **第 [2/4] 步**：`Remove-Item webroot/dist` + `Copy-Item $WEB/dist webroot/dist` | ⚠️ **仅在全量构建时** | **本次因绕过该脚本直接 `go build -tags webui` 而滞后一整版** |

**⚠️ 重要更正（2026-09-16 复核）**：本节初稿曾判定 `webroot/dist`「无任何脚本负责同步」，
**该结论有误，现予更正**。经复核 `email-aggregator-go/build.ps1:36-38` 与 `build.sh:46-48`，
两个脚本的第 [2/4] 步**都包含完整同步逻辑**（先删旧目录再整目录拷贝）。

**正确的成因分析**：问题不在「缺少自动化」，而在「**同步挂在全量构建脚本里，日常增量构建会绕过它**」。
`build.ps1` 的完整流程是 [1/4] `npm ci` + `npm run build` → [2/4] 拷 `webroot/dist`
→ [3/4] `CGO_ENABLED=0 -tags webui` 交叉编译 → [4/4] 输出到 `release/`。
只要**走这个脚本**就不会漏。而直接 `go build -tags webui ./cmd/server` 做快速验证时，
不经过任何同步步骤，`webroot/dist` 便保持上次的状态 —— 目录里有旧文件照样编译通过，于是静默漂移。

**因此建议改为**：不必再往 `build.ps1` 加同步（已有），而是
(a) 增加一个**轻量 `sync-web.ps1`**（只做拷贝 + SHA-256 校验，不触发完整构建）供日常使用；
或 (b) 在 `webui` 构建前置一步 `go:generate` / 预检脚本，校验 `webroot/dist` 与
`../email-aggregator-web/dist` 的 SHA-256 是否一致，不一致即报错中止。
(b) 更稳妥——它把「静默漂移」变成「显式失败」。

---

## 五、发现的问题清单

| 级别 | 问题 | 位置 | 建议 |
|------|------|------|------|
| 🔴 | EdgeOne 部署链路未入库 | `.github/workflows/deploy-edgeone.yml`、`deploy/`、`.dockerignore` | 提交入库，否则 CI 在全新克隆上必失败 |
| 🔴 | 6 份核心文档未入库 | 根目录（见 4.1） | 提交入库 |
| 🟡 | `webui` 内嵌 SPA 易静默滞后（**成因已更正**：非缺自动化，而是同步步骤挂在全量构建脚本内，直接 `go build -tags webui` 会绕过） | `email-aggregator-go/webroot/dist/` | 加 `sync-web.ps1` 轻量同步，或在构建前置 SHA-256 预检（详见 4.5） |
| 🔴 | **`.env` 族文件未被 gitignore 覆盖** | `.gitignore` 规则为 `.env` / `**/.env`，匹配不到 `.env.keycloak-prod`、`.env.production`；前者含 `KC_ADMIN_PASSWORD`/`KC_DB_PASSWORD`/`OIDC_CLIENT_SECRET` 明文 | 加 `**/.env.*` 规则并保留 `!**/.env.example`；`.env.production` 因只含非敏感构建开关，应改为入库 |
| 🔴 | **`/api/demo/push` 无鉴权、无条件注册** | `email-aggregator-go/src/api/server.go:144`（注册）/`:616`（实现）；未包 `s.require()`，无 demo 模式判断，默认注入 `acc_demo` | Go 侧需与 EdgeOne 云函数的 `ENABLE_DEMO` 开关对齐（云函数版已做，Go 版未做） |
| 🔴 | **未入库源码会导致「半提交」后仓库不可编译** | `email-aggregator-go/src/api/auth_handler.go` 从未提交，但同批已修改的 `cmd/oidc_assemble_nosso.go` 已引用其导出的 `api.SSORouter` | 提交时**必须同时**包含新文件；`git commit -am` 会漏掉未跟踪文件 |
| 🟢 | 调试残留 10 个文件 | `email-aggregator-go/deploy/`：`bisect_fields.py`·`bisect_realm.py`·`bisect_roles.py`·`bisect_v2.py`·`bisect_v2b.py`·`roundtrip2.py`·`roundtrip_format.py`·`test_flat_groups.py`·`convert_and_import.py`·`kc-token.json`·`zz-test.json` | 清理；`kc-token.json` 更应尽快删（含凭据） |
| 🟢 | 陈旧日志文件 | 根目录 `ab.log`·`ab-shot.log`·`ab-open.log`；`deploy/edgeone-app/smoke*.log` | 已在 gitignore 覆盖，可留可删 |

---

## 六、文档阅读顺序建议

**新人上手**：
`README.md` → `docs/overview.md` → `邮箱聚合平台系统架构设计.md` → 对应实现目录的 `README.md`

**部署运维**：
`DEPLOY.md` → `本地运行可行性评估.md` → `email-aggregator-go/INTEGRATION.md` → `deploy/multiaz/RUNBOOK.md`

**前端开发**：
`邮箱聚合平台-UI设计规范.md` → `邮箱聚合平台-UI重设计改动说明.md` → `email-aggregator-web/README.md`

**追溯历史决策**：
`接管状态报告-20260829.md` → `统一契约与双实现等价性验证.md` → `.workbuddy/memory/` 日志

---

*索引生成：2026-09-16 · 清点基于文件系统实扫，非记忆推断*
