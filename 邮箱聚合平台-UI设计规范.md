# 邮箱聚合平台 · UI/UX 设计规范 v1.0

> 适用范围：`email-aggregator-web`（React 18 + TypeScript + Vite，纯 CSS，无 UI 框架）
> 制定日期：2026-09-15
> 设计语言代号：**Inbox Calm**（克制、信息密度可控、状态语义明确）

---

## 0. 设计原则

| 原则 | 含义 | 落地约束 |
|------|------|----------|
| **内容优先** | 界面装饰不参与竞争，邮件内容是绝对主角 | 面板底色与页面底色对比度 ≤ 3%，靠 1px 边框与阴影分层 |
| **状态即色彩** | 颜色只用于表达状态与语义，不做纯装饰 | 品牌蓝仅用于「可交互 + 主操作」，语义色仅用于在线/警告/错误/未读 |
| **密度分级** | 列表高密度、详情中密度、表单低密度 | 三套纵向间距节奏：紧凑 4/8、标准 12/16、宽松 24/32 |
| **焦点永不丢失** | 键盘用户与鼠标用户获得同等清晰的操作反馈 | 所有可交互元素均有 `:focus-visible` 描边，且描边不依赖颜色单独区分 |
| **动效克制** | 仅用位移/淡入表达「元素从哪来」 | 时长 120–200ms，缓动 `cubic-bezier(0.16,1,0.3,1)`，尊重 `prefers-reduced-motion` |

---

## 1. 栅格系统

### 1.1 基础栅格

- **单位**：4px 基线栅格。所有间距、尺寸均为 4 的整数倍。
- **主容器**：`max-width: 1440px`，水平居中，左右内边距随断点变化。
- **宏观布局**：三段式 —— `顶栏（Header） / 侧栏 + 主区（Sidebar + Main） `。

```
┌──────────────────────────────────────────────────────────────┐
│  Header  (sticky, 高 64px, 全宽, 底边框)                       │
├────────────┬─────────────────────────────────────────────────┤
│            │  Toolbar (刷新 · 检索 · 状态)                     │
│  Sidebar   ├────────────────────────────┬────────────────────┤
│  240px     │  MailPane  (1fr, min 480px)│  AiPane 380px      │
│  (sticky)  │                            │  (sticky)          │
│            │                            │                    │
└────────────┴────────────────────────────┴────────────────────┘
```

### 1.2 列宽定义

| 区域 | 宽度 | 说明 |
|------|------|------|
| Sidebar | `240px` 固定 | ≥900px 时 Sticky 跟随；<900px 折叠为横向 chips |
| MailPane | `minmax(0, 1fr)` | 弹性主体，`min-width: 0` 防止溢出 |
| AiPane | `380px` 固定 | 通知面板 + AI 助手；<1280px 降至 340px；<1080px 下移堆叠 |
| 右侧信息列 | `360px` | 详情模态内的元信息列宽 |

### 1.3 断点（移动优先语义，实现为 max-width 覆盖）

| 断点 | 目标设备 | 布局变化 |
|------|----------|----------|
| `≥1280px` | 桌面宽屏 | 三列完整布局 |
| `1080–1279px` | 桌面/横屏平板 | AiPane 收窄至 340px |
| `900–1079px` | 平板竖屏 | AiPane 下移，Content 变单列 |
| `640–899px` | 大屏手机 | Sidebar 折叠为顶部横向滚动 chips |
| `<640px` | 手机 | Header 两行堆叠，工具栏全宽，详情模态全屏化 |

---

## 2. 间距尺度（Space Scale）

| Token | 值 | 典型用途 |
|-------|-----|----------|
| `--sp-1` | `4px` | 图标与文字间距、徽标内边距 |
| `--sp-2` | `8px` | 按钮内元素间距、紧凑列表行内间距 |
| `--sp-3` | `12px` | 列表项内边距、卡片内元素间距 |
| `--sp-4` | `16px` | 卡片内边距、模块间标准间距 |
| `--sp-5` | `20px` | 主区间距、页面横向内边距（桌面） |
| `--sp-6` | `24px` | 区块间隔、模态内边距 |
| `--sp-8` | `32px` | 大区块分隔、错误页留白 |
| `--sp-10` | `40px` | 极端留白（空状态） |

**间距使用铁律**

1. **同层级间距必须相等**：同一容器内所有兄弟节点用同一个间距 Token。
2. **容器内 < 容器间**：卡片内边距（16px）不得大于卡片之间的间距（20px），否则分组关系会视觉反转。
3. **分组用间距，不用分割线**：优先用 24px 留白分组；仅当纵向空间受限时才用 1px `--border` 分割。

---

## 3. 圆角尺度（Radius Scale）

| Token | 值 | 用途 |
|-------|-----|------|
| `--radius-xs` | `4px` | 徽标、极小标签、内联代码 |
| `--radius-sm` | `6px` | 输入框、次级按钮、列表项悬浮态 |
| `--radius-md` | `10px` | 主按钮、卡片内层容器 |
| `--radius-lg` | `14px` | 面板、模态、侧栏 |
| `--radius-xl` | `20px` | 大容器、错误页卡片 |
| `--radius-pill` | `999px` | 状态徽标、chips、头像 |

**嵌套规则**：内层圆角 = 外层圆角 − 内边距。例：面板 `--radius-lg(14px)` + 内边距 4px ⇒ 内层用 `--radius-sm(6px)`，视觉同心。

---

## 4. 字号与行高（Type Scale）

字族栈：`system-ui, -apple-system, 'Segoe UI', 'PingFang SC', 'Microsoft YaHei', sans-serif`
等宽字族栈：`ui-monospace, SFMono-Regular, Menlo, Consolas, monospace`（用于账户 ID、idempotencyKey）

| Token | 字号 / 行高 | 字重 | 用途 |
|-------|-------------|------|------|
| `--fs-display` | `24px / 1.25` | 700 | 页面主标题（模态中的邮件主题） |
| `--fs-h1` | `18px / 1.4` | 600 | 顶栏品牌名、区块大标题 |
| `--fs-h2` | `15px / 1.45` | 600 | 面板标题（收件箱、AI 助手、实时通知） |
| `--fs-h3` | `13px / 1.5` | 600 | 子区块标题、侧栏分组标题（配 `letter-spacing: 0.04em` + 大写或小字距） |
| `--fs-body` | `14px / 1.6` | 400 | 正文、邮件正文、AI 消息 |
| `--fs-sm` | `13px / 1.55` | 400 | 列表元信息、辅助说明 |
| `--fs-xs` | `12px / 1.5` | 500 | 徽标、时间戳、计数 |
| `--fs-2xs` | `11px / 1.4` | 600 | 极紧凑徽标（仅限 chips 计数） |

**层级规则**：任意时刻页面上可见的字号层级不超过 4 个；相邻层级字号差 ≥ 1px 且字重必须不同（400 vs 600），避免"看不出谁更重要"。

---

## 5. 配色与状态色

### 5.1 中性色（浅色主题）

| Token | 值 | 用途 |
|-------|-----|------|
| `--bg` | `#f6f8fb` | 页面底色 |
| `--panel` | `#ffffff` | 卡片/面板底色 |
| `--panel-2` | `#f1f5f9` | 次级底色（悬浮、未读行、内嵌容器） |
| `--panel-3` | `#e8eef6` | 三级底色（输入框底、chip 底） |
| `--border` | `#e2e8f0` | 常规边框 |
| `--border-strong` | `#cbd5e1` | 强调边框、悬浮态边框 |
| `--text` | `#0f172a` | 主文本 |
| `--text-2` | `#475569` | 次文本 |
| `--muted` | `#7b8798` | 辅助文本（对比度 ≥ 4.5:1 on `--bg`） |

### 5.2 品牌与语义色

每个语义色提供 `--x`（主）/ `--x-text`（文本，保证 AA 对比度）/ `--x-bg`（浅底）/ `--x-border`（描边）四个 Token，组件只允许引用这四者。

| 语义 | 主色 | 文本色 | 浅底 | 使用场景 |
|------|------|--------|------|----------|
| **primary（品牌）** | `#2563eb` | `#1d4ed8` | `#eff6ff` | 主按钮、链接、焦点环、可交互边框 |
| **ok（在线/成功）** | `#16a34a` | `#15803d` | `#f0fdf4` | 后端在线、WS 已连接、同步成功 |
| **warn（进行中/警告）** | `#d97706` | `#b45309` | `#fffbeb` | 重连中、同步中、检测中、同步失败提示 |
| **err（错误/未读）** | `#dc2626` | `#b91c1c` | `#fef2f2` | 后端离线、错误提示、未读计数 |
| **pending（中性等待）** | `#7b8798` | `#475569` | `#f1f5f9` | 未连接、未知状态 |

**状态色使用铁律**

1. 一个状态只能用一个语义色，禁止混用（例：WS 未连接不得用 `--err`，那会让常态噪声看起来像故障）。
2. 浅底 + 深字是最小实现，只承载"标签级"信息；要强调时加左侧 3px 色条，不加整块实色背景。
3. 语义色与品牌蓝同时出现时，同一区域内品牌色元素不超过 1 个，保证主操作唯一。

### 5.3 暗色主题

暗色主题仅覆盖 Token 值，不新增选择器。原则：

- 底色改为深色层次（`--bg #0b1220` / `--panel #151e2e` / `--panel-2 #1d2739`），靠亮度差分层，不靠边框。
- 品牌色提亮为 `#60a5fa`，语义色同步提亮，保证在深底上对比度 ≥ 4.5:1。
- 不再使用硬编码色值，全部走 Token（v1.0 已清理 9 处硬编码）。

---

## 6. 组件变体规范

### 6.1 按钮（Button）

统一基类，通过修饰类区分为 5 个变体，尺寸分 3 档。

| 变体 | 类名 | 底色 | 文字 | 边框 | 用途 |
|------|------|------|------|------|------|
| 主操作 | `.btn--primary` | `--primary` | `#fff` | 无 | 每屏最多 1 个：搜索、创建、发送 |
| 次操作 | `.btn--secondary` | `--panel` | `--text` | `--border` | 刷新、预览、同步、占位 |
| 幽灵 | `.btn--ghost` | 透明 | `--text-2` | 无 | 图标按钮、收起/展开、清空 |
| 危险 | `.btn--danger` | `--panel` | `--err-text` | `--err-border` | 删除（默认弱化，悬浮实色） |
| 强调轮廓 | `.btn--outline` | 透明 | `--primary` | `--primary` | 添加账户入口、清除检索 |

| 尺寸 | 高度 | 内边距 | 字号 | 用途 |
|------|------|--------|------|------|
| `--sm` | `28px` | `0 10px` | 12px | 侧栏操作、内联按钮 |
| `--md` | `36px` | `0 14px` | 13px | 默认：工具栏、详情操作 |
| `--lg` | `44px` | `0 20px` | 14px | 主操作、移动端所有按钮 |

- 过渡：`background/border-color/color 140ms`。
- 悬浮：底色降一级亮度（primary → `#1d4ed8`）或边框提到 `--border-strong`。
- 禁用：`opacity: .45` + `cursor: not-allowed`，**不做位移动画**。
- 图标 + 文字间距固定 `--sp-2(8px)`。

### 6.2 输入（Input / Select / Textarea）

| 属性 | 值 |
|------|-----|
| 高度 | `36px`（`--md`），多行 textarea `min-height: 72px` |
| 内边距 | `0 12px`（单行）/ `10px 12px`（多行） |
| 边框 | `1px solid --border`，圆角 `--radius-sm` |
| 底色 | `--panel`（浅色）/ 暗色用 `--panel-2` |
| placeholder | `--muted` |
| 焦点 | 边框转 `--primary` + `3px` 半透明光环 `box-shadow: 0 0 0 3px rgba(37,99,235,.15)`，**不使用 outline 位移** |

### 6.3 徽标 / 状态点（Badge / StatusDot）

| 变体 | 实现 | 用途 |
|------|------|------|
| `.badge--ok/--err/--warn/--idle` | 浅底 + 深字 + `--radius-pill` + 12px 字 | 后端状态、WS 状态 |
| `.status-dot` | 6px 圆点 + 同色 30% 光晕 | 与文字组合的状态指示 |
| `.count-badge` | 实色 + 白字 + `min-width: 18px` | 未读计数（≤99 显示数字，>99 显示 `99+`） |

### 6.4 列表项（List Item）

- **邮件列表项**：固定高度 `76px`（虚拟滚动常量 `ROW_HEIGHT` 同步为 76），三段式 —— 主题行（14px/600）→ 元信息行（13px/400 muted）→ 摘要行（13px/400 muted，单行省略）。
- **未读标识**：左侧 3px `--primary` 竖条（非整块背景翻转）+ 主题加粗 + 蓝点，三重冗余（符合 WCAG 1.4.1 不只靠颜色）。
- **悬浮**：底色 `--panel-2`，左侧竖条由透明过渡到 `--primary`。
- **选中态**：底色 `--primary-bg`，左侧竖条实心。

### 6.5 面板（Panel / Card）

- 结构：`--panel` 底 + `1px --border` + `--radius-lg` + `--shadow-sm`。
- 面板标题区统一 `--fs-h2`，与内容区之间用 `--sp-4(16px)` 分隔。
- 面板内不再嵌套同款带边框卡片（避免"盒中盒"），改用 `--panel-2` 平底容器区分。

### 6.6 模态（Modal）

- 遮罩：`rgba(15,23,42,.5)` + `backdrop-filter: blur(2px)`。
- 面板：`max-width: 720px`，`max-height: 84vh`，圆角 `--radius-xl`，`--shadow-lg`。
- 结构：固定头部（主题 + 关闭按钮）→ 可滚动正文 → 固定底部操作条。
- `<640px` 时全屏化（`inset: 0`，圆角 0）。

### 6.7 Toast

- 位置：右下角，`bottom: --sp-6`，`right: --sp-6`，堆叠间距 `--sp-2`。
- 形态：`--panel` 底 + 左侧 3px 语义色条 + `--shadow-md`。
- 进入动画：`translateY(8px) → 0` + 淡入，160ms；离场反向，120ms。

---

## 7. 信息层级与视觉动线

### 7.1 三层视觉权重

| 层级 | 手段 | 示例 |
|------|------|------|
| **一级**（主操作） | 实色底 + 白字 + 唯一 | 「搜索」「创建账户」「发送」 |
| **二级**（常用操作） | 描边 + 常规字重 | 「刷新邮件」「立即同步」 |
| **三级**（低频/破坏性） | 幽灵或弱化文字色 | 「删除账户」「标记未读」 |

### 7.2 阅读动线（Z 型 + F 型）

1. 顶栏左侧：品牌（视线起点）
2. 顶栏右侧：状态 + 主操作「+ 添加账户」
3. 侧栏：账户列表（纵向扫描）
4. 主区顶部：工具栏（左对齐操作，右侧检索框收窄）
5. 邮件列表：从左到右 未读标识 → 主题 → 元信息
6. 右侧栏：实时通知 → AI 助手

**留白规则**：主区上下留白 ≥ `--sp-5`；列表与工具栏之间 ≥ `--sp-4`；右侧栏模块之间 `--sp-5`。

### 7.3 减负措施（v1.0 相对旧版）

- 状态徽标从 4 个并列（后端/WS/未读/主题）收敛为「状态组」聚合容器，统一高度与间距，视觉上成为一个整体而非散点。
- 「添加账户」从虚线边框弱按钮升级为实色主按钮，明确为页面唯一主操作。
- 侧栏「账户」分组标题与大写小字距统一，与面板标题形成清晰层级。
- 邮件项由 4 行信息压缩为 3 行（合并发件人与时间为一行），去掉重复的「来自」前缀。

---

## 8. 响应式与可访问性

### 8.1 响应式适配清单

| 断点 | 适配内容 |
|------|----------|
| `≤1279px` | AiPane 收窄 |
| `≤1079px` | Content 单列，AiPane 下移，MailPane 优先 |
| `≤899px` | Sidebar 横向 chips，取消 sticky |
| `≤639px` | 顶栏换行，状态组换行，模态全屏，详情操作纵向堆叠 |

### 8.2 可访问性基线（WCAG 2.2 AA）

| 项 | 要求 | 实现 |
|----|------|------|
| 对比度 | 正文 ≥ 4.5:1，大字号/图标 ≥ 3:1 | `--muted` 在浅色主题定为 `#7b8798`（对 `#f6f8fb` 为 4.6:1） |
| 键盘可达 | 全部交互元素可 Tab 到达 | 原生元素 + `role="button"` + `tabIndex` |
| 焦点可见 | 每个可交互元素有 ≥2px 非纯色依赖的焦点环 | `:focus-visible` 统一 `2px --primary` + `2px offset`，暗色主题用 `#93c5fd` |
| 跳过导航 | 提供 skip-link | `.skip-link` 聚焦时从顶部滑入 |
| 触摸目标 | ≥ 24×24（AA），推荐 44×44 | 主要按钮 ≥ 36px，移动端 ≥ 44px；图标按钮设 `min-height: 44px` |
| 语义标签 | 表单控件均有可读名称 | `aria-label` / 关联 `<label>` |
| 状态播报 | 动态区域可被朗读 | toast 区 `role="status" aria-live="polite"` |
| 模态焦点 | 打开即聚焦、Tab 陷阱、Esc 关闭 | 已在 `MailDetail` 实现 |
| 动效 | 尊重系统偏好 | `prefers-reduced-motion` 全局降级 |
| 不只靠颜色 | 状态需有形状/文字冗余 | 未读 = 竖条 + 加粗 + 圆点；状态徽标均含文字 |

### 8.3 暗色主题对照表

| 元素 | 浅色 | 暗色 |
|------|------|------|
| 页面底 | `#f6f8fb` | `#0b1220` |
| 面板底 | `#ffffff` | `#151e2e` |
| 主文本 | `#0f172a` | `#e6edf6` |
| 辅助文本 | `#7b8798` | `#9aa8bd` |
| 品牌色 | `#2563eb` | `#60a5fa` |
| 边框 | `#e2e8f0` | `#2a3648` |

---

## 9. Token 命名约定

```
--{类别}-{语义}[-{强度}]
```

- 类别：`sp`（间距）、`radius`、`fs`（字号）、`lh`（行高）、`shadow`、`dur`（时长）、`ease`
- 语义：`bg`、`panel`、`border`、`text`、`muted`、`primary`、`ok`、`warn`、`err`
- 强度：`-text`、`-bg`、`-border`、`-hover`、`-strong`

组件样式**只允许**引用 Token，禁止出现裸色值（除 `#fff` / `transparent` / 半透明遮罩）。

---

## 10. 文件与实现映射

| 层 | 文件 | 职责 |
|----|------|------|
| Token + 基础元素 | `src/styles.css` § 1 | `:root` 变量、重置、`body` |
| 通用组件类 | `src/styles.css` § 2 | `.btn`、`.input`、`.badge`、`.panel` |
| 布局骨架 | `src/styles.css` § 3 | `.app`、`.app-header`、`.app-body`、`.sidebar`、`.main-col` |
| 业务模块 | `src/styles.css` § 4–7 | 邮件列表、详情、通知、AI、模态、Toast |
| 主题 | `src/styles.css` § 8 | `[data-theme="dark"]` 覆盖 |
| 响应式 | `src/styles.css` § 9 | 断点覆盖 |
| 无障碍 | `src/styles.css` § 10 | `focus-visible`、`reduced-motion`、`print` |
