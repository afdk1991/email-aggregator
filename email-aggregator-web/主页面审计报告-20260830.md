# 主页面全面审计报告

**日期**：2026-08-30
**范围**：`email-aggregator-web/` 前端全部 14 个源文件
**基准**：`tsc --noEmit` exit 0 + `vite build` ok（修复前后均通过）

---

## 审计维度与发现

### 1. 设计令牌一致性（Task 99）

| 问题 | 严重度 | 状态 |
|---|---|---|
| `--accent` 未在 `:root` 定义，全文件 10 处使用 `var(--accent, #e5484d)` 兜值 | 中 | ✅ 已修复 |
| `--panel-2` 未定义，2 处使用 `var(--panel-2, #f4f6fb)` 兜值 | 低 | ✅ 已修复 |
| `--ok-text` 缺失，badge 绿色文字对比度不足（见下） | 中 | ✅ 已修复 |

**修复**：`:root` 增补三个令牌；全局替换为干净引用，消除 12 处 fallback 散落。

### 2. 响应式布局（Task 100）

| 问题 | 严重度 | 状态 |
|---|---|---|
| `.content` 网格 `1fr 360px` 在窄屏（<860px）不折叠，导致横向溢出 | 高 | ✅ 已修复 |
| header 工具栏无 flex-wrap，窄屏挤压 | 中 | ✅ 已修复 |
| search-bar 无弹性增长 | 低 | ✅ 已修复 |

**修复**：新增 `@media(max-width:860px)` 断点——单列布局、header 折行、搜索框撑满、AI 面板移至底部（order:1）。

### 3. 键盘可达性（Task 101）

| 问题 | 严重度 | 状态 |
|---|---|---|
| button 无 focus-visible 轮廓 | 高 | ✅ 已修复 |
| .account-chip 无焦点环 | 中 | ✅ 已修复 |
| .detail-btn / .add-account-btn / .search-clear 无焦点指示 | 中 | ✅ 已修复 |

**修复**：新增 7 条 `:focus-visible` 规则（2px solid primary outline + 2px offset）。

### 4. 模态对话框 A11y（Task 102）

| 问题 | 严重度 | 状态 |
|---|---|---|
| 模态缺少 role=dialog / aria-modal | 高 | ✅ 已修复 |
| 关闭按钮无打开时聚焦 | 中 | ✅ 已修复 |
| 标题无 aria-labelledby 关联 | 中 | ✅ 已修复 |

**修复**：MailDetail.tsx 加 `role="dialog"` / `aria-modal="true"` / `aria-labelledby="mail-detail-subject"` / useRef 自动聚焦关闭按钮。

### 5. 搜索交互一致性（Task 103）

| 问题 | 严重度 | 状态 |
|---|---|---|
| × 仅清空输入但不退出检索态（后端命中残留） | 中 | ✅ 已修复 |

**修复**：× onClick 同时调用 `setQ('')` + `onSearch('')`。

### 6. ARIA 语义完整性（Task 104）

| 问题 | 严重度 | 状态 |
|---|---|---|
| 账户 chips 用 role=tab/aria-selected 但无 tabpanel（残缺模式） | 高 | ✅ 已修复 |

**修复**：改为 `role="group"` + `aria-pressed` 切换按钮（原生 `<button>` 语义正确）。

### 7. 对比度与 Toast 布局（Task 105）

| 问题 | 严重度 | 状态 |
|---|---|---|
| badge.ok 文字 #16a34a 在 #dcfce7 底对比度 ~3:1（WCAG AA 需 4.5:1） | 高 | ✅ 已修复 |
| ws-badge.on 同上 | 高 | ✅ 已修复 |
| toast-zone 在文档流中导致内容跳动 | 低 | ✅ 已修复 |

**修复**：改用 `--ok-text(#15803d)` 达标；toast-zone 改 `fixed` 右下悬浮 z-index:40。

---

## 验证矩阵

| 检查项 | 结果 |
|---|---|
| `tsc --noEmit` | ✅ exit 0 |
| `vite build` | ✅ 39 modules, CSS 11.11 kB, JS 161 kB |
| Preview HTTP 200 | ✅ `http://localhost:5173/` 可达 |
| CSS 包含新令牌 | ✅ --accent×10, focus-visible×7, --ok-text×3, --panel-2×2, @media×1 |

---

## 修改文件清单

| 文件 | 改动类型 |
|---|---|
| `src/styles.css` | :root 增补 3 令牌 + 清除 12 处 fallback + @media 响应式 + focus-visible 7 条 + 对比度修复 + toast fixed |
| `src/components/MailDetail.tsx` | useRef 导入 + 打开聚焦 + role=dialog/aria-modal/aria-labelledby |
| `src/components/SearchBar.tsx` | × 按钮同时调用 onSearch('') |
| `src/App.tsx` | role=group + aria-pressed 替换残缺 tab 模式 |
