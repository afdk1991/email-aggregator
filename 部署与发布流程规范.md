# 项目003「邮箱聚合平台」· 部署与发布流程规范

> 版本 **MALLV0.0.0** ｜ 最后更新 2026-09-16 ｜ 权威版本源 `version.json`
>
> 本文回答四个问题：**有哪些环节**、**各自被什么触发**、**依赖哪些配置与工具**、
> **同步推送时必须遵守哪些对应关系与约束**。
>
> 相关文档：`多端部署与版本号规范.md`（六端产物与版本号命名细则）、
> `DEPLOY.md`（线上形态能力边界与排查清单）、`邮箱聚合平台系统架构设计.md`（架构与 ADR）。

---

## 0. 总览

本项目有**四条并行的发布通道**，共用同一个版本源。它们的触发条件互不重叠：

```
                    ┌──────────────────────────────┐
                    │  version.json  （唯一版本源） │
                    └──────────────┬───────────────┘
                                   │ node scripts/version.mjs sync
                    ┌──────────────▼───────────────┐
                    │  21 个版本落点（见 §2.2）     │
                    └──────────────┬───────────────┘
        ┌──────────────────────────┼──────────────────────────┐
        │                          │                          │
  ① CI 校验                  ② 官网发布                  ③ 六端发布
  push/PR 任意分支            push master + paths          打 MALLV* 标签
  ci.yml                      deploy-edgeone.yml           release-multiplatform.yml
  构建+测试+类型检查          构建+同步+冒烟+部署+线上验证    安装包 + GitHub Release
        │                          │                          │
        └──────────────────────────┴──────────────────────────┘
                                   │
                          ④ 本地一键（开发者手动）
                          scripts/deploy-edgeone.mjs
                          与 ② 走完全相同的代码路径
```

**核心设计原则**：官网发布只有**一份实现**。
`scripts/deploy-edgeone.mjs` 被本地脚本、`.ps1`/`.sh` 薄壳与 CI 共同调用，
不存在"CI 一套、本地另一套"的漂移空间。

---

## 1. 四条通道的职责与触发

| # | 通道 | 入口 | 触发条件 | 产物 | 时长 |
|---|------|------|---------|------|------|
| ① | **CI 校验** | `.github/workflows/ci.yml` | push 到 `main`/`master`；任意 PR | 无产物，只输出结论 | ~5 min |
| ② | **官网发布** | `.github/workflows/deploy-edgeone.yml` | push 到 `master` **且命中 paths 白名单**；或手动 `workflow_dispatch` | EdgeOne Makers 线上站点 | ~4 min |
| ③ | **六端发布** | `.github/workflows/release-multiplatform.yml` | push 形如 `MALLV0.0.1` 的标签；或手动 | PC 安装包 / APK / xcarchive / Go 单二进制 + GitHub Release | ~25 min |
| ④ | **本地一键** | `scripts/deploy-edgeone.mjs` | 开发者手动 | 同 ② | ~2 min |

### 1.1 ② 的 paths 白名单（为什么是这些）

```yaml
email-aggregator-web/**      # 内容本体
deploy/edgeone-app/**        # 部署工作区（云函数、冒烟脚本、绑定文件）
scripts/version.mjs          # 版本工具链本身
scripts/sync-web-assets.mjs  # 同步工具链本身
scripts/deploy-edgeone.mjs   # 发布器本身
version.json                 # 版本源
VERSION                      # 版本源派生落点
deploy/container/**          # 容器形态的版本落点（镜像 tag / OCI 标签）
.github/workflows/deploy-edgeone.yml  # 发布流程自身
```

**为什么必须把"工具链与版本源"也列入**：只改 `version.json` 是一次
「线上版本号该变但代码没变」的发布，漏掉这条 paths 会让线上版本号永远停在旧值，
而 CI 全绿、没有任何报错。这类"静默不触发"比构建失败更难发现。

### 1.2 并发策略

| 工作流 | 并发组 | 是否取消在跑任务 | 理由 |
|--------|--------|-----------------|------|
| ci.yml | `ci-${{ github.ref }}` | ✅ `cancel-in-progress: true` | 校验类任务，旧的结果无价值 |
| deploy-edgeone.yml | `deploy-official` | ❌ `cancel-in-progress: false` | 发布是"同步资源 → 上传 → 部署"的多步变更，中途取消会停在**"部署工作区已换新、线上仍是旧版"**的中间态 |
| release-multiplatform.yml | 默认（按 ref） | ❌ | 同上；且打标签是显式动作 |

---

## 2. 环节一：版本号（MALLV 规范）

### 2.1 单一源与三种形态

唯一权威源是**仓库根 `version.json`**：

```json
{ "prefix": "MALLV", "major": 0, "minor": 0, "patch": 0, "channel": "dev" }
```

工具据此**计算**（而非冗余存储）三种形态：

| 形态 | 值 | 用途 | 谁在用 |
|------|-----|------|--------|
| `canonical` | `MALLV0.0.0` | 用户可见版本、安装包文件名、镜像 tag、git tag、文档标题 | 桌面 `artifactName`、Android `versionName`、鸿蒙 `versionName`、Go `/api/health`、前端页脚徽标 |
| `semver` | `0.0.0` | 有格式约束的场合（npm 要求纯 x.y.z） | 4 个 `package.json`、iOS `CFBundleShortVersionString` |
| `code` | `1` | 严格单调的正整数 = `major*10000 + minor*100 + patch + 1` | Android/鸿蒙 `versionCode`、iOS `CFBundleVersion` |

> `code` 的进位基数（minor/patch 各两位）决定了 **minor/patch 上限为 99**，
> 工具会在越界时直接失败，而不是静默产出会溢出的 versionCode。

### 2.2 21 个落点（缺一即"版本号不一致"）

`node scripts/version.mjs list` 会实时列出全部落点。按类别：

| 类别 | 落点 | 写入形态 |
|------|------|---------|
| **包元数据** | `email-aggregator-web/package.json`<br>`email-aggregator/package.json`<br>`platforms/desktop/package.json`<br>`platforms/mobile/package.json`<br>`deploy/edgeone-app/package.json`<br>`deploy/edgeone-app/package-lock.json`（含 `packages[""]` 根包） | `semver` |
| **纯文本** | `VERSION` | `canonical` |
| **Go 主干** | `email-aggregator-go/src/version/version.go`（整文件生成） | `canonical`/`semver`/`code`/`prefix` |
| **Web 入口** | `email-aggregator-web/index.html`（两个 meta）<br>`deploy/edgeone-app/index.html`（两个 meta） | `canonical` + `code` |
| **云函数** | `deploy/edgeone-app/cloud-functions/api/[[default]].js` | `canonical` + `code` |
| **容器** | `deploy/container/docker-compose.yml`（image tag）<br>`deploy/container/Dockerfile`（OCI version/revision）<br>`email-aggregator-web/Dockerfile`（OCI version） | `canonical` |
| **桌面端** | `platforms/desktop/electron-builder.yml`（`artifactName` 宏） | `${env.MALLV_VERSION}` |
| **移动端** | `platforms/mobile/android/app/build.gradle` ⚠️可选<br>`platforms/mobile/ios/App/App/Info.plist` ⚠️可选 | `code`+`canonical` / `semver`+`code` |
| **鸿蒙端** | `platforms/harmony/AppScope/app.json5`<br>`platforms/harmony/entry/src/main/ets/generated/AppVersion.ets`（整文件） | `code`+`canonical` / 三形态 |
| **各端运行时模块** | `email-aggregator-web/src/version.generated.ts`<br>`platforms/desktop/src/version.generated.cjs` | 三形态（整文件生成） |

⚠️ 标记的两个落点带 `optional`：文件**不存在时跳过**，存在时**必须一致**。
它们的生命周期是「`npx cap add android|ios` 先播种槽位，之后由工具接管」，
所以 CI 上全新克隆会显示 `SKIP` —— 这是预期，不是故障。

**`go-version` / 三个 `*.generated.*` / `VERSION` 属于"可由 sync 创建"的落点**
（整文件生成，机器所有）。其余落点必须先由人/脚手架播种槽位，
工具**不会**凭空创建 —— 否则会把一个"合法但不完整"的配置写进仓库，静默破坏构建。

### 2.3 版本号进入**产物内容**，而不只是文件名

这是"可追踪"的硬指标。仅把版本号写进文件名，安装包内部仍无法自证版本。
因此版本号同时存在于：

| 载体 | 读取方式 |
|------|---------|
| Go 单二进制 | `GET /api/health` → `{"version":"MALLV0.0.0","versionCode":1}` |
| EdgeOne 云函数 | 同上（`APP_VERSION` / `APP_VERSION_CODE` 常量） |
| 前端页面 | header 版本徽标（import `version.generated.ts`） |
| 桌面安装包 | asar 内 `src/version.generated.cjs`；`email-aggregator-*.exe` sidecar 二进制字节 |
| 鸿蒙 HAP | `AppVersion.ets` 经 `javaScriptProxy`（对象名 `eaHarmony`）暴露给 Web 容器 |
| 容器镜像 | `LABEL org.opencontainers.image.version` |

验证命令：`node scripts/version.mjs artifacts [dir]`
—— 逐个文件检索版本号字节（文件名 + 内容双通道），当前实测命中 16 项。

#### 移动端产物为什么单独有一道校验

`platforms/mobile/scripts/verify-artifacts.mjs`。不是因为移动端特殊，而是因为它有
两个**静默假阴性**陷阱 —— 不抛异常、不报错，只表现为"包明明打对了却判失败"：

| 陷阱 | 现象 | 正确做法 |
|------|------|---------|
| APK / AAB / IPA **都是 ZIP**，文本条目默认走 deflate 压缩 | 版本号只出现在 `assets/public/index.html` 的 `<meta name="app-version">` 里，被压缩后明文不再连续出现 → 对文件原文 `includes('MALLV0.0.0')` **恒为 false** | `zip-search.mjs`：先走原文快路径（stored 条目仍能命中），未命中再解析中央目录逐条目解压查找；带 8 MiB 条目体积上限，跳过不可能藏文本的大 `.so`/`.dex` |
| Xcode 把 Info.plist **重序列化成二进制 plist**（`bplist00`） | 文本正则匹配 `<key>CFBundleVersion</key>` **恒为 null** | `plist.mjs`：纯 JS 的最小 bplist00 读取器，自动区分 XML / 二进制两种形态。刻意不调 `plutil` —— 它只在 macOS 存在，会让检查在 Windows 开发机上静默失效 |

两者的回归自测：`node platforms/mobile/scripts/selftest.mjs`（12 项，纯 Node 零依赖、
自带夹具：ZIP 由文件内的最小 ZIP writer 现造，bplist 用内嵌 base64），已挂进 `version-guard`，
任何构建之前先跑。**断言里包含「原文搜不到」这一条** —— 先证明陷阱真实存在，再证明修复有效。

---

## 3. 环节二：构建

### 3.1 三套实现与构建入口

| 实现 | 目录 | 构建命令 | 备注 |
|------|------|---------|------|
| **Go 主干**（生产主选） | `email-aggregator-go/` | `go build ./...` | 默认**纯标准库零外部依赖**；`integration`/`webui`/`sso` 等标签隔离第三方依赖 |
| **TS PoC**（契约对照） | `email-aggregator/` | `node --experimental-strip-types src/demo.ts` | 需要 Node ≥ 22.6；系统默认 node 是 v20，**必须显式用 Node 22** |
| **React 前端** | `email-aggregator-web/` | `npm run build`（= `tsc --noEmit && vite build`） | 唯一产出被六端复用的 `dist/` |

### 3.2 前端产物的路径与命运

```
email-aggregator-web/dist/            ← 唯一构建产物
  ├── index.html                      （含 app-version / app-version-code meta）
  └── assets/index-<hash>.js|.css
        │
        └── scripts/sync-web-assets.mjs ──► 五处目标（见 §4）
```

**`vite.config.ts` 的 `base: './'` 是多端的关键**：六端的 origin 各不相同
（`https://` / `file://` / `http://localhost` / `rawfile://`），绝对路径会漏掉
`www/` 一层导致 404。生产构建必须是相对路径，开发服务器（serve）保持 `/`。

### 3.3 Go 二进制形态

| 命令 | 形态 | 用途 |
|------|------|------|
| `go build ./...` | 纯 API 服务，零外部依赖 | 默认 CI 门禁 |
| `go build -tags webui ./cmd/server` | 内嵌 SPA，单进程同端口 REST+WS+页面 | 容器版 / 桌面 sidecar / 单二进制发布 |
| `go build -tags integration ./...` | 接入真实 PG/MinIO/OpenSearch/Kafka | 集成环境；本机无 Docker 时**只能做编译级验证** |
| `email-aggregator-go/build.ps1` / `build.sh` | 六目标交叉编译 + zip/tar.gz 归档 | 手动发版 |

---

## 4. 环节三：内容同步（五处目标）

前端 `dist/` 必须被同步到**五个**消费方。这是本项目历史上出事故最多的一环
（两次线上/单二进制跑旧资源），因此收敛成**一份实现 + 逐文件 SHA-256 比对**：
`scripts/sync-web-assets.mjs`。

### 4.1 五处目标的对应关系

| id | 目标目录 | 同步语义 | 消费方 | 不同步的后果 |
|----|---------|---------|--------|-------------|
| `go-webroot` | `email-aggregator-go/webroot/dist` | `mirror` 整体镜像 | `//go:build webui` 的 `embed` 输入 | **静默**产出"无样式/旧版"单二进制（embed 过期不报错） |
| `edgeone-app` | `deploy/edgeone-app/{assets,index.html}` | 映射式，`assets` 带 `prune` | EdgeOne Makers 云上静态站 | 线上页面空白 / 404 |
| `desktop-dist` | `platforms/desktop/resources/app-dist` | `mirror` | Electron `extraResources` 静态兜底页 | 内置服务未就绪时兜底页是旧版 |
| `mobile-www` | `platforms/mobile/www` | `mirror` | Capacitor `webDir` → Android/iOS 原生工程 | APK/IPA 内嵌旧版页面 |
| `harmony-rawfile` | `platforms/harmony/entry/src/main/resources/rawfile/www` | `mirror` | HarmonyOS `$rawfile('www/index.html')` | HAP 内嵌旧版页面 |

### 4.2 同步时必须遵守的约束

1. **必须"先构建、后同步"**。同步脚本在 `dist/` 不存在时以明确错误退出（exit 1），
   不会静默跳过 —— 静默跳过正是"漏同步"的温床。
2. **JS 与 CSS 两个文件都要比对**。只看文件数或总大小会漏
   （曾捕获 `webroot/dist` 缺 `index-D8Uws_fG.css`，单二进制会吐出无样式页面）。
3. **同步模式会删除目标侧的陈旧文件**。旧版本 hash 命名的 bundle 残留在
   embed / 静态站 / 应用包内，既是体积浪费，更让"线上跑的是哪个版本"无法判断。
4. **增量同步，不整目录删除**。刻意不用"先 `rm -rf` 再拷贝"——后者中途失败
   （回收站不可用、脚本被中断）会让 embed **输入目录整体消失**，
   把"产物不一致"升级成"根本没法构建"。
5. **`--verify` 是只读门禁**（一致 exit 0 / 不一致 exit 1），可挂在任何构建前置。
6. **同步只挂在显式调用处**。直接 `go build -tags webui ./cmd/server` 做快速验证时
   **会绕过同步**，旧文件照样编译通过 → 静默漂移。故走捷径后必须手工复验。
7. **同步产物一律不入库**（见 §9.5）。`webroot/dist`、`deploy/edgeone-app/{assets,index.html}`、
   `platforms/*/...` 均在 `.gitignore` 内，由 `npm run build` + sync 现场生成。

### 4.3 复用同一实现的三个下游

`sync-web-assets.mjs` 不只是 CLI，它导出 `DESTINATIONS` / `inspectDestination` /
`syncDestination`，被以下三处 import 复用，杜绝"各写一份同步逻辑"：

- `scripts/deploy-edgeone.mjs`（官网发布器，步骤 ③）
- `platforms/desktop/scripts/prepare.mjs`（Electron 出包前置）
- `platforms/mobile/scripts/prepare.mjs` / `platforms/harmony/scripts/prepare.mjs`

---

## 5. 环节四：官网部署（EdgeOne Makers）

> 「官网」在此项目特指 **EdgeOne Makers 上已上线的站点**。

### 5.1 唯一入口

```bash
node scripts/deploy-edgeone.mjs [--skip-build] [--dry-run] [--verify-live] [--project <名>]
```

本地薄壳（只是转调，不含逻辑）：
`deploy/edgeone-app/deploy.ps1`（Windows）、`deploy/edgeone-app/deploy.sh`（macOS/Linux）。

### 5.2 六个环节（顺序即依赖）

| # | 环节 | 做什么 | 失败即中止 |
|---|------|--------|-----------|
| ① | **版本门禁** | `version.mjs sync`（幂等下发）+ `verify`（21 落点全一致） | ✅ |
| ② | **前端构建** | `npm ci`（缺 node_modules 时）+ `npm run build` | ✅ |
| ③ | **产物同步** | 五处目标同步 + SHA-256 复验 | ✅ |
| ④ | **项目绑定** | `edgeone-project.json` → `.edgeone/project.json`，并断言名字与 `-n` 一致 | ✅ |
| ⑤ | **冒烟 + 部署** | `node smoke.mjs`（演示形态 23 项）+ `node smoke.mjs --prod`（生产形态 7 项）→ `edgeone makers deploy -n <名> --json` | ✅ |
| ⑥ | **线上验证** | `node scripts/live-verify.mjs "<带 eo_token 的 URL>"`（`--verify-live`），36 项 | ✅ |

任一步失败都**不产出"部分成功"**：脚本以非零码退出并打印失败环节。
部署记录写入 `deploy/edgeone-app/.edgeone/last-deploy.json`（含部署 ID、URL、原始 JSON）。

### 5.3 项目绑定：为什么要写两个文件

| 文件 | 是否入库 | 作用 |
|------|---------|------|
| `deploy/edgeone-app/edgeone-project.json` | ✅ 入库 | 唯一权威绑定：`{Name, ProjectId}` |
| `deploy/edgeone-app/.edgeone/project.json` | ❌ gitignore | CLI 实际读取的本地绑定，每次部署前由发布器**重新播种** |

**当前绑定**：项目名 `email-aggregator-p003` ↔ ProjectId `makers-1qp5qzh9qml7`
（线上域名 `https://email-aggregator-p003-ii6yabbf.edgeone.cool`）。

> ⚠️ **CLI 按项目名远程解析，`-n` 才是权威**。若 `-n` 的名字与绑定文件不同名，
> CLI 会顺手**另建一个新项目**（旧项目与域名一起失联）。这是本项目最贵的坑，
> 因此发布器把"两者必须同名"做成**硬断言**而非提示。
> 历史事故：旧项目 `makers-jnzfzsp1nlt6` 被弃用后，其域名立即 404。

### 5.4 预览网关的三个约束（不遵守会得到一片假的 401/400）

1. **`eo_token` 以 HttpOnly cookie 下发**。Node 的 `fetch` 拿不到 HttpOnly 项 →
   必须用 `curl` 的 cookie jar，否则后续请求全部 401。
2. **token 在 query 上，path 必须拼在 query 之前**：`$ORIGIN + path + "?" + $QUERY`。
   直接 `"$BASE$path"` 会把 `/` 插进 query 值里，同样全 401。
3. **建立会话后一律不再带 query**。实测带 `?eo_token=` 的 POST 会让云函数
   拿不到 request body，接口回 400 `id and provider are required`。

另外三条环境相关的坑（`scripts/live-verify.mjs` 已内建规避）：

- `curl` 无法把 cookie jar 写到**含中文的绝对路径**（MSYS 风格 `/d/中文/...` 被拒），
  表现为 jar 静默不生成、后续全 401 → 脚本在系统临时目录下建 jar，天然规避中文路径。
- **bash 不可用**：Windows 上的 PortableGit 是残缺 shim，连 `bash --version` 都返回 1
  （"The system cannot execute the specified program"），外部命令 `dirname`/`head` 时有时无。
  原 194 行 bash 实现因此**无法本地执行** —— 恰好它是发布链路的最后一道门禁。
  故实现已移植为 **Node**（`scripts/live-verify.mjs`），`shots/live-verify.sh` 降为薄壳
  （`exec node … "$@"`）。Node 是本工具链唯一跨平台无条件可用的运行时。
- 判定方式从"读输出末行"改为**以退出码为准**（Node 版正常 `exit 1`）；
  结果行「结果（线上全接口）：N 通过 / M 失败」仍保留用于展示与留档。
  bash 版旧的"刻意不 exit 1"是 `SIGTERM` 截断输出导致的历史妥协，Node 下不再需要。

### 5.5 时效与形态

| 项 | 值 | 注意 |
|----|----|------|
| 预览 token 有效期 | **约 30 分钟** | 老 token 表现为全 401 + jar 无 `eo_token`，**极易误判为线上故障**。验证前需重新部署取新 token |
| 裸域名不带 token | 返回 401 | 属**预期**行为，不是故障 |
| 线上形态 | `/api/health` → `mode:"serverless-blob"`、`demo:false` | `serverless-memory` 说明 Blob SDK 未生效 |
| 无长驻进程 | WebSocket 与 30s 定时投递不生效 | 需完整功能请走容器形态（`deploy/container/`） |

---

## 6. 环节五：版本发布（六端 + GitHub Release）

### 6.1 触发

```bash
node scripts/version.mjs bump patch     # MALLV0.0.0 → MALLV0.0.1（并下发 21 落点）
node scripts/version.mjs verify         # 门禁
git add -A && git commit -m "chore(release): MALLV0.0.1"
git tag MALLV0.0.1 && git push origin master --tags   # ← 触发六端发布
```

**标签名必须等于 `canonical`**，工作流里有硬校验（`tag != canonical` 即失败）。

⚠️ **首次验证不要直接打标签**。打标签会立刻创建**公开**的 GitHub Release，
构建矩阵里任何一个 job 失败都来不及撤回。先用 `workflow_dispatch` 空跑一遍 ——
它的 `github.ref` 是 `refs/heads/master`，`release` job 的
`if: startsWith(github.ref, 'refs/tags/')` 会把它跳过，因此**不产生任何公开产物**，
只做构建与产物上传（仅仓库成员可见）：

```bash
gh workflow run release-multiplatform.yml --ref master -f reason="发布链路验证"
gh run list --limit 1                 # 取 run id
gh run watch <run-id> --exit-status   # 全部 job 绿了再打标签
```

### 6.2 构建矩阵与宿主约束

| 平台 | runner | 产物 | 版本号落点 |
|------|--------|------|-----------|
| Windows | `windows-latest` | NSIS 安装包 + 免安装便携版（x64/arm64） | `artifactName` 宏 |
| macOS | `macos-latest` | dmg + zip（x64/arm64，未签名） | 同上 |
| Linux | `ubuntu-latest` | AppImage + deb + rpm | 同上 |
| Android | `ubuntu-latest` + **JDK21** + SDK | debug APK / AAB | `build.gradle` |
| iOS | `macos-latest` + Xcode | 未签名 xcarchive | `Info.plist` |
| Go 单二进制 | `ubuntu-latest` | 五目标交叉编译 | 二进制内 `version` 包 |
| HarmonyOS | `[self-hosted, harmony]`，**默认关闭** | HAP | `app.json5` / `AppVersion.ets` |

#### iOS：工程形态由 Capacitor 决定，不能硬编码 workspace

`@capacitor/cli/dist/config.js` 里 `iosPlatformTemplateArchive` 的默认值是
**`ios-spm-template.tar.gz`**（Capacitor 8 起默认走 SPM），只有显式
`npx cap add ios --packagemanager CocoaPods` 才会切成 `ios-pods-template.tar.gz`。
解包实测两者的顶层结构：

| 模板 | 顶层内容 |
|------|---------|
| SPM（默认） | `App/App.xcodeproj` + `App/CapApp-SPM/Package.swift` —— **没有顶层 `App.xcworkspace`** |
| CocoaPods | `App/App.xcodeproj` + `App/App.xcworkspace` + `App/Podfile` |

因此：**绝不能写 `xcodebuild -workspace App.xcworkspace`**（默认形态下该路径不存在）；
把 `.xcodeproj` 传给 `-workspace` 同样会报 `is not a workspace`。
`platforms/mobile/scripts/build.mjs` 已按实际存在的形态自动选择 `-workspace` / `-project`，
并对依赖做正确解析（CocoaPods 走 `pod install`，SPM 走包解析）。iOS job 直接调它即可。

#### HarmonyOS：默认跳过的可选项

工作流里有一个 `harmony` job（`runs-on: [self-hosted, harmony]`），用仓库变量门控：

```yaml
if: vars.HARMONY_RUNNER == 'true'
```

DevEco Studio（含 hvigor / ohpm / HarmonyOS SDK）无法在 GitHub 托管 runner 上合法获取，
所以必须先自建 runner。启用两步：

1. 在装了 DevEco Studio 5.0+ 的机器上注册 self-hosted runner，额外打 `harmony` 标签，
   并确保 `DEVECO_SDK_HOME` 进入 runner 的环境变量；
2. 仓库 → Settings → Secrets and variables → Actions → **Variables** → 新建
   `HARMONY_RUNNER = true`。

未设置该变量时 job 立即 `skipped`，**不会排队等待一个不存在的 runner**。
它刻意不在 `release` 的 `needs` 里 —— HAP 属可选产物，缺席不应阻断其余五端发布；
而 `download-artifact` 会自然把它的产物一并收进 Release。

装真机还需在 `build-profile.json5` 的 `signingConfigs` 配签名（`.p12` + `.cer` + profile），
密钥放 Secret。

⚠️ **各平台构建必须在其对应宿主系统上进行，不能交叉**：Windows 包 → Windows runner；
dmg/ipa → macOS runner；AppImage/rpm → Linux runner。

### 6.3 首轮实测结果（2026-09-16，run 35118604475）

用 `workflow_dispatch` 空跑（不产生公开 Release），暴露出 4 处**必挂**缺陷 —— 此前该
工作流只有静态审阅、从未真实执行过，所以它们一直隐形。

| Job | 结果 | 根因 | 修复 |
|-----|------|------|------|
| `version-guard` | ✅ | — | — |
| `desktop (windows)` | ✅ **首个完整跑通** | — | — |
| `harmony` | ⏭️ 按设计跳过 | 未设仓库变量 | — |
| `release` | ⏭️ 按设计跳过 | `github.ref` 非 tag | — |
| `android` | ❌ | `android-actions/setup-android@v3` 自身退出码 1（无 error 注解） | 移除该 action，改用 runner 预装 SDK + 幂等补 `platforms;android-36` |
| `desktop (linux)` | ❌ | deb/rpm 走 fpm，强制要求 `homepage`；AppImage 已成功 | `platforms/desktop/package.json` 补 `homepage` |
| `desktop (macos)` | ❌ | 打包 4 产物全部成功；校验脚本只认 `resources/`，不认 macOS 的 `.app/Contents/Resources` | `verify-artifacts.mjs` 增 `resolveResources()` 双形态探测 |
| `ios` | ❌ | SPM 形态识别已生效（正确用 `-project`），卡在 Xcode 26 的签名前置检查 | `build.mjs ios --unsigned` 把签名开关作为 build setting 传入 |

**这次空跑的价值**：四个缺陷里有三个（Linux / macOS / iOS）都是「产物其实已经正确产出，
只是最后一道校验或签名开关写错」—— 单靠读工作流文本发现不了。**凡新增或修改发布
流水线，一律先 `workflow_dispatch` 空跑，再打标签。**

### 6.4 第二轮实测结果（2026-09-16，run 35119405180）

| Job | 结果 | 根因 | 修复 |
|-----|------|------|------|
| `version-guard` | ✅ 8s | — | — |
| `desktop (windows)` | ✅ 4m34s | — | — |
| `desktop (macos)` | ✅ 3m25s | ← 首轮新修生效 | `resolveResources()` 双形态探测 |
| `desktop (linux)` | ✅ 5m35s | ← 首轮新修生效 | `homepage` 补齐后 deb/rpm 正常出包 |
| `ios` | ✅ 1m3s | ← 首轮新修生效 | SPM 自适应 + www 预置 + `--unsigned` + 归档路径**四环咬合**才通过 |
| `android` | ❌ 56s | 🔴 **`versionCode=0` 被 Gradle 拒绝** | 新增 `CODE_OFFSET = 1`（见下） |
| `harmony` | ⏭️ 0s | 按设计跳过 | — |
| `release` | ⏭️ | 按设计跳过（`github.ref` 非 tag） | — |

结果 **4/6 端全绿**，仅 android 一项失败。

#### 🔴 这不是 CI 缺陷，而是 MALLV 版本号方案本身的设计缺陷

```
* What went wrong:
A problem occurred configuring project ':app'.
> android.defaultConfig.versionCode is set to 0, but it should be a positive integer.
BUILD FAILED in 32s
```

原公式 `code = major*10000 + minor*100 + patch` 在初始版本 **MALLV0.0.0** 上算出 `0`。
Android / HarmonyOS 的 `versionCode` 与 iOS 的 `CFBundleVersion` 都要求**正整数**，`0` 会被
直接拒绝 —— 也就是说**初始版本在 Android 上物理无法出包**。这与任何 CI 改动无关，单靠读文档
永远发现不了，必须真跑一次才会浮出来。

修复：`CODE_OFFSET = 1`，公式改为 `major*10000 + minor*100 + patch + 1`。落点仍严格单调：
`0.0.0→1`、`0.0.1→2`、`0.1.0→101`、`1.0.0→10001`，上限 `99.99.99→1000000`。

#### 另一个发现：修好 versionCode，android 仍会差一步

据此推演 `verify-artifacts.mjs`，发现它用**文件原文**搜索版本号 —— 而 APK 是 ZIP、
文本条目默认 deflate 压缩，于是必然假阴性。用真实夹具已证实：

```
原文 includes("MALLV0.0.0") = false                        ← 旧写法在此判失败
archiveContains            = hit:true, how:"zip",
                             where=["assets/public/index.html"]
```

所以 android 转绿需要**同时**修好 `versionCode` 与校验脚本，缺一不可（分别在 `d41c3d4`
与 `f378d91` 落地）。

### 6.5 第三轮实测结果（2026-09-16，run 35120977342）

| Job | 结果 | 说明 |
|-----|------|------|
| `version-guard` | ✅ 5s | 新增的 `selftest.mjs`（12 项）已在 CI 上真实跑通 |
| `ios` | ✅ 1m27s | 连续两轮全绿 |
| `desktop (macos)` | ✅ 2m6s | — |
| `desktop (windows)` | ✅ | — |
| `desktop (ubuntu)` | ✅ | — |
| `android` | ❌ 65s | 🔴 **JDK 17 太旧**：`invalid source release: 21`（见下，是第二个独立根因） |
| `harmony` / `release` | ⏭️ 按设计跳过 | — |

结果 **5/6 端全绿**，仅 android 一项失败。`versionCode` 修复本身**已生效**
（Gradle 不再报 `versionCode is set to 0`），android 挂在了更后面一步。

#### 🔴 android 的第二个根因：Capacitor 8 要求 JDK 21，工作流里写的是 17

```
> Task :capacitor-android:compileDebugJavaWithJavac FAILED
* What went wrong:
Execution failed for task ':capacitor-android:compileDebugJavaWithJavac'.
> Java compilation initialization error
    error: invalid source release: 21
BUILD FAILED in 45s
```

铁证来自**依赖源码**，不是猜：

```
node_modules/@capacitor/android/capacitor/build.gradle:66-67
  sourceCompatibility JavaVersion.VERSION_21
  targetCompatibility JavaVersion.VERSION_21
```

`@capacitor/android@8.5.2`，而工作流是 `java-version: '17'`（temurin 17.0.20）→ javac 17
无法以 `--release 21` 编译。**Gradle 报的这句话完全没有版本上下文** —— 不说当前是 JDK 几、
也不说该升到几，极易误判成 SDK 或 AGP 问题。

修复：`java-version: '21'`；并在 `platforms/mobile/scripts/build.mjs` 加 **JDK 主版本预检**
（读 `$JAVA_HOME/release`，回退 `java -version`），低于 21 时直接抛出可读错误。
预检已回归验证 5/5：JDK 17 → 在进入 Gradle 之前拦下；JDK 21 → 放行并打印实际版本；
判定不出 → 只警告不阻断。

#### 顺带排除的其他嫌疑（均已实测不是原因）

- **Android SDK 组件**：`准备 Android SDK` 步骤输出 `所需 SDK 组件已齐备，跳过安装` —— runner
  自带 `platforms;android-36` / `build-tools;36.0.0` / `platform-tools`。
- **Gradle 版本**：`gradle-8.14.3-all.zip` 正常下载，`Welcome to Gradle 8.14.3!`。
- **依赖与资源**：`:capacitor-android:` 的 preBuild / 资源 / manifest / R 文件任务全部成功，
  只在 `compileDebugJavaWithJavac` 失败 → **单根因**，不是"多处都错"。

### 6.6 第四轮实测结果（2026-09-16，run 35121898488）—— 🎉 6/6 端首次全绿

| Job | 耗时 | 结果 | 关键步 |
|-----|------|------|--------|
| `version-guard` | 10s | ✅ | 21 落点一致 + `selftest.mjs` 12 项 |
| `android` | 1m58s | ✅ | **构建 Android 包 ✓ / 校验产物版本号 ✓** |
| `ios` | 1m18s | ✅ | 归档（未签名）✓ / 校验产物版本号 ✓ |
| `desktop (macos)` | 2m27s | ✅ | 打包 ✓ / 校验 ✓ |
| `desktop (windows)` | 4m43s | ✅ | 打包 ✓ / 校验 ✓ |
| `desktop (ubuntu)` | 5m38s | ✅ | 打包 ✓ / 校验 ✓ |
| `harmony` | 0s | ⏭️ 按设计跳过 | 未设 `HARMONY_RUNNER` |
| `release` | 0s | ⏭️ 按设计跳过 | `github.ref` 非 tag |

`workflow_dispatch` 空跑退出码 **0**。**该流水线自创建以来第一次完整跑通**，
且是在不产生任何公开 Release 的前提下做到的。

#### android 校验步骤的原始输出（修复生效的直接证据）

```
[mobile-verify] 期望版本 MALLV0.0.0（semver=0.0.0, code=1）

        android/app/build/outputs/apk/debug/app-debug.apk  4.0 MB
        版本号已检出（解压后命中：assets/public/assets/index-DhJHAJOb.js,
                                 assets/public/index.html）

  OK    Android 版本槽位
        versionCode=1, versionName="MALLV0.0.0"
  OK    安装包内含规范版本号
        1 个产物均含 MALLV0.0.0

[mobile-verify] 全部通过
```

两处关键：

- `versionCode=1` —— 证明 `CODE_OFFSET = 1` 生效。若仍是 `0`，Gradle 会在配置阶段就拒绝。
- `版本号已检出（**解压后**命中：…）` —— 明确走出的是 ZIP 解压分支（原文快路径未命中）。
  **若还是原来的 `fs.readFileSync().includes()`，这一步必然判失败**，两种修复缺一不可。

#### iOS 校验步骤的原始输出（二进制 plist 陷阱的直接证据）

```
  OK    iOS 版本槽位
        xml：CFBundleShortVersionString=0.0.0, CFBundleVersion=1
  OK    iOS 归档版本槽位（ios/build/App.xcarchive）
        binary：CFBundleShortVersionString=0.0.0, CFBundleVersion=1
```

- 归档内的 Info.plist **确实是 `binary` 形态**（Xcode 26.6 重序列化的结果）——
  这从经验上证明了「文本正则读 `.xcarchive` 里的版本键恒为 null」这个陷阱真实存在，
  且 `plist.mjs` 的纯 JS bplist 读取器能正确读出两个键。
- 同一个 job 的日志另外确认 `[mobile] 工程形态：SPM 工程（App.xcodeproj）`
  —— Capacitor 8 的 SPM 自适应在真实 Xcode 26.6 上生效（若硬编码 `-workspace` 必然失败）。

---

## 7. 触发条件总表

| 事件 | 触发的工作流 | 关键动作 |
|------|-------------|---------|
| push 到 `main`/`master` | `ci.yml` | 版本门禁 → Go build/vet/test → TS demo/test/typecheck → Web build → 真实中间件集成测试 |
| 任意 PR | `ci.yml` | 同上（不含需要 Docker 服务的两条需视配置） |
| push 到 `master` 且命中 paths 白名单 | `deploy-edgeone.yml` | 门禁 → 构建 → 同步 → 冒烟 → 部署 → **线上验证** |
| 手动 `workflow_dispatch` | `deploy-edgeone.yml` | 同上；`dry_run=true` 时只做到冒烟 |
| push `MALLV*` 标签 | `release-multiplatform.yml` | 标签一致性 → 六端并行构建 → 汇总 Release |
| 手动 `workflow_dispatch` | `release-multiplatform.yml` | 同上（不带标签时**不**创建 Release） |
| 开发者手动 | `scripts/deploy-edgeone.mjs` | 同 ②，忽略 paths 过滤 |
| 开发者手动 | `scripts/ci.ps1` | 本地版 ①（`-Integration` 追加集成；**同样先预置迁移 + 建桶**） |

---

## 8. 依赖的配置与工具

### 8.1 密钥与变量（严禁硬编码）

| 名称 | 类型 | 位置 | 用途 | 留空行为 |
|------|------|------|------|---------|
| `EDGEONE_PAGES_API_TOKEN` | **Secret** | GitHub 仓库 Secret | 官网部署令牌 | 部署步骤失败 |
| `EDGEONE_TOKEN` | 本地环境变量 | `$env:` / shell | 同上（本地无头部署） | 退回 CLI 本地登录态 |
| `AI_GATEWAY_*` / `AI_SELF_HOSTED_*` / `AI_THIRD_PARTY_*` | 平台环境变量 | `edgeone makers env set` | AI 能力网关 | 降级为本地演示回复（带 `[demo]` 标注，**不伪造模型输出**） |
| `DEFAULT_TENANT_ID` | 平台环境变量 | 同上 | 默认租户（ADR-009） | 缺省 `default` |
| `ENABLE_DEMO` | 平台环境变量 | 同上 | 演示模式开关 | 缺省 `true`；**生产必须 `false`** |
| `PAGES_SOURCE` | 环境变量 | 由发布器注入 | 平台部署通道选择 | — |
| `HARMONY_RUNNER` | **Variable**（非 Secret） | GitHub 仓库 Variables | 启用六端里的 `harmony` job（自建 runner） | job 立即 `skipped`，**不排队**等一个不存在的 runner |

仓库内已排除密钥：`.gitignore` 忽略 `.env`、`**/.env.*`、`**/kms-keyring*.json`、
`.edgeone/`；例外白名单 `!email-aggregator-web/.env.production`（只含构建期开关，必须入库，
缺失会让 `VITE_DEMO_MODE` 退回 `true` 从而产出带演示模式的生产包）。

### 8.2 工具链版本

| 工具 | 版本 | 用在哪 | 备注 |
|------|------|--------|------|
| Node | **22** | 前端构建、全部 `.mjs` 工具链 | TS PoC 需 ≥ 22.6（`--experimental-strip-types`）；系统默认 v20 不够 |
| Go | 1.26.x | Go 主干 | 无 CGO 依赖，交叉编译友好 |
| edgeone CLI | **固定 `1.6.40`** | 官网部署 | 用 `@latest` 会让"同样的提交今天能发、明天发不出去"（实测 1.6.17-beta.1 与 1.6.40 的 `deploy --json` 输出结构不同） |
| JDK | 17 | Android | Capacitor 8.x 要求 |
| Android SDK | `android-actions/setup-android@v3` | Android | — |
| Xcode | `macos-latest` 自带 | iOS | 未签名归档 |
| DevEco Studio | 本地 / `[self-hosted, harmony]` runner | HarmonyOS | 托管 runner 不具备；仓库变量 `HARMONY_RUNNER=true` 才启用 job |
| rpm | `apt install rpm` | Linux 桌面包 | electron-builder 出 rpm 需要 |
| Docker | — | 容器形态、集成 E2E | **本机未安装**（见 §9.6） |

### 8.3 镜像与加速（避免 runner 拉取超时）

| 变量 | 值 |
|------|-----|
| `ELECTRON_MIRROR` | `https://npmmirror.com/mirrors/electron/` |
| `ELECTRON_BUILDER_BINARIES_MIRROR` | `https://npmmirror.com/mirrors/electron-builder-binaries/` |
| `CSC_IDENTITY_AUTO_DISCOVERY` | `false`（未配证书时不签名） |
| `GOPROXY` | `https://goproxy.cn,direct`（Dockerfile 内可用 `--build-arg` 覆盖） |

---

## 9. 对应关系与约束（必读）

### 9.1 版本号 → 落点形态对应

| 落点 | canonical | semver | code |
|------|:---------:|:------:|:----:|
| 4 个 `package.json` + 2 个 EdgeOne 工作区文件 | | ✅ | |
| `VERSION` | ✅ | | |
| Go `version.go` / 三个 `*.generated.*` | ✅ | ✅ | ✅ |
| `index.html` meta（2 处） | ✅ | | ✅ |
| 云函数 `APP_VERSION` / `APP_VERSION_CODE` | ✅ | | ✅ |
| 容器 tag / OCI 标签 | ✅ | | |
| `electron-builder.yml` `artifactName` | ✅（宏） | | |
| Android `versionName` / `versionCode` | ✅ | | ✅ |
| iOS `CFBundleShortVersionString` / `CFBundleVersion` | | ✅ | ✅ |
| 鸿蒙 `versionName` / `versionCode` | ✅ | | ✅ |

**为什么 iOS 用 `semver` 而非 `canonical`**：Apple 对 `CFBundleShortVersionString`
有格式约束（只接受点分数字），`MALLV0.0.0` 会被拒绝。`MALLV` 前缀只出现在
用户可见处（包名、文档、UI），不进入平台合规字段。

### 9.2 产物 → 消费方对应

| 产物 | 消费者 | 传输方式 |
|------|--------|---------|
| `email-aggregator-web/dist/**` | 六端全部 | `sync-web-assets.mjs` 五处同步 |
| `email-aggregator-go/webroot/dist/**` | Go `embed`（`webui` 标签） | 编译期内嵌 |
| `deploy/edgeone-app/**` | EdgeOne Makers CLI | `edgeone makers deploy` 上传 |
| `platforms/desktop/release/*` | 用户 | GitHub Release 附件 |
| `platforms/desktop/resources/app-dist/**` | Electron 兜底静态服务 | `extraResources` |
| `platforms/desktop/resources/bin/*` | Electron sidecar | 随包分发，随机端口拉起 |
| `platforms/mobile/www/**` | Capacitor → Android/iOS 工程 | `cap sync` 读取 `webDir` |
| `platforms/harmony/entry/src/main/resources/rawfile/www/**` | ArkTS Web 容器 | `$rawfile` 资源内嵌 |
| `email-aggregator-go/release/*` | 自部署用户 | GitHub Release 附件 |

### 9.3 时序约束（顺序不可颠倒）

1. **`version.mjs sync` 必须在所有构建之前**。
   `version.generated.ts` / `version.go` 是构建输入；先构建后 sync 会得到"版本号落后一版"的产物。
2. **`npm run build` 必须在 `sync-web-assets.mjs` 之前**（同步的源是 `dist/`）。
3. **`node smoke.mjs` 必须在 `edgeone makers deploy` 之前**（冒烟是部署前唯一的
   路由级门禁；部署后再发现，线上已经有流量了）。
4. **Android：`npx cap add android` → `node scripts/version.mjs sync` → `node scripts/build.mjs android`**。
   `prepare.mjs` 会做一致性断言，顺序颠倒直接失败。
5. **`prune` 型的同步必须在部署之前**，否则陈旧 hash bundle 会被一起上传。
6. **`node scripts/sync-web-assets.mjs` 必须在 `npx cap add android` / `npx cap add ios` 之前**。
   `cap add` 时若 `webDir`（`platforms/mobile/www`）不存在，它只打印一行
   `sync could not run--missing www directory` 就**跳过整段 sync**（插件注入、
   `local.properties` 生成全被跳过），工程处于半初始化状态，且**不 fatal**。
7. **iOS 归档必须走 `node scripts/build.mjs ios --unsigned`**，不要手写 `xcodebuild`：
   `build.mjs` 内部先跑 `prepare()`（版本门禁 + 灌 `www` + 生成图标）再 `cap sync ios`，
   并按实际工程形态选 `-workspace` / `-project`。
8. **`verify-artifacts.mjs` 必须在构建之后**：它校验的是产物内容（APK/AAB/IPA 的 ZIP
   条目、`.xcarchive` 里的 Info.plist），产物不存在时只会打印"跳过"并**照样通过**。

### 9.4 一次性/不可重复的约束

- **`version.json` 单向写入**：任何落点都不得反向修改版本。改版本只有三种方式：
  `bump <part>` / `set <版本串>`，且必须先改 `version.json`。
- **`MALLV*` 标签名必须等于 `canonical`**，否则 `release-multiplatform.yml` 直接失败。
- **标签一旦推送不要删除重打**：已发布的 Release 与安装包会被引用。

### 9.5 禁忌清单（做过就会出事的）

| ❌ 不要 | 后果 |
|--------|------|
| 手工编辑任何版本落点 | 下一次 `verify` 全红；且工具算不出"哪个才是对的" |
| 把 `dist/` / `webroot/dist` / `edgeone-app/assets` 提交入库 | 仓库产物与源码静默漂移：改完前端忘构建，部署出来的仍是旧的，**而 diff 里看不出来** |
| 直接 `go build -tags webui` 而不先 sync | 单二进制内嵌旧资源，**编译通过、无任何报错** |
| 用 `npx cap add android` 之后不 sync 直接 build | APK 里 `versionName` 与项目版本号不符 |
| 改 `-n` 的项目名而不改绑定文件 | CLI 另建新项目，旧域名失联 |
| 在 `deploy-edgeone.yml` 里重新写一遍同步/构建逻辑 | 回到"三份实现"的老路，漏同步必然重演 |
| 用 `git commit -am` | 漏掉新增（未跟踪）文件，如 `scripts/*.mjs`、`platforms/**` |
| 依赖 `@latest` 安装 CLI | 同一次提交在不同日期可能成功/失败 |
| 把 `?eo_token=` 拼进后续请求 | 云函数收不到 body（400） |
| 把预览 URL 的 401/302 当成线上故障 | 浪费排查时间；裸域名不带 token 401 是预期 |
| iOS 里硬编码 `xcodebuild -workspace App.xcworkspace` | Capacitor 8 默认 SPM 模板**不生成**顶层 `App.xcworkspace`；报 `does not exist`。把 `.xcodeproj` 传给 `-workspace` 则报 `is not a workspace` |
| iOS 手写 `xcodebuild` 而不走 `scripts/build.mjs ios` | 少掉 `prepare()`（`www` 未灌入）与 `cap sync ios`（依赖未解析）→ 归档产物是无 Web 资源的空壳，或编译期缺 Capacitor 模块 |
| iOS 上传 `ios/App/build/App.xcarchive` | 脚本实际归档到 `ios/build/App.xcarchive`；路径不符 + `if-no-files-found: error` → 归档成功后再失败一次 |
| 在 `cap add` 之前不同步 `www` | `cap add` 静默降级（只 warn），插件注入与 `local.properties` 生成被跳过 |
| 用 `android-actions/setup-android@v3` | 它会把 runner 自带的 cmdline-tools 换成自己下载的 16.0，随后在 `sdkmanager --licenses` 里退出码 1，且**整段日志无任何 `##[error]` 注解**。runner 镜像本就预装 SDK 且已接受许可，直接补缺即可 |
| 缺 `homepage` 就出 deb / rpm | electron-builder 的 fpm 目标强制要求 `homepage`；表现为 **AppImage 成功产出、deb/rpm 全部失败**（`⨯ Please specify project homepage`） |
| 在 macOS 的未打包目录里找 `<outDir>/resources` | macOS 的包布局是 `<outDir>/<ProductName>.app/Contents/Resources`；`resources/` 是 Windows/Linux 的布局。产物完全正常也会误报失败 |
| iOS 只把 `CODE_SIGNING_ALLOWED=NO` 放在 step `env` 里 | Xcode 26 在 `GatherProvisioningInputs` 阶段就报 `Signing for "App" requires a development team`，早于签名动作；必须作为 **xcodebuild 的 build setting**（命令行参数）传入，即 `build.mjs ios --unsigned` |
| 对 APK / AAB / IPA 的**文件原文**做 `buf.includes('MALLVx.y.z')` 来验版本号 | 它们都是 ZIP，文本条目默认走 deflate 压缩，明文版本号在原文里不再连续出现 → **假阴性**：包明明打对了却判失败。必须走 `zip-search.mjs` 的 `archiveContains()` |
| 用文本正则读 `.xcarchive` 里 `App.app/Info.plist` 的版本键 | Xcode 会把 Info.plist **重序列化成二进制 plist**（`bplist00`），`<key>CFBundleVersion</key>` 匹配不到 → 恒为 null。必须走 `plist.mjs` |
| 用 `plutil` 去做上面那件事 | 它只在 macOS 存在，会让这条检查在 Windows 开发机上静默失效、无法本地复现；`plist.mjs` 是纯 JS，两端行为一致 |
| 在 ZIP 夹具里放未压缩（stored）条目，用来验证"ZIP 压缩假阴性" | 明文会直接出现在文件原文里，原文快路径命中，陷阱就验不出来了。本自测第一版正是这样被自己抓到的 —— **stored 与 deflate 必须拆成两个夹具** |
| 改完 `verify-artifacts.mjs` / `zip-search.mjs` / `plist.mjs` 却不跑 `selftest.mjs` | 这两个陷阱的症状是"看起来通过"或"明明对了却判失败"，且都不抛异常，不用夹具钉死就发现不了 |
| 给 Android 出包配 **JDK 17** | Capacitor 8 的 `capacitor-android` 要求 `sourceCompatibility = JavaVersion.VERSION_21`（`build.gradle:66-67`）。JDK 17 报 `error: invalid source release: 21`，而这句话**不带任何版本上下文**（不说当前是几、也不说该升到几），极易误判成 SDK / AGP 问题。CI 必须 `java-version: '21'` |
| 把 `versionCode` 与校验脚本当成同一个问题 | android 有三层独立根因：`versionCode=0`（方案层）→ JDK 17（工具链层）→ ZIP 假阴性（校验层）。修一层不足以转绿，必须各自验证 |

### 9.6 已知环境阻断（本机）

| 能力 | 状态 | 说明 |
|------|------|------|
| Docker / 容器形态构建 | ⛔ 本机未安装 | PG、MinIO、OpenSearch、Kafka **运行时**验证不可做；`-tags integration` 仅**编译级**验证 |
| Android 出包 | ⛔ 缺 **JDK 21** + Android SDK | 需在 CI 或本地补装 |
| iOS 出包 | ⛔ 非 macOS | 需 macOS + Xcode；产 `.ipa` 还需开发者证书 |
| HarmonyOS 出包 | ⛔ 缺 DevEco Studio | 需本地或自托管 runner；CI 侧 `harmony` job 由仓库变量 `HARMONY_RUNNER=true` 启用 |
| macOS / Linux 桌面包 | ⛔ 跨系统不可交叉 | 需对应 runner（CI 已配置） |
| Windows 桌面包 | ✅ 本机可出 | 实测 4 产物全部携带 `MALLV0.0.0` |

---

## 10. 回滚

| 场景 | 操作 |
|------|------|
| 官网回滚到上一版 | EdgeOne 控制台 → 项目 → 部署历史 → 选上一个成功部署 → 回滚 |
| 官网回滚（代码侧） | `git revert <commit>` 后重新触发发布（本工作流会跑完整验证链） |
| 紧急止血 | 控制台把生产切到任一历史版本 |
| 六端产物回滚 | GitHub Release 删除/标记，或重新打一个更低序号的标签 |
| 容器版 | Render 控制台 → Deploy 历史 → Rollback |

**建议**：每次部署前确认冒烟全绿；把部署 ID 与 ProjectId 记录进提交信息，便于定位回滚点。
发布器已自动把部署记录写入 `deploy/edgeone-app/.edgeone/last-deploy.json`。

---

## 11. 验证清单（三层递进）

### L1 本地（提交前）
```bash
node scripts/version.mjs verify                    # 21 落点一致
node scripts/version.mjs artifacts                 # 产物确实带版本号
node platforms/mobile/scripts/selftest.mjs         # 移动端校验链路回归（12 项，守两个"静默假阴性"）
cd email-aggregator-web && npm run build           # tsc + vite
node scripts/sync-web-assets.mjs --verify          # 五处目标一致
cd deploy/edgeone-app && node smoke.mjs && node smoke.mjs --prod
node scripts/deploy-edgeone.mjs --skip-build --dry-run   # 门禁+同步+冒烟演练
```

改动**工作流文本**时另加两道静态自检（不消耗 runner 时间，改完立刻能跑）：
PyYAML 结构断言（job 集合、`needs`、`runs-on`、步骤先后顺序、上传路径与脚本实际落点一致）
＋ 全部工作流共 56 个 `run:` 块的 `bash -n` 语法自检。
**它们只能证明"结构对"，不能证明"能跑"** —— 真跑仍必须 `workflow_dispatch` 空跑。

### L2 CI
```bash
powershell -ExecutionPolicy Bypass -File .\scripts\ci.ps1            # 本地版 CI
powershell -ExecutionPolicy Bypass -File .\scripts\ci.ps1 -Integration  # 需 Docker 栈
powershell -ExecutionPolicy Bypass -File .\scripts\check-ps1-encoding.ps1
```

### L3 线上
```bash
node scripts/live-verify.mjs "<带 eo_token 的 URL>"   # 36 项：静态资源 + 生产包纯净度 + 无障碍令牌 + 全 API 契约
```
CI 的 `deploy-edgeone.yml` 已内建 L3（`--verify-live`），失败即让 job 失败。
**实测已闭环**：2026-09-16 完整发布（部署 ID `dp35z8crsj2d`）六环节全绿，
线上 36/36，`/api/health` 回 `version:"MALLV0.0.0"`。

---

## 12. 命令速查

| 目的 | 命令 |
|------|------|
| 查看当前版本 | `node scripts/version.mjs show` |
| 列出全部落点及当前值 | `node scripts/version.mjs list` |
| 校验落点一致性 | `node scripts/version.mjs verify` |
| 下发版本到全部落点 | `node scripts/version.mjs sync` |
| 递增版本（推荐发版方式） | `node scripts/version.mjs bump patch` |
| 显式设定版本 | `node scripts/version.mjs set MALLV0.0.7` |
| 在产物中检索版本号 | `node scripts/version.mjs artifacts` |
| 移动端校验链路回归自测 | `node platforms/mobile/scripts/selftest.mjs` |
| 校验移动端已构建产物 | `cd platforms/mobile && node scripts/verify-artifacts.mjs` |
| 同步前端产物（五处） | `node scripts/sync-web-assets.mjs` |
| 只校验不同步 | `node scripts/sync-web-assets.mjs --verify` |
| 官网发布（全流程） | `node scripts/deploy-edgeone.mjs` |
| 官网发布（复用 dist，演练） | `node scripts/deploy-edgeone.mjs --skip-build --dry-run` |
| 官网发布 + 线上验证 | `node scripts/deploy-edgeone.mjs --verify-live` |
| 六端打包（单平台） | `cd platforms/desktop && node scripts/build.mjs --win` |
| 校验安装包带版本号 | `cd platforms/desktop && node scripts/verify-artifacts.mjs` |
| 本地全量 CI | `powershell -ExecutionPolicy Bypass -File .\scripts\ci.ps1 -Integration` |
| 集成环境预置（迁移 + 建桶） | `cd email-aggregator && node scripts/bootstrap-integration.mjs` |
| 预置计划预览（不连服务） | `cd email-aggregator && node scripts/bootstrap-integration.mjs --dry-run` |
| TS 集成层真实联调 | `cd email-aggregator && node scripts/bootstrap-integration.mjs && npx --yes tsx tests/integration_real_e2e.ts` |

---

## 附录 A：本轮新增与变更

| 文件 | 变更 |
|------|------|
| `scripts/deploy-edgeone.mjs` | **新增** 官网发布器（唯一实现，六环节，跨平台） |
| `.github/workflows/deploy-edgeone.yml` | **重写** 为完整发布流水线（门禁 → 构建 → 同步 → 冒烟 → 部署 → 线上验证 → 留档） |
| `deploy/edgeone-app/deploy.ps1` / `deploy.sh` | **改为薄壳**（消除三份并行实现） |
| `scripts/version.mjs` | 落点 **18 → 21**：恢复 `harmony-app-json5`；新增 `edgeone-pkg`、`edgeone-lock` |
| `部署与发布流程规范.md` | **新增** 本文档 |

## 附录 B：本轮发现的缺陷（均已修复）

| # | 缺陷 | 影响 | 修复 |
|---|------|------|------|
| 1 | `harmony-app-json5` 落点**整条丢失**（被并发编辑覆盖） | `bump` 后鸿蒙 `app.json5` 的 `versionCode/versionName` **静默不更新**；下一次出包时被 `platforms/harmony/scripts/prepare.mjs` 的一致性断言拦下 —— 鸿蒙链路在**下一个版本**就会断 | 恢复落点（正则改为带引号的 `"versionCode":`，并加注释说明为何不能用裸键名） |
| 2 | `deploy/edgeone-app/package.json` 版本为 `1.0.0`，且**不在落点注册表内** | "项目版本号 MALLV0.0.0" 与"部署工作区版本 1.0.0"长期并存，正是用户要求消除的漂移 | 新增 `edgeone-pkg` + `edgeone-lock` 两个落点（含 lockfile 的 `packages[""]` 根包） |
| 3 | `deploy.sh` / `deploy.ps1` / `deploy-edgeone.yml` **三份并行发布实现**，同步语义各不相同 | 两个 shell 脚本手工 `cp` 且**不清理陈旧资源**；CI 同样手工 `cp`、**无版本门禁**、**无线上验证** | 收敛为 `scripts/deploy-edgeone.mjs` 单一实现 |
| 4 | 官网自动发布**缺** paths 覆盖 `version.json` / `scripts/**` | 只改版本号时**不触发**发布，线上版本号停在旧值且 CI 全绿 | paths 白名单补齐 |
| 5 | CLI 用 `@latest` 安装 | 同一次提交在不同日期可能成功/失败 | 固定 `edgeone@1.6.40` |

## 附录 C：六端发布流水线实跑暴露的缺陷（共 12 处，均已修复）

该工作流此前**只有静态审阅、从未真实执行过**，所以下面这些缺陷一直隐形。
四轮 `workflow_dispatch` 空跑与配套推演/夹具验证共暴露 12 处。

| 阶段 | # | 缺陷 | 现象 | 修复 |
|------|---|------|------|------|
| 首轮实跑 | 1 | iOS job 从不灌 `www` | 手写 `xcodebuild` 且缺 `prepare()` → 归档出来是没有 Web 资源的空壳 | 改为复用 `build.mjs ios`，并前置 `sync-web-assets.mjs` |
| 首轮实跑 | 2 | iOS 硬编码 `-workspace App.xcworkspace` | Capacitor 8 默认 SPM 模板**不生成**顶层 workspace | `build.mjs` 按实际形态选 `-workspace` / `-project` |
| 首轮实跑 | 3 | `android-actions/setup-android@v3` 自身失败 | 退出码 1，**整段日志无任何 `##[error]` 注解** | 移除，改用 runner 预装 SDK + 幂等补缺 |
| 首轮实跑 | 4 | 缺 `homepage` | AppImage 成功、deb/rpm **全部失败**（fpm 强制要求 `homepage`） | `platforms/desktop/package.json` 补 `homepage` |
| 首轮实跑 | 5 | macOS 包布局误判 | 4 个 dmg/zip 全部正确，却报 `resources 目录存在` FAIL | `verify-artifacts.mjs` 增 `resolveResources()` 双形态探测 |
| 首轮实跑 | 6 | iOS 签名开关只放 step `env` | Xcode 26 在 `GatherProvisioningInputs` 阶段就报缺 development team | 改为 xcodebuild 的 build setting（`--unsigned`） |
| 首轮实跑 | 7 | iOS 上传路径脚本从不产出 | 配合 `if-no-files-found: error` → 归档成功之后再失败一次 | 改为 `ios/build/App.xcarchive` |
| 次轮实跑 | 8 | 🔴 **`code` 公式在 MALLV0.0.0 上算出 `0`** | Gradle `versionCode is set to 0, but it should be a positive integer` → BUILD FAILED。**初始版本在 Android 上物理无法出包** | 新增 `CODE_OFFSET = 1` |
| 三轮实跑 | 9 | 🔴 Android job 配 **JDK 17** | Capacitor 8 的 `capacitor-android` 要求 `sourceCompatibility = JavaVersion.VERSION_21`（`build.gradle:66-67`）→ `error: invalid source release: 21`，且该错误**不带任何版本上下文** | `java-version: '21'` + `build.mjs` 加 JDK 主版本预检 |
| 次轮推演 | 10 | APK/AAB/IPA 用**文件原文**搜版本号 | ZIP 内文本条目默认 deflate 压缩 → **假阴性**；修好 `versionCode` 也仍会挂在"校验产物版本号" | 新增 `zip-search.mjs`（`archiveContains()`） |
| 推演+夹具 | 11 | `.xcarchive` 里的 Info.plist 是**二进制 plist** | 文本正则匹配 `<key>CFBundleVersion</key>` 恒为 null。**文档 §6 声称有此检查、实现里其实没有** | 新增 `plist.mjs`（纯 JS bplist00 读取器） |
| 推演+夹具 | 12 | 上述两处**没有回归保护** | 症状是"看起来通过"或"明明对了却判失败"，且都不抛异常，极难发现 | 新增 `selftest.mjs`（12 项自带夹具），挂进 `version-guard` |

**android 一个 job 就有三层独立根因**（#8 方案层 → #9 工具链层 → #10 校验层），
修任何一层都不足以转绿。这是"用空跑逐轮收敛"而不是"一次读代码找出全部问题"的直接理由。


**方法论沉淀**（按投入产出比排序）：

1. **先 `workflow_dispatch` 空跑，再打标签。** 打 `MALLV*` 标签会立刻创建**公开** Release，
   失败也来不及撤回；而 `workflow_dispatch` 的 `github.ref` 是 `refs/heads/master`，
   `release` job 的 `if: startsWith(github.ref, 'refs/tags/')` 会自动跳过 →
   **不产生任何公开产物**，可以反复试。
2. **模板解包求证，不要猜。** 直接解 `ios-spm-template.tar.gz` / `android-template.tar.gz`
   看真实文件树，并读 `@capacitor/cli` 的 `config.js` / `tasks/add.js` 源码定论。
   猜"应该有个 xcworkspace"就会写出必挂的工作流。
3. **静态自检先行。** PyYAML 结构断言 + `bash -n` 在花 runner 时间之前就能筛掉一批错；
   **但它们只能证明"结构对"，不能证明"能跑"**。
4. **"产物已正确、只差最后一步"是最常见的失败形态。** 11 个缺陷里有 6 个属于此类
   （5 处校验/签名逻辑 + 1 处路径），全部表现为"其实已经成功，只是被判失败"。
5. **假阴性比真失败更危险。** 真失败会红；假阴性只会"看起来通过"，或让人以为包打错了
   而去改正确的东西。对抗手段只有一个：**用夹具把陷阱本身钉死**
   —— 断言里先证明"原文搜不到"，再证明"解压后搜得到"。

## 附录 D：本轮新增文件

| 文件 | 作用 |
|------|------|
| `platforms/mobile/scripts/zip-search.mjs` | ZIP 中央目录解析 + 按条目解压查找。先走原文快路径（stored 条目仍能命中），未命中再解压；带 8 MiB 条目体积上限 |
| `platforms/mobile/scripts/plist.mjs` | plist 读取（XML + 二进制 `bplist00`）。纯 JS，不依赖 `plutil` |
| `platforms/mobile/scripts/selftest.mjs` | 上述两者的回归自测，12 项。ZIP 夹具由文件内最小 ZIP writer 现造，bplist 夹具用内嵌 base64，**不往仓库塞二进制文件、零第三方依赖** |
