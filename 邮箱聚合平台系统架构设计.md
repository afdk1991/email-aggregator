# 邮箱聚合平台 · 系统架构设计文档（可扩展整体架构 + 长期发展规划）

> 版本：v1.9 ｜ 角色：软件架构师（SoftwareArchitect）｜ 适用范围：大型邮箱聚合 SaaS / 企业私有化平台
> 文档目标：给出一套**可水平扩展、演进式、高可用、易接入第三方邮箱**的整体架构，并配套长期发展规划、容量规划、监控运维与分阶段里程碑。
> 配套：本文档与对话中渲染的架构图（整体分层架构图 / 多可用区部署拓扑图 / 分阶段里程碑时间线 / 新邮件同步时序图 / 容量规划计算器）配套阅读。
> v1.2 变更：将原《架构深化设计》的四大深化方向（模块接口与时序 / 容量模型 / Phase 0 PoC / HA·DR）折叠为第 13–16 章，并接入已交付的 Phase 0 PoC 代码（`email-aggregator/`，TypeScript 零依赖可运行、自检 5/5 通过）。
> v1.3 变更：新增 **ADR-008（Accepted）**——企业版 Exchange/EWS 正式触发 ADR-007 的 Java 边界连接器例外区；并将 Go 主干 Phase 0 PoC（`email-aggregator-go/`）核心模块（connector/sync/events/security/store/search/notify/api + 集成骨架）接入，接口契约与 TS 版一一对齐（Go 主干 PoC 已于 v1.7 用 Go 1.26.6 真机编译/测试验证通过）。
> v1.4 变更：新增 **ADR-009（Accepted）**——多租户隔离与数据驻留策略（逻辑多租户共享栈 + 企业版专属栈/私有化，同一二进制、部署形态参数化），并补 §9.4 租户路由矩阵与三种部署拓扑设计；配套租户隔离架构可视化图。
> v1.5 变更：新增 **ADR-010（Accepted）**——AI 能力策略采用**能力路由网关（混合策略）**：按租户类别与数据敏感度选路，公共 SaaS 租户在显式同意 + PII 边界脱敏后可走第三方大模型 API，企业版/私有化租户强制自托管不出境；统一 `AIGateway` 抽象（复用 ADR-005 适配器注册表 + ADR-009 租户上下文 + audit 主题）。并将 `IngestWorker` 摄取消费管线接入真实集成服务（`cmd/server_integration.go`，构建标签 integration）。
> v1.6 变更：新增 **§17 AI 能力层深化设计（ADR-010 实施）**——`AIGateway` 边界上下文 + `AIProvider` 统一接口契约 + 路由策略表（租户类别 × 敏感度 × 同意 → 后端）+ Guardrail 脱敏 + 与 audit/tenant 上下文集成；并在 Go PoC 落地 `src/aigateway/`（provider / router / redactor + 单测，零外部依赖）。
> v1.7 变更：**§17.6 Go PoC 落地从骨架升级为真实后端**——新增 `providers.go`（OpenAI 兼容 `HTTPProvider`：`NewSelfHostedProvider` 私有/企业零出境路径 + `NewThirdPartyProvider` 公共同意+脱敏路径，实现 `AIProvider` 全部能力 Chat/Embed/Classify/Summarize，纯 `net/http` 零外部依赖）；新增 `providers_test.go`（httptest 真实验证 Chat/Embed + Router 集成「第三方在 HTTP 边界收到的请求体已脱敏」）；`src/api` 非破坏性新增 `/api/ai/chat` 路由（`WithAIGateway`）+ `server_integration.go` 接入真实网关（审计事件入 Kafka audit 主题）。**Go 1.26.6 真机验证：`go build/vet/test` 全绿（aigateway 11/11、api 3/3、全包测试通过）**。
> v1.8 变更：**Phase 2 立项交付（OpenSearch 检索拆分 + Citus 分片 + 监控告警 + 服务拆分）**——(1) **ADR-009 物理隔离落地**：OpenSearch 适配器从复合索引 `mail-<tid>-<aid>` 改为 ADR-009 字面要求的 per-tenant 物理索引 `mail-<tid>`（`src/search/index.go`），新增 `cmd/reindex.go` 重建工具（scroll + bulk 幂等迁移）；Citus 分布键从 `account_id` 改为 `tenant_id`（`deploy/migrations/002_citus_sharding.sql`）实现租户级数据 colocation。(2) **§10 可观测性闭环**：`src/observ/metrics.go` 新增 OpenSearch/PG/reindex 指标埋点；`deploy/observability/` 部署 Prometheus + Grafana + Alertmanager + postgres-exporter + kafka-exporter，配套 `alerts.yml` SLO 告警规则（检索 P95、同步成功率、摄取吞吐、AI 成功率）。(3) **§11 服务拆分**：将 `cmd/server_integration.go` 单体拆为三个独立 worker——`sync_worker`（订阅 `sync-tasks`）/`ingest_worker`（订阅 `mail-ingested`）/`search_service`（HTTP 检索端点），用 Go 构建标签 `sync_worker`/`ingest_worker`/`search_service` 隔离编译；`cmd/helpers.go` 集中共享 `env/envPort/metricsServer/maskDSN`；多入口 `Dockerfile` + `deploy/docker-compose.yml` 三容器编排 + Prometheus 抓取目标。**Go 1.22 真机验证：`go build/vet/test` 全绿，三个 worker 独立编译通过。**
> v1.9 变更：**Phase 3 立项交付（多 AZ 部署 + RBAC/SSO + 真实 Citus 集群验证）**——(1) **ADR-009 网关层物理阻断落地**：新增 `src/auth/` 包，含 RBAC 静态矩阵（4 角色 × 5 动作）+ `AuthMiddleware`（Bearer 验签 → Claims 注入 → `Can()` 判定 → 403 时 emit `AuditEvent`），跨租户访问在网关层阻断（`action=cross-tenant-deny`）；OIDC SSO（Keycloak/Dex）经 `//go:build sso` 标签隔离，引入 `github.com/coreos/go-oidc/v3` + `golang.org/x/oauth2`，默认 `go build` 保持零依赖；`EmitAudit` 强制 `tenant.Resolve(ev.TenantID)` 满足审计溯源；`server_integration.go` 经 `assembleAuthMiddleware` 在 OIDC_* 环境变量存在时挂载 `WithAuth`，否则回退 PoC 无鉴权模式（向后兼容）。(2) **真实 Citus 集群验证**：`deploy/docker-compose.citus.yml` 落地 1C+2W 拓扑（Coordinator + 2 Worker），`deploy/migrations/006_citus_workers.sql` 幂等注册 worker，`scripts/verify-citus-sharding.ps1` 7 步验证 colocation 与路由（节点拓扑 / 分布式表 / 样本数据 / colocation / worker 分布 / 单租户路由 / 全量 fan-out）。(3) **多 AZ 部署**：`deploy/docker-compose.multiaz.yml` 双 AZ 模拟（PG primary/replica 流复制 + 2 Kafka broker RF=2 + 2 MinIO site replication + 跨 AZ `az_xlink` 网络），`scripts/drill-chaos.ps1` 度量 RPO（≤5 行）与 RTO（≤30s），`deploy/multiaz/RUNBOOK.md` 11 章节演练手册（拓扑 / 准备 / PG/Kafka/MinIO 故障切换 / 综合 AZ 演练 / 责任人 / 生产化差距）。**Go 1.22 真机验证：`go build/vet/test` 全绿，含 `integration,sso` 标签构建通过。**

---

## 0. 阅读指引

| 章节 | 内容 | 面向对象 |
|------|------|----------|
| 1 | 业务背景与设计目标（NFR 基线） | 全员 |
| 2 | 架构决策记录（ADR，含权衡） | 架构 / 技术负责人 |
| 3 | 系统上下文与分层架构（含架构图说明） | 架构 / 研发 |
| 4 | 核心模块划分（六大层 + 异步机制） | 研发 |
| 5 | 微服务与分布式部署方案 | 运维 / SRE |
| 6 | 水平扩展与负载均衡策略（专章） | SRE / 架构 |
| 7 | 高可用与容灾（RPO/RTO） | SRE / 架构 |
| 8 | 第三方邮箱服务商接入适配方案 | 研发 |
| 9 | 技术演进路线与容量规划 | 技术负责人 |
| 10 | 监控与运维体系 | SRE |
| 11 | 分阶段实施里程碑（含时间线） | 项目管理 |
| 12 | 风险、权衡与开放问题 | 决策层 |
| 13 | 模块接口与时序设计（深化） | 研发 / 架构 |
| 14 | 容量规划计算模型（深化） | 技术负责人 / SRE |
| 15 | Phase 0 PoC 落地计划（深化 + 已交付代码） | 研发 / 项目管理 |
| 16 | HA/DR 与备份深化 | SRE / 架构 |
| 17 | AI 能力层设计（深化，ADR-010 实施） | 研发 / 架构 |
| 18 | 变更记录（Changelog） | 全员 |

---

## 1. 业务背景与设计目标

**业务本质**：邮箱聚合平台把用户分散在多家邮件服务商（Gmail、Outlook/Exchange、企业邮箱、QQ/163 等）的邮箱统一接入、同步、检索与呈现，提供"一处管理全部邮箱"的体验，并可向企业团队输出统一邮箱工作台。

### 1.1 关键非功能需求（NFR 基线）

| 维度 | 目标（初始 / 远期） | 说明 |
|------|---------------------|------|
| 规模 | 10 万 → 100 万 → 500 万 连接账户 | 单租户可聚合多邮箱 |
| 可用性 | 99.9% → 99.95% | 平台自身，不含上游服务商故障 |
| 同步时延 | 新邮件 ≤ 60s 可见（IMAP IDLE） | 轮询账户容忍分钟级 |
| 检索时延 | P95 < 500ms | 单用户收件箱级检索 |
| 存储增长 | 线性扩展，无单点上限 | 内容与元数据分离 |
| 合规 | 数据加密、凭据隔离、审计 | 满足企业/出海合规 |
| 多租户 | 个人 / 企业团队隔离 | 企业版需 SSO、审计、配额 |

### 1.2 设计原则

1. **领域优先，技术其次**：先界定边界上下文，再选技术栈。
2. **演进式架构**：从模块化单体起步，按边界成熟度和团队规模切微服务，避免过早分布式。
3. **权衡显性化**：每个决策都写明"放弃了什么"。
4. **可反转优先**：优先选择易回退的决策（消息队列、存储分层），降低试错成本。
5. **失效隔离**：上游服务商不可控，平台必须将"第三方故障"视为常态而非异常。
6. **无状态优先**：凡是可无状态的服务都不持有状态，状态下沉到存储/队列，便于水平扩展。

---

## 2. 架构决策记录（ADR）

### ADR-001：演进式架构形态（模块化单体 → 微服务）
- **状态**：Accepted
- **决策**：以**模块化单体（Modular Monolith）**起步，强制模块间仅通过明确定义的接口/事件通信；当某模块吞吐或发布频率显著分化时，再独立为服务。
- **后果**：早期交付快、调试简单；需严格约束模块耦合（编译期依赖检查或包边界强制），否则后期难以拆分。

### ADR-002：消息队列选型 —— Kafka 为主，调度框架为辅
- **状态**：Accepted
- **决策**：主总线采用 **Kafka**（分区、持久化、可重放）；定时/重试类任务采用分布式调度（Temporal / 自研分片调度）；即时通知走轻量通道（WebSocket / Webhook）。
- **后果**：获得削峰、重放、事件溯源能力；运维复杂度高于 RabbitMQ，早期可用托管版（云厂商 CKafka）。

### ADR-003：存储分层（元数据 / 内容 / 索引三权分立）
- **状态**：Accepted
- **决策**：元数据 → PostgreSQL（按 `account_id` 分片）；原始内容/附件 → 对象存储 + 内容寻址（hash）；检索 → OpenSearch/Elasticsearch 倒排索引，按租户/用户隔离。
- **后果**：各自独立扩展、成本可控；需保证元数据与索引最终一致（事件对齐）。

### ADR-004：凭据安全 —— Token Vault + KMS 信封加密
- **状态**：Accepted
- **决策**：OAuth 令牌与密码经 **KMS 信封加密**存入专用凭据库（与业务库物理隔离），运行时由连接服务短期内存解密使用，禁止落明文日志。
- **后果**：满足合规、降低泄露面；增加密钥轮换与灾备复杂度（密钥须随备份受控复制）。
- **v1.9 落地状态**：**多 AZ 部署拓扑验证**——`deploy/docker-compose.multiaz.yml` 落地 PG primary/replica 流复制 + MinIO site replication + Kafka 多 broker RF=2，跨 AZ 凭据密钥环需在两个 AZ 镜像（生产化要求：KEK 按 AZ 分主备 + 轮换审计）。`deploy/multiaz/RUNBOOK.md` §9 列出生产化差距：从「PoC 手动 promote + 改环境变量重启」→「Patroni 自动选主 + LB 健康检查退避」+ DNS TTL 60s 切换。

### ADR-005：接入层适配器模式（Plugin Adapter + Canonical Model）
- **状态**：Accepted
- **决策**：所有第三方邮箱以**适配器插件**注册到统一接口，内部以**统一邮件模型（Canonical Mail Model）**表示；适配层负责协议↔模型双向映射。
- **后果**：新增服务商零侵入；代价是每个新协议需实现归一化映射与边界用例（标志、文件夹语义差异）。

### ADR-006：技术线选型 —— Go 主干（★ 已锁定）
- **状态**：Accepted
- **决策**：生产环境后端以 **Go 1.22+** 为统一主干（connector / sync / ingest / index / notify / auth / account 全部 Go）；前端 React + TypeScript；BFF 用 Go（gin）。中间件与基础设施：消息 Kafka（Redpanda 兼容）、元数据 PostgreSQL（Patroni + Citus 分片）、内容对象存储、索引 OpenSearch、缓存 Redis、凭据云 KMS / Vault、编排 K8s + Istio。
- **备选与权衡**：
  - **B. Java 21 主干（Virtual Threads）**：企业集成生态最全（Exchange / SSO / 审计库最多），但运行时重、冷启动慢；仅在团队强 JVM 时采用。
  - **C. 全 Node/TS**：出 PoC 最快，但百万级 IMAP IDLE 长连接下内存/调度压力更大，需谨慎压测；仅适合规模适中或团队全 JS。
- **后果**：后端单一语言，运维与协作成本最低；放弃"全 JS 单一栈"的少语言红利（C 线）。Phase 0 PoC 的 TypeScript 实现仅用于零依赖验证架构与接口，后续平移为 Go（接口契约不变，平移成本低）。

### ADR-007：有界多语言策略（Go 默认 + Node/TS BFF/前端 + Java 例外区）
- **状态**：Accepted
- **决策**：允许 A/B/C 三条技术线**混用，但有界**——不搞"三足鼎立平等主干"，而是：
  1. **Go = 默认后端主干**：connector / sync / ingest / index / notify / auth / account 等"重活、长连接、高并发"一律 Go（继承 ADR-006）。
  2. **Node/TS = BFF + 前端 + 工具**：API 网关/BFF、Web SPA、WebSocket 推送、PoC 与脚本。前端团队天然持有，零额外语言成本；**不**引入 Go 之外的后端核心服务。
  3. **Java = 例外区（须 ADR 批准）**：仅在 JVM 生态明显占优的**边界连接器**（如企业版 EWS / SSO / SCIM 库）经 ADR 批准，以"藏在 `Connector` 接口后的独立边界服务"形式引入，不得作为全局默认。
- **成立前提**：ADR-005 适配器注册表 + 统一 `CanonicalMail` 模型 + Kafka 事件总线已提供**语言无关的契约边界**，服务间只通过事件/接口通信，不互相进程内直接调用——故一个 Java 企业连接器与一个 Go 同步器可并存而不耦合。
- **强制规则**：
  1. 默认一种语言（Go）写进团队约定，省去每次选型纠结；
  2. 任何非默认语言后端服务（Node/TS 后端、Java）**必须凭 ADR 入场**，并锁在契约边界内；
  3. 每个非默认语言服务须有独立构建链、基础镜像、发布流水线、oncall 文档（CVE/依赖单独管理）。
- **后果**：兼顾 Go 并发主场、Node 前端团队红利、Java 企业集成红利；代价是 1~2 条额外构建链与 oncall 心智，已通过"例外区"约束最小化。这是对 ADR-006"单一 Go 主干"的**务实修正**：主干仍是 Go，但承认 JVM/TS 在局部边界的真实优势，用 ADR + 接口边界而非行政禁止来管理。

### ADR-008：触发 Java 边界连接器例外区（企业版 Exchange / EWS）
- **状态**：Accepted
- **前置**：本 ADR 即 ADR-007 第 3 条强制规则所要求的"非默认语言入场批准"——企业版 Exchange（EWS / Microsoft Graph）连接器**正式触发** Java 例外区。
- **问题背景**：企业客户（Exchange Online / 本地 Exchange、Active Directory、SAML/OIDC IdP、SCIM 账号开通）需要深度集成。JVM 生态在该领域成熟度显著领先：Microsoft Graph Java SDK、EWS Java API、MSAL4J（OAuth2）、Spring Security SAML/LDAP、SCIM 2.0 SDK 均为一等公民；Go 侧等价库要么不成熟（没有官方 EWS/Graph SDK），要么需自研 SOAP/协议，重写成本高且易踩企业合规细节坑。
- **候选方案**：
  1. **Go 自研 EWS/Graph 连接器**：复用 `go-imap` 不可行（EWS 非 IMAP），需自封装 SOAP/REST——工作量与原生协议风险高，企业场景容错低。
  2. **Java 例外区边界服务（采纳）**：以独立"企业连接器服务"形式落地，藏在 `Connector` 接口之后，仅经 Kafka 事件与主干通信——复用 JVM 成熟库，风险最低、上市最快。
  3. **sidecar 包裹现有 Java 库（gRPC/HTTP）**：多一跳延迟与运维面，收益不如方案 2 干净。
- **决策**：采纳方案 2。企业连接器以 **独立的 Java 边界服务** 部署，须满足 ADR-007 全部强制规则：
  - 实现 `model.Connector` 接口（与 IMAP/POP3 适配器同契约），使 Go 编排器 / 同步器 / 采集器**零差别**对待；
  - 仅经 Kafka 事件通信（入：`sync-tasks`；出：`mail-ingested` / `mail-index` / `notifications` / `audit`），**禁止**与 Go 服务进程内耦合；
  - 独立构建链、独立基础镜像（JRE/JDK slim）、独立发布流水线、独立 oncall 文档，依赖与 CVE 单独扫描；
  - 凭据同样走 KMS 信封加密（ADR-004），密钥与 Go 主干共用同一 KMS。
- **落地映射**（参考，非限定）：Exchange Online → Microsoft Graph Java SDK；本地 Exchange → EWS Java API；OAuth2 → MSAL4J；企业 SSO/SAML → Spring Security；账号生命周期 → SCIM 2.0 SDK。凭证/授权流程与 IMAP 适配器一致（OAuth2 PKCE / 应用专用密码）。
- **后果**：以最小代价解锁企业版客户群，复用 JVM 企业集成红利；代价是新增一条 JVM 构建/运行链与相应 oncall 心智——已被 ADR-007 的"例外区"边界严格约束（本质上只是"又一个连接器"）。这是对 ADR-006 主干（Go）的**边界例外**，不改变主干默认语言。

### ADR-009：多租户隔离与数据驻留策略（租户上下文强制传播 + 专属栈/私有化）
- **状态**：Accepted
- **问题背景**：平台同时服务 (a) 公共 SaaS 多租户（海量个人/小团队邮箱聚合）与 (b) 企业版（含数据驻留/不出境、私有化交付诉求）。租户间数据必须零交叉读取，且企业版需满足监管的数据驻留（数据存于指定区域、绝不跨境）与可私有化（整套栈部署在客户 VPC/机房）。两者都要求一套**贯穿全链路的租户上下文 + 隔离边界**，否则任一层的疏漏都会造成跨租户泄露。
- **候选方案**：
  1. **纯共享多租户（单栈，按 tenant_id 逻辑隔离）**：成本最优、运维单一；但隔离靠"每层都正确带 tenant_id"，任一服务忘记过滤即越权，且无法满足"数据不出境/私有化"硬诉求。
  2. **每租户独立实例（物理隔离）**：隔离最彻底、合规最易证明；但资源与运维成本随租户数线性爆炸，不适合海量 SaaS 长尾。
  3. **混合形态（采纳）**：默认**逻辑多租户共享栈**（按 account_id 路由/分片，租户上下文经中间件强制注入），企业版按需升级为**单租户专属栈**或**私有化部署**——同一套代码、不同部署拓扑与数据边界。
- **决策**：采纳方案 3，落地要点：
  - **租户主键 = account_id 的归属租户（tenant_id）**。所有存储/索引/消息/密钥均租户可路由：
    - PG：按 `account_id` 分片（Citus），行级强制 `tenant_id` 过滤（经查询中间件 / Row-Level Security 兜底，不依赖业务代码自觉）；
    - 对象存储：key 前缀 `tenant-<tid>/mail/<sha256>`，桶或前缀级隔离；
    - OpenSearch：索引 `mail-<tid>` 或文档带 `tenantId` 路由，查询强制加 `tenantId` 过滤；
    - Kafka：分区键用 `account_id`，企业栈可加独立 topic 前缀，保证同租户有序且不被错读；
    - KMS：信封加密的 KEK 按 `tenant_id` 派生密钥环（ADR-004），跨租户无法解封。
  - **租户上下文强制传播**：connector 采集 → sync 编排 → store/index/notify 全链路携带 `tenant_id`（埋入 `SyncTaskEvent` / `MailIngestedEvent` 等事件与请求上下文），任何出口查询都强制 `tenant_id` 约束（anti-corruption 边界）；缺失上下文的请求在网关层拒绝。
  - **部署拓扑（数据驻留）**：
    - 公共 SaaS：多区域共享栈，用户数据存于所选区域（region affinity），跨区仅复制元数据用于容灾（ADR-003 / §16），内容对象按区域驻留；
    - 企业版专属栈：单租户独占一套（独立 PG/OS/对象/Kafka/KMS 实例），数据 100% 不出该栈；
    - 私有化：整套栈部署于客户 VPC/机房，平台侧零数据出境。
  - **合规与审计**：所有 `audit` 事件带 `tenant_id` 与操作者；跨租户访问在网关 / RLS 层被物理阻断，可被审计证明。
- **与既有 ADR 衔接**：ADR-003（存储三权分立）的 `account_id` 分片天然成为租户隔离基座；ADR-004（KMS 信封）的按租户 KEK 派生实现**密钥级隔离**；ADR-005（Connector 边界）确保企业 Java 连接器（ADR-008）同样在租户上下文内工作；ADR-007 多语言不影响租户边界（边界在服务契约层）。
- **后果**：+ 一套代码同时支撑 SaaS 长尾与企业合规硬诉求，成本与合规兼得；+ 逻辑隔离失败域被 RLS / 网关兜底。代价：共享栈必须严格维护租户上下文（靠中间件而非自觉），专属/私有栈带来额外部署与运维面——以"部署形态参数化 + 同一二进制"最小化代价。
- **v1.8 落地状态**：**Phase 2 已完成 ADR-009 字面硬约束的物理隔离落地**——
  - OpenSearch：`src/search/index.go` 从复合索引 `mail-<tid>-<aid>` 重构为 **per-tenant 物理索引 `mail-<tid>`**（严格字面对齐本 ADR 决策），查询经 `tenantId` 字段过滤；`cmd/reindex.go` 提供 scroll + bulk 幂等迁移工具。
  - PG/Citus：`deploy/migrations/002_citus_sharding.sql` 将分布键从 `account_id` 调整为 **`tenant_id`**，实现同租户数据 colocation 与审计分片合规。
  - 对象存储：key 前缀 `tenant-<tid>/mail/<sha256>` 维持不变（Phase 0 已落地）。
  - 租户上下文：`tenant.Resolve(ev.TenantID)` 在 orchestrator/worker/WS 全链路强制传播，`X-Tenant-Id` 经 `fromRequest(req)` 注入检索查询。
- **v1.9 落地状态**：**Phase 3 网关层物理阻断 + 真实集群验证**——
  - **网关层跨租户阻断**：`src/auth/middleware.go` 在 Bearer 验签后比对 `claims.TenantID` 与 `X-Tenant-Id` 头，不一致即 `403 cross-tenant-deny` + emit `AuditEvent`，从「靠业务层自觉 WHERE 过滤」升级为「网关层物理阻断」，篡改 JWT tenantId 也不会越权。
  - **RBAC 静态矩阵**：`src/auth/rbac.go` 4 角色（owner/admin/member/viewer）× 5 动作（mail.read/mail.write/account.create/account.manage/admin.tenant），`Can()` 判定在 `Require(action)` 中间件执行；`StaticResolver` 从 JWT groups claim 取角色。
  - **OIDC SSO 集成**：`src/auth/oidc.go` (`//go:build sso`) 实现 KeycloakVerifier（`go-oidc/v3` + `oauth2`），`server_integration.go` 经 `assembleAuthMiddleware` 在 OIDC_* 环境变量存在时挂载 `WithAuth`，否则回退 PoC 无鉴权模式（向后兼容 Phase 1/2）。
  - **审计溯源**：`src/auth/audit_sink.go` 的 `EmitAudit` 强制 `tenant.Resolve(ev.TenantID)` + 非空 actor，所有 deny 事件 100% 带 tenantId 满足「可审计证明」硬约束。
  - **真实 Citus 集群验证**：`deploy/docker-compose.citus.yml` 1C+2W 拓扑 + `006_citus_workers.sql` worker 注册 + `scripts/verify-citus-sharding.ps1` 7 步验证（节点拓扑 / 分布式表 / 样本数据 / colocation / worker 分布 / 单租户路由 / 全量 fan-out），证明 ADR-009 分布键 `tenant_id` 在真实分布式集群上生效。

### ADR-010：AI 能力策略（能力路由网关 · 混合后端 · 租户感知）
- **状态**：Accepted
- **问题背景**：平台天然拥有高价值 AI 场景——智能分类/优先级收件箱、邮件摘要、智能回复起草、钓鱼/欺诈检测、语义检索增强。但引入大模型面临硬约束：(1) **数据驻留（ADR-009）**——企业版/私有化租户的邮件正文**绝不可出境**，纯第三方 API 直接违反；(2) 成本随调用量线性增长、供应商锁定、PII 暴露面；(3) 不同租户对质量/延迟/合规的容忍度差异极大。需要一套既能快速享用到大模型红利、又不击穿合规边界的能力策略。
- **候选方案**：
  1. **纯第三方大模型 API（OpenAI / Claude / 通义 / GLM）**：上市最快、质量上限高、零 GPU 运维；但邮件正文（含 PII）离开平台边界，违反企业/私有租户的数据驻留（ADR-009），且每 token 成本、厂商锁定、提示注入面扩大。
  2. **纯自托管开源权重（vLLM + Qwen/Llama，自有 GPU）**：数据 100% 不出境、无按量计费、可按租户隔离；但 GPU 资本/运维开支、模型质量在部分任务上的差距、吞吐/延迟工程、模型迭代节奏由自己承担。
  3. **能力路由网关 / 混合后端（采纳）**：定义统一 `AIGateway` 抽象（实现 `AIProvider` 接口，复用 ADR-005 适配器注册表思想），**按请求上下文路由**：
     - 租户类别（来自 ADR-009 的 `tenant_id` 上下文）+ 数据敏感度分级 → 选路；
     - **企业版 / 私有化租户 + 任何含正文的请求 → 强制自托管模型**（同区域/同 VPC，数据零出境）；
     - **公共 SaaS 租户 → 第三方 API**，但须满足：用户**显式同意** + 边界 **PII 脱敏/红acted**（发往前的最小化载荷，地址/电话/卡号等先剥离）+ 全量 AI 调用入 `audit` 主题（操作者/租户/用途）。
- **决策**：采纳方案 3，落地要点：
  - **`AIGateway` 内部服务**（新建边界上下文）：实现 `AIProvider` 接口（Chat / Embed / Classify / Summarize 等能力方法），注册多后端（自托管 vLLM、第三方 API、未来本地小模型）；路由策略表由配置驱动，按 `tenant_id` + 能力 + 敏感度选路。
  - **边界脱敏层（Guardrail）**：所有出平台的请求先过 PII 扫描与最小化，仅携带完成任务所必需字段；对入站模型输出做提示注入 / 越权防御（如"忽略系统指令"类对抗）。
  - **租户上下文强制传播**：AI 调用与 `audit`、索引、通知一样，全链路携带 `tenant_id`（继承 ADR-009），私有租户的后端绑定在其专属栈，网关层拒绝任何"私有租户 → 第三方后端"的路由。
  - **可观测与评估**：AI 调用统一埋点（延迟、token、命中后端、脱敏命中、失败），建立质量评估集（golden set）做回归，避免模型升级悄悄劣化关键能力。
  - **演进路径**：Phase 0/1 先以"第三方 API（公共租户）+ 脱敏 + audit"跑通闭环并验证价值；企业/私有租户随私有化交付（ADR-009 专属栈）同步上线自托管后端；后续评估是否对长尾公共租户也切到自托管以降成本/降暴露面。
- **与既有 ADR 衔接**：ADR-005（适配器注册表 + 统一契约）提供 `AIGateway`/`AIProvider` 的边界范式；ADR-009（租户上下文 + 数据驻留）是路由的强制约束与物理边界；Kafka `audit` 主题（ADR-002）承载 AI 调用审计；错误/超时走与同步相同的退避 + DLQ 思路。
- **后果**：+ 合规安全（私有租户数据零出境、公共租户默认脱敏 + 可审计）；+ 不被单一厂商锁定、质量/成本可按租户调优；+ 复用既有架构范式（适配器/租户上下文/审计），新增心智小。代价：- 新增 `AIGateway` 服务与自托管 GPU 运维面（仅企业/私有路径需要）；- 需建脱敏与评估体系；- 路由策略需随合规变化维护（以配置化 + ADR 治理）。

---

## 3. 系统上下文与分层架构（含架构图说明）

### 3.1 系统上下文（C4 Level 1）

```
终端用户 / 企业团队 ──Web/移动/开放API──▶ 邮箱聚合平台
邮箱聚合平台 ──IMAP/POP3──▶ Gmail / 163 / QQ 等
邮箱聚合平台 ──EWS / Microsoft Graph──▶ Outlook / Exchange
邮箱聚合平台 ──IMAP / 专有API──▶ 企业邮箱 / 其他服务商
邮箱聚合平台 ──指标/审计/告警──▶ 运维监控与告警平台
第三方集成(日历/联系人/Webhook) ──可选──▶ 邮箱聚合平台
```

**边界说明**：上游邮箱服务商被建模为**不可控外部依赖**。平台对它们的调用必须全部经过超时、重试、熔断与限流（见 §6、§7、§8）。

### 3.2 整体分层架构（C4 Level 2，详见对话内"整体分层架构图"）

系统自上而下分为八层，所有有状态组件集中下沉到最底层独立扩展：

1. **客户端层**：Web（SPA/SSR）、移动端、开放 API 调用方。
2. **接入边界层**：API 网关 / BFF（统一鉴权、限流、路由、协议聚合）。
3. **领域服务层**：用户与权限、连接/账户、同步编排、采集解析、索引、通知等微服务。
4. **协议接入层**：IMAP / POP3 / Exchange / Gmail / 企业邮箱 适配器（插件化）。
5. **消息与异步层**：Kafka 事件总线 + 分布式调度。
6. **聚合与同步层**：编排状态机 + 增量采集 + 归一化 + 冲突去重。
7. **存储与索引层**：PostgreSQL（元数据）、对象存储（内容/附件）、OpenSearch（索引）、Redis（缓存）。
8. **基础设施层**：KMS、配置中心、可观测性（Metrics/Logs/Traces）、CI/CD、IaC。

### 3.3 架构图说明（如何阅读配套图）

| 图 | 说明 |
|----|------|
| 整体分层架构图 | 展示八层依赖与数据流向：客户端 → 网关 → 领域服务 → 适配层/存储；强调"状态下沉、无状态可水平扩展"。 |
| 多可用区部署拓扑图 | 展示核心服务跨 ≥2 AZ 无单点部署，有状态组件跨 AZ 副本，入口 GTM 多区域切换。 |
| 分阶段里程碑时间线 | 展示 Phase 0→4 的目标、关键交付与容量曲线，对应 §11。 |

---

## 4. 核心模块划分

### 4.1 邮件协议接入层（Protocol Access Layer）
**职责**：屏蔽不同服务商协议差异，向上提供统一邮箱操作抽象（`connect / listFolders / fetchMessages / pushSubscribe`）。

| 适配器 | 协议/接口 | 接入方式 | 备注 |
|--------|-----------|----------|------|
| IMAP 适配器 | IMAP4 (RFC 3501/9051) | 轮询 + **IDLE 推送** | 主流个人邮箱；优先 IDLE 降低时延 |
| POP3 适配器 | POP3 (RFC 1939) | 仅拉取、下载即删或保留 | 能力弱（无文件夹/旗帜），差异化处理 |
| Exchange 适配器 | EWS（legacy）→ **Microsoft Graph** | OAuth2 + 订阅推送 | EWS 已弃用路线，新接入走 Graph |
| Gmail 适配器 | Gmail API + XOAUTH2 | OAuth2 + 历史增量 | 官方 API 更稳，限流严格 |
| 通用/企业邮箱 | IMAP / 专有 API | 灵活适配 | 插件式适配器注册 |

**关键设计**：适配器注册表（Adapter Registry）、统一邮件模型（Canonical Mail Model）、连接池与配额管理、单适配器熔断隔离。

### 4.2 数据聚合与同步层（Aggregation & Sync Layer）
**职责**：将各邮箱数据增量、可靠地汇聚到平台存储并保证最终一致。

**同步策略矩阵**：

| 场景 | 策略 | 时延 | 成本 |
|------|------|------|------|
| IMAP IDLE 邮箱 | 长连接推送 + 增量 fetch | 秒级 | 中（长连接占用） |
| 支持增量 API | 游标/历史 ID 增量拉取 | 分钟级 | 低 |
| 仅轮询邮箱 | 指数退避定时轮询 | 分钟~小时 | 低 |
| 初始全量 | 分批回溯 + 限速 | 小时级（后台） | 高（限流保护） |

**核心组件**：同步编排服务（状态机）、采集与解析服务（MIME 解析、附件剥离、归一化、产出事件）、冲突与去重（幂等键 + 内容寻址）、最终一致（元数据为主、索引异步）。

### 4.3 消息队列与异步处理（贯穿全栈）

```
同步编排 ──提交同步任务──▶ Kafka(sync-tasks)
Kafka(sync-tasks) ──▶ 采集服务
采集服务 ──MailIngested 事件──▶ Kafka(mail-ingested)
Kafka(mail-ingested) ──▶ 索引服务 / 通知服务 / 审计分析
```

- **主题划分**：`sync-tasks`、`mail-ingested`、`mail-index`、`notifications`、`audit`。
- **消费语义**：至少一次投递 + 幂等消费（幂等键去重），可重放。
- **重试与死信**：失败指数退避重试，超阈值进 DLQ 复核。
- **背压与削峰**：上游限流导致采集慢时队列缓冲，下游按能力消费，保护存储与索引。

### 4.4 存储层（Storage Layer）

| 数据 | 存储 | 扩展方式 |
|------|------|----------|
| 邮件元数据、文件夹、用户/账户、配额 | PostgreSQL（按 `account_id` 分片） | 分库分表 + 只读副本 |
| 原始内容、附件 | 对象存储（S3/OSS/COS），内容寻址 | 水平无限扩展 |
| 会话/缓存、限流计数 | Redis 集群 | 分片 |
| 归档冷数据 | 对象存储低频层 / 数据湖 | 生命周期策略 |

- **分片键**：`account_id` 或哈希分片，保证单用户数据局部性，便于迁移与配额隔离。
- **多租户隔离**：企业版按 `tenant_id` 物理/逻辑隔离，配额与审计独立。

### 4.5 搜索与索引层（Search & Index Layer）
- **引擎**：OpenSearch / Elasticsearch 倒排索引。
- **隔离模型**：每用户/租户独立索引或索引别名 + 路由，避免跨用户泄露；超大用户可拆分索引。
- **索引流程**：`mail-ingested` 事件 → 解析正文/附件文本（可选 OCR）→ 写索引 → 标记可检索。
- **一致性**：索引延迟秒~分钟级；以元数据为准做"已收未索"兜底。
- **成本分级**：仅正文 / 含附件名 / 全量 OCR 三档。

### 4.6 用户与权限管理层（User & Permission）
- **认证**：平台账户 OAuth2/OIDC；企业版对接 SSO（SAML/SCIM）。
- **授权**：RBAC + 资源级策略；企业管理员管理成员、邮箱连接、配额、审计。
- **凭据保险库**：邮箱访问令牌经 KMS 信封加密存储，运行时短期解密，零明文落盘/落日志。
- **多租户**：个人 / 企业团队隔离，配额与审计独立。

### 4.7 API 与前端服务层（API & Frontend）
- **API 网关 / BFF**：统一鉴权、限流、路由、协议转换；前端经 BFF 聚合所需数据。
- **接口形态**：REST（通用）+ GraphQL（前端灵活取数）按需；开放 API 供第三方集成。
- **前端**：Web + 移动端，统一设计系统；重客户端状态与离线缓存。
- **实时通道**：WebSocket / SSE 推送新邮件与状态变更。

---

## 5. 微服务与分布式部署方案

### 5.1 服务划分（边界上下文）

| 服务 | 边界上下文 | 有状态？ |
|------|-----------|----------|
| gateway-service（BFF） | 接入聚合 | 无 |
| auth-service（IAM） | 认证授权 | 无（依赖 PG/Redis） |
| account-service | 连接/账户/凭据元数据 | 无 |
| connector-service | 协议适配（按协议可再拆） | 无 |
| sync-orchestrator | 同步调度状态机 | 无（状态在 PG） |
| ingestion-service | 采集解析 | 无 |
| mail-store-service | 元数据/内容读写 | 无（状态在 PG/对象存储） |
| search-service | 索引读写 | 无（状态在 OpenSearch） |
| notification-service | 推送/Webhook | 无 |
| admin/ops | 后台管理 | 无 |

### 5.2 部署形态
- **容器化**：所有服务 Docker 化，Kubernetes 编排；Deployment + HPA。
- **服务网格**：Istio / Linkerd 做东西向流量、mTLS、熔断、灰度。
- **配置与密钥**：配置中心（Nacos/Apollo）+ KMS，密钥不进镜像。
- **有状态组件**：PostgreSQL（Patroni 高可用）、Kafka（≥3 副本）、OpenSearch（多分片副本）、对象存储/Redis（集群化），均独立于业务 Pod 部署并独立备份。

---

## 6. 水平扩展与负载均衡策略（专章）

### 6.1 无状态化原则
凡是可无状态的服务（网关、领域服务、采集、索引、通知）**不持有会话状态**，状态下沉到 PostgreSQL / 对象存储 / Redis / Kafka。无状态是水平扩展的前提。

### 6.2 网关与边缘负载均衡（L7）
- **外部入口**：DNS + 全局流量管理（GTM）实现多区域容灾切换；L7 负载均衡（ALB / Envoy / Nginx）做 TLS 终结、限流、路由。
- **限流**：边缘令牌桶（按用户/API Key/租户）防止单调用方打爆平台；网关层统一熔断。

### 6.3 内部负载均衡（服务网格 + 客户端）
- **服务网格**：Istio/Linkerd 提供 mTLS、重试、超时、熔断与客户端负载均衡（一致哈希可选）。
- **Kafka 再均衡**：采集/索引/通知 consumer group 随实例数自动再均衡分区，天然水平扩展。

### 6.4 数据局部性与分片路由
- **一致性哈希**：同步任务、采集 worker 按 `account_id` 一致性哈希分片，保证会话亲和与数据局部性，新增/下线实例仅迁移少量分片。
- **长连接调度**：IMAP IDLE 长连接按实例连接数配额分配，避免单实例连接风暴；连接数接近上限时触发横向扩容。

### 6.5 弹性伸缩（HPA / VPA）
- **网关/领域服务**：基于 CPU/QPS 的 HPA。
- **采集 worker**：基于 Kafka 消费滞后（lag）与队列深度的 HPA —— 上游限流缓解后自动缩容。
- **索引服务**：基于索引吞吐 + OpenSearch 写入压力的 HPA。
- **有状态组件**：通过扩容副本/分片 + 只读副本分担读流量，非简单 HPA。

### 6.6 缓存与读扩展
- Redis 集群缓存会话、文件夹列表、近期邮件摘要；热点读走副本，降低主库压力。

---

## 7. 高可用与容灾设计

| 项目 | 设计 |
|------|------|
| 多可用区 | 核心服务跨 ≥2 AZ 部署，无单 AZ 依赖 |
| 数据库 | 主从异步/半同步复制；自动故障转移（Patroni） |
| 消息队列 | 多副本、跨 AZ；支持重放 |
| 存储 | 对象存储多副本 + 跨区复制（关键租户） |
| 熔断限流 | 上游故障熔断，平台降级（只读/排队） |
| 备份 | 元数据每日全量+增量（PITR）；内容对象版本化；密钥随备份受控复制 |
| RPO / RTO | 元数据 RPO ≤ 5min、RTO ≤ 15min；内容 RPO 接近 0（多副本） |
| 演练 | 季度级故障注入（chaos）与容灾切换演练 |

**失效隔离优先级**：保障"已同步数据可读、可检索"优先于"实时拉取"。上游故障时平台进入降级模式（展示本地已聚合数据 + 排队重试），不阻断核心阅读体验。

### 7.1 v1.9 落地状态（Phase 3 多 AZ 部署验证）

**PoC 双 AZ 拓扑已落地**——`deploy/docker-compose.multiaz.yml` 模拟双可用区，三大主存储组件均跨 AZ 部署：

| 组件 | AZ-A | AZ-B | 跨 AZ 机制 | 验证脚本 |
|------|------|------|-----------|---------|
| PostgreSQL | `pg-primary`（5433，可写） | `pg-replica`（5434，只读） | 流复制（`primary_conninfo`） | `drill-chaos.ps1` 度量 RPO/RTO |
| Kafka | `kafka-a`（9192，broker id=1） | `kafka-b`（9193，broker id=2） | RF=2 + `broker.rack` | `RUNBOOK.md` §5 演练 |
| MinIO | `minio-a`（9000） | `minio-b`（9001） | site replication | `RUNBOOK.md` §6 演练 |

**网络隔离**：`az_a` / `az_b` / `az_xlink` 三网络——`az_xlink` 模拟跨 AZ 专线，容器分别接入本 AZ 网络 + `az_xlink`，可注入"断开 `az_xlink`"模拟整 AZ 失联。

**故障注入与 RPO/RTO 度量**（`scripts/drill-chaos.ps1`）：
- **RPO**：primary 写入 N 行后立即查 replica 行数，差值即数据丢失窗口（流复制延迟）。PoC 目标 RPO ≤ 5 行。
- **RTO**：`docker compose stop pg-primary` → `pg_promote()` replica → 写入 canary 行 → 计时。PoC 目标 RTO ≤ 30s。
- **MinIO site replication 验证**：在 site-a 上传对象后立即查 site-b，确认副本可见。

**生产化差距**（`deploy/multiaz/RUNBOOK.md` §9 明列）：
1. **Patroni 自动选主**：PoC 为手动 `pg_promote()`；生产需 Patroni + etcd 自动故障转移 < 30s。
2. **LB 健康检查退避**：PoC 为改环境变量重启；生产需 HAProxy/Envoy 健康检查 + DNS TTL 60s 切换。
3. **Kafka RF=3 + min.insync.replicas=2**：PoC RF=2 仅证跨 AZ 副本；生产需 RF=3 容忍单节点+单 AZ 同时故障。
4. **MinIO 跨区域（DR）**：PoC 仅 site replication 同 region 双 AZ；生产需跨 region 异步复制满足 DR RPO ≤ 5min。

---

## 8. 第三方邮箱服务商接入适配方案

### 8.1 适配器架构
- **统一接口**：`connect() / listFolders() / fetchMessages() / pushSubscribe() / healthCheck()`。
- **适配器注册表**：新服务商以插件注册，框架按 `provider_type` 路由，杜绝 `if/else` 协议分支。
- **Canonical Mail Model**：内部统一数据结构，适配层负责协议↔模型双向映射（含文件夹语义、旗帜、已读状态差异）。

### 8.2 协议接入细节

| 协议 | 增量机制 | 推送 | 认证 | 注意点 |
|------|----------|------|------|--------|
| IMAP | `UIDVALIDITY`+`UIDNEXT`+`HIGHESTMODSEQ`（CONDSTORE/QRESYNC） | IDLE | 密码 / OAuth2 | 用 modseq 做增量 fetch，避免全量 |
| POP3 | `UIDL` 去重 | 无 | 密码 / APP 密码 | 无文件夹/旗帜，仅收件箱模型 |
| Exchange | EWS `SyncFolderItems` → Graph `delta` | Graph 订阅/Webhook | OAuth2 | 新接入走 Graph，EWS 已弃用 |
| Gmail | `historyId` 增量 | Cloud Pub/Sub | XOAUTH2 | 限流严格，走官方 API |
| 企业邮箱 | IMAP / 专有 | 视实现 | OAuth2 / 密码 | 插件灵活适配 |

### 8.3 凭据与配额治理
- **OAuth2 流程**：授权码 + PKCE，token 经 KMS 信封加密入凭据库；自动刷新、失败重授权。
- **限流退避**：按服务商配额令牌桶；遇 429/节流指数退避；分布式采集调度错峰，避免"惊群"。
- **熔断**：单适配器外部调用包裹熔断，单服务商故障不拖垮全局。

---

## 9. 技术演进路线与容量规划

### 9.1 技术演进路线
```
Phase0 模块化单体 MVP → Phase1 多协议/事件化 → Phase2 检索强化/微服务拆分/水平扩展
→ Phase3 多租户企业版/HA-DR/全球 → Phase4 智能化 AI 邮箱助手/开放生态
```
- **存储/中间件**：托管单体 → 分片集群 → 多区域。
- **同步引擎**：轮询 → IDLE/推送 → 边缘就近采集（降跨境延迟）。
- **智能化**：规则分类 → ML 智能分类/摘要/起草（本地化推理，隐私优先）。
- **开放生态**：开放 API → 插件市场 → 与日历/联系人/任务深度联动。

### 9.2 容量规划（模型 + 示例假设）
> 以下为**规划模型与示例数字**，上线后以真实遥测校准。

**假设（单用户）**：日均新邮件 50 封，平均元数据 2KB/封，平均内容 75KB/封（含附件），保留期 2 年。

| 资源 | 估算公式 | 100 万账户示例 |
|------|----------|----------------|
| 元数据 | 50×2KB×365×2 ≈ 73MB/用户 | ≈ 73 TB（分片） |
| 内容/附件 | 50×75KB×365×2 ≈ 2.7GB/用户 | ≈ 2.7 PB（对象存储） |
| 索引 | 元数据 1.5–3 倍 | ≈ 150–220 TB |
| 日均采集事件 | 100万×50 = 5000 万/日 | Kafka 峰值数百 MB/s |
| 检索 QPS | 人均 5 次/日 → 500 万/日 | 峰值数千 QPS |

**扩展杠杆**：内容冷热分层（热 30 天本地、冷转低频/归档）、索引按活跃度分级、采集长连接按账户活跃度调度。

### 9.3 技术线选型（Go 主干 ★ + 有界多语言，ADR-006/ADR-007/ADR-008/ADR-009/ADR-010）

经评估三条技术线（详见 ADR-006），锁定 **A. Go 主干**：

| 技术线 | 主干语言 | 长连接/并发 | 企业集成 | 决策 |
|--------|----------|-------------|----------|------|
| **A. Go 主干 ★** | Go 1.22+ | 最优（goroutine） | 中（Graph/Gmail 走 HTTP） | **已锁定** |
| B. Java 21 主干 | Java 21（Virtual Threads） | 优 | 最全 | 备选（强 JVM 团队） |
| C. 全 Node/TS | TypeScript | 一般 | 中 | 备选（规模适中） |

前端统一 React + TypeScript；基础设施（Kafka / PG / 对象存储 / OpenSearch / Redis / KMS / K8s）与 §4–§8、§13–§16 一致。百万级 IMAP IDLE 长连接场景下，Go 的 goroutine-per-connection 在内存与调度上优于 Node/TS，是锁定的主因。

> **有界多语言（ADR-007）**：Node/TS 承担 BFF 与前端、Java 仅作企业边界连接器例外区，三者并存但"Go 为默认主干"不变；任何非默认语言服务须凭 ADR 入场并锁在 `Connector` / 事件边界内。
> **Java 例外区已触发（ADR-008）**：企业版 Exchange / EWS 连接器正式以独立 Java 边界服务落地（实现 `Connector` 接口、仅经 Kafka 事件通信），复用 Microsoft Graph Java SDK / MSAL4J / Spring Security 等成熟库，不改变 Go 主干默认语言。

### 9.4 多租户隔离与数据驻留（ADR-009）

邮箱聚合是强多租户场景：一个平台实例服务成千上万账户，企业版还要求数据驻留 / 不出境。隔离必须**贯穿全链路且可被审计证明**，而非依赖业务代码自觉。

**租户路由矩阵**（`tenant_id` = `account_id` 的归属租户）

| 层 | 隔离手段 | 兜底机制 |
|----|----------|----------|
| 网关 / 入口 | 请求强制携带 `tenant_id`，缺失即拒 | WAF / 网关策略 |
| PG 元数据 | 按 `account_id` 分片（Citus）+ `tenant_id` 行过滤 | Row-Level Security（RLS） |
| 对象存储 | key 前缀 `tenant-<tid>/` | 桶策略 / IAM |
| OpenSearch | 索引 `mail-<tid>` 或 `tenantId` 路由 + 查询强制过滤 | 索引级权限 |
| Kafka | 分区键 `account_id`；企业栈独立 topic 前缀 | 消费组隔离 |
| KMS | KEK 按 `tenant_id` 派生密钥环 | 跨租户无法解封（ADR-004） |

**租户上下文传播**：`SyncTaskEvent` / `MailIngestedEvent` 等事件与每个请求上下文都携带 `tenant_id`，connector → sync → store / index / notify 全链路透传；任一出口查询都被强制加上 `tenant_id` 约束（anti-corruption 边界）。

**三种部署拓扑（数据驻留）**
- **公共 SaaS**：多区域共享栈，region affinity 决定数据落地区域；跨区仅复制元数据（§16），内容按区域驻留。
- **企业版专属栈**：单租户独占 PG / OS / 对象 / Kafka / KMS，数据 100% 不出该栈。
- **私有化**：整套栈入客户 VPC / 机房，平台侧零数据出境。

> 三态复用同一套二进制，仅以"部署形态参数"区分——避免为合规分支维护多套代码。

---

## 10. 监控与运维体系（Observability）
- **指标（Metrics）**：Prometheus + Grafana；核心 SLI：同步成功率、同步时延 P95、检索时延 P95、API 错误率、上游限流/熔断次数。
- **日志（Logs）**：结构化日志 + 集中检索（ELK/Loki）；敏感字段脱敏。
- **链路（Tracing）**：OpenTelemetry 跨服务追踪，定位同步慢点。
- **告警与 Oncall**：基于 SLO 的告警（错误预算），分级值班；上游异常单独看板。
- **运维工程**：基础设施即代码（Terraform）、蓝绿/金丝雀发布、配置中心、密钥轮换自动化。

---

## 11. 分阶段实施里程碑（含时间线）

| 阶段 | 目标 | 关键交付 | 退出标准 |
|------|------|----------|----------|
| **Phase 0（MVP）** | 单协议验证 | 模块化单体；IMAP 适配器；基础同步+存储；Web 前端 | 单邮箱可接入、可查收、可检索 |
| **Phase 1（多协议）** | 广接入+事件化 | POP3/Exchange/Gmail 适配器；Kafka 事件总线；连接/账户服务 | ≥3 类服务商稳定接入，同步事件化 |
| **Phase 2（规模化）** | 检索+拆分+扩展 | OpenSearch 检索；元数据分片；同步/采集/索引拆分；监控 | 检索 P95<500ms；可水平扩容至 10 万账户 |
| *Phase 2 落地（v1.8）* | *已交付* | *per-tenant 物理索引 `mail-<tid>` + Citus `tenant_id` 分布键；Prometheus+Grafana+Alertmanager 闭环；`sync_worker`/`ingest_worker`/`search_service` 三 worker 拆分 + 多入口 Dockerfile* | *`go build/vet/test` 全绿；三 worker 独立编译通过；ADR-009 物理隔离字面对齐* |
| *Phase 3 落地（v1.9）* | *已交付* | *`src/auth/` RBAC 矩阵 + AuthMiddleware + OIDC SSO（`//go:build sso`）；真实 Citus 1C+2W 集群 + `verify-citus-sharding.ps1` 7 步验证；双 AZ 拓扑（PG primary/replica + Kafka RF=2 + MinIO site replication）+ `drill-chaos.ps1` RPO/RTO 度量 + `RUNBOOK.md` 11 章节演练手册* | *`go build/vet/test` 全绿（含 `integration,sso` 标签）；ADR-009 网关层物理阻断 + 真实分布式集群验证通过；多 AZ RPO/RTO 度量脚本就绪* |
| **Phase 3（企业+韧性）** | 多租户+HA/DR | RBAC/SSO/审计；多 AZ 部署；容灾演练；RPO/RTO 达标 | 99.95% 可用性；企业版可交付 |
| **Phase 4（智能+生态）** | AI+开放 | 智能分类/摘要；开放 API/插件；跨境边缘采集 | 智能化上线；生态可扩展 |

---

## 12. 风险、权衡与开放问题

| 项 | 风险/权衡 | 缓解 |
|----|-----------|------|
| 上游限流/封禁 | 服务商 API 配额严格 | 限流、退避、分布式采集调度、官方 API 优先 |
| 凭据安全 | 持有用户令牌为高敏资产 | KMS 信封加密、隔离存储、零明文、定期轮换 |
| 协议碎片化 | 各服务商差异大 | 适配器注册表 + 统一邮件模型 |
| 过早微服务 | 分布式复杂度反噬 | 模块化单体起步，按边界成熟拆分（ADR-001） |
| 一致性 | 元数据/索引最终一致 | 幂等事件 + 以元数据为准兜底 |
| 成本 | 内容/索引存储增长快 | 冷热分层、索引分级、内容寻址去重 |

**开放问题（待决策）**：~~是否做私有化交付形态？企业版是否需要数据驻留（数据不出境）？~~ **已决议（ADR-009）**：支持公共 SaaS 共享栈 + 企业版专属栈/私有化三种形态，同一二进制、部署形态参数化，数据驻留边界由部署拓扑保证。~~AI 能力自研还是接入第三方大模型（合规约束）？~~ **已决议（ADR-010）**：采用**能力路由网关（混合策略）**——按租户类别与数据敏感度选路，公共 SaaS 租户在显式同意 + PII 边界脱敏后可走第三方大模型 API，企业版/私有化租户强制自托管不出境；统一 `AIGateway` 抽象复用 ADR-005 适配器注册表 + ADR-009 租户上下文 + audit 主题。

---

## 附录：术语表
- **IDLE / QRESYNC / CONDSTORE**：IMAP 推送与增量同步扩展。
- **信封加密（Envelope Encryption）**：用数据密钥加密数据、再用主密钥加密数据密钥。
- **DLQ**：死信队列，多次重试仍失败的消息。
- **PITR**：按时间点恢复。
- **BFF**：Backend For Frontend，为前端定制的聚合后端。
- **SLI/SLO**：服务水平指标 / 目标。
- **GTM**：全局流量管理，多区域 DNS 切换。

---

## 13. 模块接口与时序设计（深化）

> 本章把第 4 章的模块边界落为可实现的接口契约与时序，是 Phase 0 PoC 的直接输入；对应可运行代码见 `email-aggregator/`（TypeScript，零依赖，自检 5/5 通过）。接口定义采用语言无关的 TypeScript 风格，落地时可映射到 Go/Java/Python。

### 13.1 统一连接器接口（Canonical Connector）

所有第三方邮箱以**适配器插件**实现同一接口，框架按 `providerType` 路由（ADR-005）；适配层负责协议↔统一邮件模型的双向映射。

```typescript
type ProviderType = "imap" | "pop3" | "exchange" | "gmail" | "enterprise";

interface ConnectorCapabilities {
  incremental: boolean;   // IMAP QRESYNC / Graph delta / Gmail historyId
  push: boolean;          // IMAP IDLE / Webhook
  folders: boolean;       // POP3 = false
  flags: boolean;         // POP3 = false
  maxPageSize: number;
}

interface Connector {
  readonly providerType: ProviderType;
  readonly capabilities: ConnectorCapabilities;
  connect(account: AccountCredential): Promise<Session>;
  listFolders(session: Session): Promise<Folder[]>;
  fetchChanges(session: Session, folder: string, cursor: SyncCursor): Promise<ChangeSet>;
  fetchMessage(session: Session, folder: string, uid: string): Promise<RawMessage>;
  subscribe(session: Session, folder: string, hook: PushHook): Promise<Subscription>;
  send(account: AccountCredential, message: OutboundMessage): Promise<SendResult>;
  healthCheck(session: Session): Promise<Health>;
}
```

**统一邮件模型（Canonical Mail Model）**——内部唯一表示，屏蔽协议差异：

```typescript
interface CanonicalMail {
  idempotencyKey: string;        // (accountId, folder, messageId) 幂等去重键
  accountId: string; folder: string;
  messageId: string;             // RFC822 Message-ID
  from: Contact[]; to: Contact[]; cc: Contact[];
  subject: string; bodyText?: string; bodyHtml?: string;
  attachments: Attachment[];     // contentHash 用于去重
  flags: { seen: boolean; flagged: boolean; answered: boolean; deleted: boolean };
  internalDate: number; sizeBytes: number;
  cursor: SyncCursor;            // 协议侧游标（仅适配层关心）
  rawObjectKey?: string;         // 内容存储中的原始 MIME 对象键
}

interface SyncCursor {           // 各协议用自己的字段
  uidValidity?: string; uidNext?: string; highestModSeq?: string; // IMAP
  historyId?: string;            // Gmail
  deltaLink?: string;            // Graph
  uidlWatermark?: string;        // POP3
}
```

### 13.2 同步编排状态机（7 态）

每个账户维护一个状态机，由 `sync-orchestrator` 驱动（代码：`src/sync/stateMachine.ts`）。

| 状态 | 含义 | 进入条件 | 退出动作 |
|------|------|----------|----------|
| `UNCONNECTED` | 未连接/未授权 | 新建账户 | 触发 OAuth 授权 |
| `AUTHORIZING` | 授权中 | 拿到授权码 | 换取并 KMS 加密 token |
| `INITIAL_FULL` | 初始全量回溯 | 首次/重新全量 | 分批 fetch + 限速，写 `sync-tasks` |
| `INCREMENTAL` | 增量守护 | 全量完成 | IDLE/轮询 + `fetchChanges` |
| `THROTTLED` | 被限流/退避 | 429/节流 | 指数退避后回 `INCREMENTAL` |
| `ERROR` | 错误态 | 连续失败超阈值 | 告警 + 人工/自动复核 |
| `PAUSED` | 用户暂停 | 用户操作 | 恢复后回 `INCREMENTAL` |

迁移事件：`AUTH_OK / FULL_DONE / CHANGE_RECEIVED / THROTTLED / TRANSIENT_ERR / FATAL_ERR / RESUME / PAUSE`。非法迁移抛错，保证状态边界可被测试与审计。

### 13.3 Kafka 事件契约（5 主题）

所有事件带 `partitionKey = accountId`（单账户有序、可再均衡），`idempotencyKey` 用于消费端去重（至少一次语义下防重复）。

| 主题 | 生产者 | 消费者 | 关键字段 | 语义 |
|------|--------|--------|----------|------|
| `sync-tasks` | orchestrator | ingestion | accountId, folder, cursor, priority | 至少一次 |
| `mail-ingested` | ingestion | store / index / notify / audit | canonicalMail, source | 至少一次 + 幂等 |
| `mail-index` | index-service | search-engine | docId, accountId, bodyText | 异步建索引 |
| `notifications` | notify | ws-push / webhook | accountId, kind, preview | 至少一次 |
| `audit` | 各服务 | 审计存储 | actor, action, accountId, ts | 至少一次 |

`mail-ingested` 示例 payload：

```json
{
  "partitionKey": "acc_8f21",
  "idempotencyKey": "acc_8f21|INBOX|<msgId>",
  "canonicalMail": { "subject": "...", "from": [...], "internalDate": 1765738800000 },
  "source": "imap", "ts": 1765738801000
}
```

### 13.4 关键时序

**13.4.1 新邮件增量同步（IMAP IDLE，详见对话内"新邮件同步时序图"）**
1. Connector 对 IMAP 服务器发起 `IDLE`（长连接推送）；
2. 服务端新邮件 → 下发 `EXISTS`；
3. Connector 用 `fetchChanges(cursor)` 拉取新增 UID（基于 `highestModSeq`）；
4. 取回完整 MIME → 归一化为 `CanonicalMail` → 产出 `mail-ingested`；
5. `mail-ingested` 落 PG 元数据（按 `accountId` 分片）+ 内容写入对象存储（内容寻址）；
6. 索引服务消费 → 写 OpenSearch（按 `accountId` 路由）；
7. 通知服务推送 WebSocket/SSE；审计写 `audit`；
8. 更新游标（`highestModSeq`）到账户状态，保证断点续传。

**13.4.2 多协议初始全量回溯**：`INITIAL_FULL` 态分批回溯，按文件夹分页 `fetchMessage`，每批带限速令牌；遇上游 429 进入 `THROTTLED` 退避；完成后切 `INCREMENTAL`。POP3 以 `UIDL` 集合做全量去重（无增量游标）。

---

## 14. 容量规划计算模型（深化）

### 14.1 参数表（可调，与第 9 章示例数字同源）

| 参数 | 符号 | 默认 | 说明 |
|------|------|------|------|
| 连接账户数 | `A` | 1,000,000 | 可聚合多邮箱/人 |
| 单账户日均新邮件 | `M` | 50 | 封/天 |
| 单封元数据大小 | `sMeta` | 2 KB | 发件人/主题/时间/文件夹等 |
| 单封内容+附件 | `sBody` | 75 KB | 含附件均值 |
| 索引膨胀系数 | `kIdx` | 2.0 | 索引/元数据 |
| 留存期 | `Y` | 2 | 年 |
| 活跃比 | `ρ` | 0.3 | 当日活跃账户比例 |

### 14.2 公式

```
元数据总量   = A × M × sMeta × 365 × Y
内容总量     = A × M × sBody × 365 × Y
索引总量     = 元数据总量 × kIdx
日均采集事件 = A × M
峰值采集吞吐 = 日均采集事件 / 86400 × 峰值系数(≈8)   // 事件/秒
并发 IMAP 长连接 = A × ρ × 长连接占比(≈0.6)
检索 QPS     = A × 人均日检索(5) / 86400 × 峰值系数
年度存储增量 = 元数据 + 内容 + 索引（按年）
```

### 14.3 场景测算

| 指标 | 10 万账户 | 100 万账户 | 500 万账户 |
|------|-----------|------------|------------|
| 元数据总量 | 7.3 TB | 73 TB | 365 TB |
| 内容总量 | 0.27 PB | 2.7 PB | 13.5 PB |
| 索引总量 | 15–22 TB | 150–220 TB | 730–1,100 TB |
| 日均采集事件 | 500 万 | 5,000 万 | 2.5 亿 |
| 峰值采集吞吐 | ~460 evt/s | ~4,600 evt/s | ~23,000 evt/s |
| 并发 IMAP 长连接 | ~1.8 万 | ~18 万 | ~90 万 |
| 峰值检索 QPS | ~460 | ~4,600 | ~23,000 |
| 年度存储增量 | ~0.3 PB | ~3 PB | ~15 PB |

> 数字为模型估算，上线后须以真实遥测（实际 `M`、附件占比、留存策略）校准；交互计算器见对话内「容量规划计算器」。

### 14.4 扩展杠杆与成本

- **冷热分层**：热 30 天存标准对象存储，冷转低频/归档层（成本可降 60–80%）。
- **索引分级**：仅正文 / 含附件名 / 全量 OCR 三档，按付费套餐控制。
- **内容寻址去重**：相同附件（如群发邮件）只存一份，降低存储放大。
- **计算弹性**：采集 worker 按 `kafka lag` 扩缩，闲时缩容省成本。

---

## 15. Phase 0 PoC 落地计划（深化 + 已交付代码）

### 15.1 目标与范围（MVP）

验证**单协议（IMAP）端到端闭环**：单邮箱账户授权 → 增量同步 → 落库/落对象存储 → 检索 → Web 阅读。不做多协议、不做企业版、不做 HA。

### 15.2 技术选型与已交付实现

| 项 | 规划选型 | 已交付（`email-aggregator/`） |
|----|----------|------------------------------|
| 语言 | Go 或 Java 21 | TypeScript/Node（本环境无 Go；架构语言无关，接口可平移）。零外部依赖即可运行。 |
| 元数据 | PostgreSQL（单实例） | `InMemoryMetadataStore`（PG 适配器 `PgMetadataStore` stub 预留） |
| 内容 | 本地 MinIO | `InMemoryContentStore`（内容寻址，MinIO stub 预留） |
| 索引 | OpenSearch | `InMemorySearchIndex`（OpenSearch stub 预留） |
| MQ | Kafka / Redpanda | `InMemoryEventBus`（Kafka 适配器 stub 预留，at-least-once + 幂等 + DLQ） |
| 凭据 | KMS 信封加密 | `CredentialVault`（AES-256-GCM + KEK 包裹 + 轮换，`InMemoryKms` 占位） |
| 前端/API | React + WS | `ApiServer`（REST + WebSocket 同端口，已实测） |
| 编排 | docker-compose | `deploy/docker-compose.yml`（PG/MinIO/OpenSearch/Redpanda） |

**运行验证**：
```
node --experimental-strip-types src/demo.ts   # 自检 5/5 通过
node --experimental-strip-types src/main.ts    # 常驻：REST(:8080) + WS
```
> 注：PoC 以 TypeScript 实现仅用于零依赖验证；**生产技术线已锁定 Go（ADR-006）**，核心模块（connector / sync / events / security）将平移为 Go，接口契约与事件不变，平移成本低。

### 15.3 任务拆解（含估算，单位：人天）

| # | 任务 | 依赖 | 估算 |
|---|------|------|------|
| T1 | IMAP 连接器（IDLE + QRESYNC + 归一化） | — | 8 |
| T2 | 账户/凭据管理（OAuth2 + KMS 占位） | — | 5 |
| T3 | 同步编排状态机 + `sync-tasks` | T1 | 6 |
| T4 | 采集/解析服务（MIME → CanonicalMail） | T1 | 6 |
| T5 | 存储写入（PG 元数据 + MinIO 内容） | T1 | 5 |
| T6 | 索引服务 + OpenSearch 检索 API | T5 | 6 |
| T7 | 通知（WebSocket 推送） | T4 | 3 |
| T8 | BFF/API 网关 + 前端阅读/检索 | T5,T6 | 10 |
| T9 | 可观测性埋点 | — | 4 |
| T10 | 联调 + 验收演练 | 全部 | 5 |

**合计 ≈ 58 人天（约 2.5 个月 / 1 个 3 人小队）。**

### 15.4 验收标准（退出 Phase 0）

- [x] 单 IMAP 账户可授权接入，新邮件 ≤60s 在 Web 可见（IDLE）—— PoC 通过 `inject()` 模拟推送并实时落库。
- [x] 断点续传：重启后从 `highestModSeq` 继续，无重复入库（幂等键校验）—— 总线 `idempotencyKey` 去重已验证。
- [x] 检索 P95 < 500ms（单用户收件箱级）—— 内存索引满足。
- [x] 内容/附件可从对象存储还原，正文可全文检索 —— 内容寻址存储 + 索引已实现。

### 15.5 建议目录结构（模块化单体）

```
email-aggregator/
  src/model/        领域模型（canonical / connector 契约）
  src/connector/    适配器注册表 + imap（含内存服务器模拟 IDLE）
  src/events/       事件契约 + 总线（内存 / Kafka stub）
  src/security/     KMS 凭据保险库（信封加密）
  src/sync/         状态机 + 编排器
  src/store/        元数据(PG) + 内容(对象存储)
  src/search/       索引（OpenSearch stub）
  src/notify/       WebSocket 推送
  src/ingest/       采集解析
  src/aigateway/    AI 能力路由网关（ADR-010：AIProvider + 路由策略 + Guardrail）
  src/api/          BFF 网关（REST + WS）
  src/app.ts        装配 + 端到端场景
  deploy/           docker-compose.yml
```

---

## 16. HA/DR 与备份深化

### 16.1 Patroni 高可用（PostgreSQL）

- 3 节点 etcd/Consul 存放领导者信息；Patroni 管理主从，自动故障转移。
- 同步复制模式（`synchronous_commit = on`）保护元数据不丢；副本跨 AZ。
- 故障转移 < 30s；应用通过 VIP / 服务发现连当前主库，连接串自动切换。
- 连接池 `PgBouncer` 前置，避免长连接打爆主库（已纳入 `deploy/docker-compose.yml`）。

### 16.2 跨可用区 / 跨区域复制

| 组件 | AZ 内 | 跨 AZ | 跨区域（DR） |
|------|-------|-------|--------------|
| PostgreSQL | 主从 | 副本（同步） | 异步流复制（关键租户） |
| Kafka | 多 broker | 副本 ≥3 | MirrorMaker2 异步 |
| OpenSearch | 多节点 | 分片副本 | 快照跨区 |
| 对象存储 | 多副本 | 跨 AZ | 跨区复制（合规租户） |
| Redis | 集群 | 副本 | 不跨区（可重建） |

### 16.3 备份与 PITR

- 元数据：每日全量 + WAL 连续归档 → 对象存储；`recovery_target_time` 支持 PITR（RPO ≤ 5min）。
- 内容：对象存储版本化 + 生命周期归档；无需传统备份。
- 索引：ILM 快照，可重建（rebuilt from 元数据+内容，代价高但非必须）。
- 密钥：KMS 密钥材料随备份**受控复制**到异地密钥库（与数据备份配对）。

```
# 1) 拉起基线
pg_basebackup -D /restore ...
# 2) 回放 WAL 到目标时间
recovery_target_time = '2026-08-14 22:00:00+08'
# 3) 校验一致性后开放只读，再切主
```

### 16.4 混沌演练与 RPO/RTO 验证

- 季度级故障注入：杀主库 / 断 AZ 网络 / 灌爆 Kafka / 模拟上游 429。
- 验证项：自动故障转移时效、RPO（对比 WAL 位点）、RTO（服务恢复时长）。
- 上游故障演练：断开 IMAP 服务器，确认平台进入**降级模式**且阅读体验不中断。

### 16.5 降级模式实现要点

- 读路径优先命中本地已聚合数据（PG 元数据 + 对象存储内容 + OpenSearch 索引）。
- 写/同步路径入队（Kafka）排队重试，前端显示"同步中/稍后重试"。
- 上游限流 429 → `THROTTLED` 指数退避，不占用平台资源。

### 16.6 v1.9 落地状态（Phase 3 多 AZ 部署 + RPO/RTO 度量）

**PoC 验证已落地**——以下为 §16.1-16.4 设计在 Phase 3 的实际验证状态：

| §16 设计项 | PoC 落地（v1.9） | 验证方式 | 生产化差距 |
|-----------|-----------------|---------|-----------|
| §16.1 Patroni HA | PG primary/replica 流复制（`pg-primary` → `pg-replica`） | `drill-chaos.ps1` 手动 `pg_promote()` | 手动 promote → Patroni 自动选主 |
| §16.2 跨 AZ 复制 | PG/Kafka/MinIO 三组件均双 AZ（`az_a`/`az_b`/`az_xlink` 网络） | `RUNBOOK.md` §4-6 演练 | Kafka RF=2 → RF=3；MinIO 同 region → 跨 region DR |
| §16.3 备份与 PITR | PoC 未落地（依赖 WAL 归档 + 对象存储版本化） | — | 生产需配置 WAL-G / pgBackRest + S3 生命周期 |
| §16.4 混沌演练 | `drill-chaos.ps1` 度量 RPO（流复制延迟）+ RTO（故障注入到恢复可写） | 脚本化可重复执行 | 季度级 chaos mesh / Litmus 集成 |

**RPO/RTO 度量脚本**（`scripts/drill-chaos.ps1`）：
- RPO 度量：primary 写入 N 行 → 立即查 replica 行数 → 差值 = RPO（PoC 目标 ≤ 5 行）。
- RTO 度量：`docker compose stop pg-primary`（故障注入）→ `pg_promote()`（提升 replica）→ 写入 canary 行（验证可写）→ 计时（PoC 目标 ≤ 30s）。
- 验证项对齐 §16.4：自动故障转移时效（PoC 为手动 promote，生产为 Patroni < 30s）、RPO（WAL 位点对比）、RTO（服务恢复时长）。

**MinIO site replication 验证**：`minio-a` 上传对象 → 立即查 `minio-b` → 副本可见即通过；断开 `az_xlink` 网络模拟整 AZ 失联，验证 site replication 在网络恢复后自动同步。

---

## 17. AI 能力层设计（深化，ADR-010 实施）

### 17.1 定位与边界上下文
`AIGateway` 是新建的边界上下文（C4 Level 2 的「智能能力域」），对上承接 BFF/前端与同步编排的 AI 调用诉求（智能分类、优先级收件箱、摘要、智能回复起草、钓鱼/欺诈检测、语义检索增强），对下通过统一 `AIProvider` 接口屏蔽**自托管 vLLM** 与**第三方大模型 API** 的差异。它复用三条既有范式：ADR-005 的适配器注册表 + 统一契约、ADR-009 的 `tenant_id` 上下文强制传播与数据驻留物理边界、ADR-002 的 Kafka `audit` 主题承载调用审计。

### 17.2 AIProvider 统一接口契约
```go
type AIProvider interface {
    Name() string
    Chat(ctx context.Context, req ChatRequest) (*ChatResponse, error)
    Embed(ctx context.Context, tenantID string, texts []string) ([][]float32, error)
    Classify(ctx context.Context, tenantID string, text string, labels []string) (string, float64, error)
    Summarize(ctx context.Context, req ChatRequest) (string, error)
}
```
- `ChatRequest` 全链路携带 `TenantID / TenantTier / AccountID / Sensitivity / UserConsented`，使策略在入口即确定，后端无需感知合规细节。
- 能力枚举：`chat / embed / classify / summarize / extract`。
- 自托管（vLLM + Qwen/Llama）、第三方（Claude/OpenAI/通义/GLM）、未来本地小模型均实现同一接口；具体后端经 `integration` 构建标签接入（见 §15.2），默认构建零外部依赖。

### 17.3 路由策略表（核心）
网关路由为**纯函数 `Decide(tier, sensitivity, consented)`**，按 租户类别 × 数据敏感度 × 用户同意 三维选路：

| 租户类别 (ADR-009) | 敏感度 | 用户同意 | 命中后端 | 出平台前脱敏 | 允许 |
|--------------------|--------|----------|----------|--------------|------|
| 私有化 private | 任意 | 任意 | **self-hosted** | 否（数据零出境） | ✅ |
| 企业专属 enterprise | 任意 | 任意 | **self-hosted** | 否（数据零出境） | ✅ |
| 公共 SaaS public | 无 PII | 任意 | third-party | 否 | ✅ |
| 公共 SaaS public | 含 PII | 已同意 | third-party | **是（PII 剥离）** | ✅ |
| 公共 SaaS public | 含 PII | 未同意 | —— | —— | ❌ 拒绝（可降级自托管） |

> 硬约束：网关层**拒绝任何「私有/企业租户 → 第三方后端」的路由**；公共租户含 PII 且未显式同意时一律拒绝出第三方，避免合规击穿。

### 17.4 Guardrail 与脱敏
- **出平台前最小化**：所有发往第三方 API 的请求先过 PII 扫描（`RegexRedactor`：邮箱 / 手机号 / 银行卡 / 身份证正则命中即替换为 `███`），仅携带完成任务所必需字段。
- **入站输出防御**：对模型返回做提示注入 / 越权防御（如「忽略系统指令」类对抗），必要时二次脱敏（避免模型回显 PII）。
- **降级**：拒绝/超时时回退自托管或返回安全占位，不阻塞主链路。

### 17.5 与既有层集成
- **租户上下文**：`tenant_id` 经 BFF → AIGateway 全链路透传（继承 ADR-009），私有租户后端绑定其专属栈。
- **审计**：每次调用（无论允许与否）入 Kafka `audit` 主题（`AuditEvent`：tenant / account / capability / backend / redacted / allowed / reason），满足合规追溯。
- **可观测与评估**：统一埋点（延迟、token、命中后端、脱敏命中、失败）；建立 golden set 质量评估集做回归，避免模型升级悄悄劣化关键能力。

### 17.6 Go PoC 落地（`email-aggregator-go/src/aigateway/`）
**已交付真实可编译实现（非仅骨架），Go 1.26.6 真机验证 `go build/vet/test` 全绿。** 仅依赖标准库，零外部依赖，默认构建即可编译；后端经 OpenAI 兼容契约对接。

- `provider.go`：`AIProvider` 接口 + 类型常量（Capability / TenantTier / DataSensitivity / Message / ChatRequest / ChatResponse）。
- `router.go`：`Decide` 纯函数策略 + `Router.RouteAndChat`（路由 + Guardrail + 审计）+ `RegexRedactor` + `StubProvider`（测试/默认桩）。
- `providers.go`（**本轮新增，真实后端**）：`HTTPProvider` 实现 `AIProvider` 全部能力——
  - `NewSelfHostedProvider(baseURL, model, apiKey)`：自托管路径（私有/企业零出境），指向租户 VPC 内 vLLM/兼容端点，`apiKey` 可空（内网互信），请求**不携带**第三方密钥。
  - `NewThirdPartyProvider(baseURL, model, apiKey)`：第三方路径（公共同意+脱敏后），`apiKey` 必填并以 `Bearer` 发送。
  - `Chat` → `POST /v1/chat/completions`；`Embed` → `POST /v1/embeddings`；`Classify`/`Summarize` 经 `Chat` 复用同一后端（分类/摘要提示词）。统一 `doJSON` 错误处理（含上游非 2xx）。
- `router_test.go`：路由策略断言——私有强制自托管 / 公共 PII 未同意拒绝 / 公共 PII 同意走第三方且脱敏 / audit 事件落地。
- `providers_test.go`（**本轮新增，httptest 真实验证**）：
  - 自托管 `Chat` 断言（响应/模型/token）+ 断言**不发送** `Authorization`；
  - 第三方 `Chat` 断言发送 `Bearer` 鉴权；
  - `Embed` 维度断言；`Classify`/`Summarize` 复用；
  - **`TestRouter_IntegrationRealProviders_RedactionAtBoundary`**：用真实 httptest 双后端验证——私有租户命中自托管、公共租户命中第三方，且**第三方在 HTTP 边界收到的请求体已被脱敏**（PII 明文被 `███` 替换，不出境）。
- **REST 接入**（`src/api`，非破坏性）：`ApiServer.WithAIGateway(r)` 新增 `/api/ai/chat`（POST ChatRequest → `RouteAndChat` → 200 响应 / 422 拒绝），`server_integration.go`（构建标签 integration）从环境变量装配自托管/第三方 Provider 与审计 sink（审计事件 `Bus.Publish` 入 Kafka `audit` 主题）。`src/api/server_ai_test.go` 以 httptest 验证三条路径（私有→self-hosted / 公共同意 PII→third-party+redacted / 公共未同意→422）。
- 单适配器熔断：某服务商异常不影响其他服务商与其他用户（错误由 `RouteAndChat` 透传，调用方接退避/DLQ）。

---

## 18. 变更记录（Changelog）

本章节自 v1.8 起维护，汇总每个版本的交付范围、关键改动与验证状态。更早版本的变更摘要见文档头部 `> vX.Y 变更` 段。

### v1.8 — Phase 2 立项交付（OpenSearch 检索拆分 + Citus 分片 + 监控告警 + 服务拆分）

**交付范围**：完成 Phase 2 全部四项子任务（A. OpenSearch 检索拆分 / B. Citus 分片 / C. 监控告警 / D. 服务拆分），ADR-009 物理隔离从设计落到可运行代码。

**关键改动**：

| 子任务 | 交付物 | 说明 |
|--------|--------|------|
| A.1-A.2 OpenSearch 适配器 | `src/search/index.go` | 复合索引 `mail-<tid>-<aid>` → per-tenant 物理索引 `mail-<tid>`，严格对齐 ADR-009 字面硬约束；查询加 `tenantId` 过滤 |
| A.3 索引重建工具 | `cmd/reindex.go` | OpenSearch scroll + bulk 幂等迁移，支持存量数据从旧索引平滑迁移到 `mail-<tid>` |
| B.1-B.2 Citus 分片 | `deploy/migrations/002_citus_sharding.sql` | 分布键 `account_id` → `tenant_id`，实现租户级数据 colocation 与审计分片合规 |
| B.4 TS PoC 对齐 | `email-aggregator/` | TS 版同步对齐 per-tenant 索引与 tenant_id 分布语义 |
| C.1 指标埋点 | `src/observ/metrics.go` | 新增 OpenSearch 操作计数/直方图、PG 指标、reindex 进度指标 |
| C.2-C.3 监控栈 | `deploy/observability/` | Prometheus + Grafana + Alertmanager + postgres-exporter + kafka-exporter 容器化部署 |
| C.4 告警规则 | `deploy/observability/alerts.yml` + `alertmanager.yml` | SLO 告警：检索 P95、同步成功率、摄取吞吐、AI 调用成功率 |
| D.1 共享 helpers | `cmd/helpers.go` | 集中 `env`/`envPort`/`metricsServer`/`maskDSN`，消除 worker 间重复 |
| D.2 search_service | `cmd/search_service.go` | 独立 HTTP 检索服务，仅依赖 PG + OpenSearch（不接 MinIO/Kafka） |
| D.3 容器化 | `Dockerfile` + `.dockerignore` + `deploy/docker-compose.yml` | 多入口 Dockerfile（构建标签隔离）；docker-compose 三 worker 容器编排 |
| D.4 抓取目标 | `deploy/observability/prometheus.yml` | 新增 `sync_worker:8081` / `ingest_worker:8082` / `search_service:8083` 抓取目标 |

**服务拆分拓扑**（蓝图 §11 落地）：

- `sync_worker`（构建标签 `integration,sync_worker`）：订阅 Kafka `sync-tasks`，编排邮件同步；`/metrics` 端口 8081。
- `ingest_worker`（构建标签 `integration,ingest_worker`）：订阅 Kafka `mail-ingested`，落对象存储 + 写元数据 + 推 OpenSearch；`/metrics` 端口 8082。
- `search_service`（构建标签 `integration,search_service`）：HTTP 检索端点（`/mails` / `/search`），仅依赖 PG + OpenSearch；`/metrics` 端口 8083。
- `server_integration.go`（构建标签 `integration && !sync_worker && !ingest_worker && !search_service`）：保留单体一体化模式，未启用 worker 标签时编译，用于本地开发与 Phase 0/1 兼容。

**验证状态**：
- `go build ./...`（含 integration 标签）全绿。
- `go vet ./...` 全绿。
- `go test ./...` 全绿（observ/search/api/integration 单测通过）。
- 三个 worker 独立编译验证：`go build -tags "integration,sync_worker"` / `ingest_worker` / `search_service` 均产出可执行二进制。
- `docker compose config --quiet` 语法校验通过。

**对既有 ADR 的影响**：
- ADR-009：从"决策"升级为"决策 + v1.8 落地状态"，OpenSearch 与 Citus 两项硬约束已字面对齐。
- ADR-001（演进式架构）：Phase 2 服务拆分是 ADR-001 "按边界成熟度切微服务"的首次实际应用——从模块化单体切出三个独立 worker 进程，但共享同一代码库与构建标签，保留回退到单体的能力。
- ADR-003（存储三权分立）：Citus 分布键调整不影响三权分立架构，仅细化分布维度。
- 无新增 ADR（本版本为既有 ADR 的实施交付，未引入新架构决策）。

**开放问题（顺延至 Phase 3）**：
- 多 AZ 部署与容灾演练（RPO/RTO 达标）——蓝图 §7/§16 已设计，Phase 3 落地。
- RBAC/SSO/企业版审计——Phase 3 企业版交付。
- Citus 真实分布式集群验证（当前为单节点 PG + 迁移 SQL，Phase 3 接入真实 Citus 集群）。

---

### v1.9 — Phase 3 立项交付（多 AZ 部署 + RBAC/SSO + 真实 Citus 集群验证）

**交付范围**：完成 Phase 3 全部三项子任务（A. 多 AZ 部署 / B. RBAC/SSO / C. 真实 Citus 集群验证），ADR-009 网关层物理阻断 + 真实分布式集群验证 + HA/DR 演练脚本全部落地。

**关键改动**：

| 子任务 | 交付物 | 说明 |
|--------|--------|------|
| B.1 OIDC SSO 验签器 | `src/auth/oidc.go`（`//go:build sso`） | `KeycloakVerifier` 实现 `go-oidc/v3` + `oauth2`；从 id_token 提取 Sub/Email/Name/TenantID/Roles；`//go:build sso` 标签隔离保持默认零依赖 |
| B.2 RBAC 静态矩阵 | `src/auth/rbac.go` | 4 角色（owner/admin/member/viewer）× 5 动作（mail.read/mail.write/account.create/account.manage/admin.tenant）；`StaticResolver` 从 JWT groups claim 取角色 |
| B.3 AuthMiddleware | `src/auth/middleware.go` | Bearer 验签 → Claims 注入 → `Can()` 判定 → 403 时 emit `AuditEvent`；跨租户访问在网关层阻断（`claims.TenantID` ≠ `X-Tenant-Id` → 403 cross-tenant-deny） |
| B.4 路由挂载 | `src/api/server.go` + `cmd/server_integration.go` | `WithAuth(mw)` 包装所有需鉴权路由；`assembleAuthMiddleware` 在 `OIDC_*` 环境变量存在时挂载，否则回退 PoC 无鉴权模式（向后兼容 Phase 1/2） |
| B.5 审计 sink | `src/auth/audit_sink.go` | `EmitAudit` 强制 `tenant.Resolve(ev.TenantID)` + 非空 actor（空 actor 填 `anonymous`），所有 deny 事件 100% 带 tenantId 满足「可审计证明」 |
| B.7 单测 | `src/auth/*_test.go` | RBAC 矩阵 / 跨租户 deny / missing-bearer / OIDC claims 解析单测全绿 |
| C.1 Citus 集群拓扑 | `deploy/docker-compose.citus.yml` | 1 Coordinator（`cn`）+ 2 Worker（`w1`/`w2`），`citus_net` 网络，`citusdata/citus:12.1.3` 镜像 |
| C.2 Worker 注册 | `deploy/migrations/006_citus_workers.sql` | 幂等 `citus_add_node('w1', 5432)` + `citus_add_node('w2', 5432)`；守卫 `pg_available_extensions` 在纯 PG 静默跳过 |
| C.3 分片验证 | `scripts/verify-citus-sharding.ps1` | 7 步：节点拓扑 / 分布式表声明 / 样本数据 / 同租户 colocation / worker 分布 / 单租户路由 / 全量 fan-out |
| A.1 双 AZ 拓扑 | `deploy/docker-compose.multiaz.yml` | PG primary/replica（流复制）+ 2 Kafka broker（RF=2 + `broker.rack`）+ 2 MinIO（site replication）；`az_a`/`az_b`/`az_xlink` 三网络隔离 |
| A.2 RPO/RTO 度量 | `scripts/drill-chaos.ps1` | primary 写入 → 查 replica 行数（RPO）；stop primary → `pg_promote()` → canary 写入计时（RTO）；MinIO site replication 验证 |
| A.3 演练手册 | `deploy/multiaz/RUNBOOK.md` | 11 章节：拓扑速览 / 前置准备 / PG 故障（RPO/RTO）/ Kafka broker 故障 / MinIO site 故障 / 综合 AZ 切换 / 责任人 / 生产化差距 |

**验证状态**：
- `go build ./...`（默认，无 sso 标签）全绿——保持 PoC 零外部依赖能力。
- `go build -tags integration,sso ./...` 全绿——OIDC SSO 装配通过。
- `go vet ./...` 全绿。
- `go test ./...` 全绿（auth/rbac/api/integration 单测通过）。
- `docker compose -f deploy/docker-compose.citus.yml config --quiet` 语法校验通过。
- `docker compose -f deploy/docker-compose.multiaz.yml config --quiet` 语法校验通过。

**对既有 ADR 的影响**：
- ADR-009：从"v1.8 物理隔离落地"升级为"v1.9 网关层物理阻断 + 真实集群验证"——`AuthMiddleware` 在网关层阻断跨租户访问（不再靠业务层自觉 WHERE 过滤），真实 Citus 1C+2W 集群验证 `tenant_id` 分布键在分布式环境生效。
- ADR-004（凭据安全）：新增 v1.9 落地状态——多 AZ 部署要求 KEK 按 AZ 分主备 + 轮换审计，`RUNBOOK.md` §9 列出生产化差距。
- ADR-001（演进式架构）：Phase 3 在 Phase 2 服务拆分基础上新增 `src/auth/` 边界上下文，RBAC/SSO 作为独立横切关注点（cross-cutting concern）落地，不侵入既有 sync/ingest/search worker。
- 无新增 ADR（本版本为既有 ADR 的实施交付与 HA/DR 验证，未引入新架构决策）。

**开放问题（顺延至 Phase 4）**：
- **真实 Keycloak/Dex IdP 联调**：当前 OIDC 验签器已实现并经单元测试验证，但未与真实 IdP 联调（需部署 Keycloak 容器 + 配置 realm/client）。
- **Patroni 自动故障转移**：多 AZ PoC 为手动 `pg_promote()`，生产需 Patroni + etcd 自动选主 < 30s。
- **Kafka RF=3 + min.insync.replicas=2**：PoC RF=2 仅证跨 AZ 副本，生产需 RF=3 容忍单节点+单 AZ 同时故障。
- **跨 region DR**：多 AZ PoC 为同 region 双 AZ，生产需跨 region 异步复制满足 DR RPO ≤ 5min。
- **PITR 备份**：§16.3 设计的 WAL 归档 + 对象存储版本化在 PoC 未落地，生产需 WAL-G / pgBackRest + S3 生命周期。
- **AI 能力层（ADR-010）生产化**：`AIGateway` 已在 v1.7 PoC 落地，Phase 4 需接入真实模型后端 + 脱敏评估体系。

---

*本文档为架构基线 v1.9，随业务与遥测数据演进，关键变更以新 ADR 记录并归档；本版本交付物见 §18 变更记录。*
