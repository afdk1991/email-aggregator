# 项目 003 · 邮箱聚合平台 — 文档通读纪要

- **通读日期**：2026-09-16
- **范围**：全项目 39 份 Markdown 文档（530.4 KB）+ 开发基础设施文件（CI / 脚本 / 编排 / 云函数）
- **方法**：四路并行通读（架构契约 / 验证审计 / 部署运维设计 / 开发基础设施），
  产出明细见 `.workbuddy/analysis/通读-*.md`（合计 100 KB）；**关键结论由本人二次复核**
- **可信度标记**：✅ 已复核（读源码/跑命令验证） · ⚠️ 待复核（文档或通读结论，未独立验证）

> 本纪要与 `项目通读报告-20260915.md` 互补：那份偏「项目讲什么」，本份偏「**我们确切知道什么、哪些说法是错的**」。

---

## 一、项目全景（实测数据，非文档引用）

### 1.1 代码规模

| 实现 | 角色 | 文件 | 代码行数 | 测试行数 | 测试占比 |
|------|------|------|----------|----------|----------|
| `email-aggregator-go/` | **生产主干** | 98 个 `.go` / 20 目录 | **16,120** | **4,199**（33 文件） | **26.0%** |
| `email-aggregator/` | TS PoC / 契约对照 | 30 个 `.ts` | 4,616 | 含在 `tests/`（19 项） | — |
| `email-aggregator-web/` | React + TS + Vite 前端 | 17 个 `.ts(x)` + 2 `.css` | 1,916（tsx/ts） | 无单测 | — |

前端细目：`App.tsx` 772 行 · `styles.css` **1,893 行** · `MailList.tsx` 204 · `MailDetail.tsx` 168 ·
`client.ts` 187 · `Icon.tsx` 126 · 其余组件均 < 100 行。

### 1.2 Go 主干模块分布（`src/` 下 82 文件 / 9,986 行源码 + 4,148 行测试）

| 模块 | 文件 | 源码 | 测试 | 备注 |
|------|------|------|------|------|
| `connector` | 22 | **3,925** | 1,418 | **复杂度中心**：IMAP/POP3/Gmail/Graph/EWS 适配 + RFC2047 解码 |
| `integration` | 4 | 1,054 | 347 | 真实中间件接线（build tag 隔离） |
| `api` | 7 | 927 | 399 | REST + WS + SSO 端点 |
| `aigateway` | 7 | 609 | 302 | ADR-010 |
| `security` | 6 | 510 | 303 | KEK / 信封加密 / PKCE |
| `auth` | 6 | 487 | 258 | RBAC + OIDC 验签 |
| `store` / `observ` / `search` / `notify` / `syncsvc` | 19 | ~1,800 | ~945 | 存储 / 观测 / 检索 / 通知 / 同步编排 |
| `events` / `ingest` / `model` / `tenant` | 11 | ~770 | 399 | |
| **`sync`** | **0** | **0** | **0** | 🟢 **空目录**（逻辑已迁至 `syncsvc`），可删 |

⚠️ **测试覆盖不均衡**：`model`（165 行）与 `tenant`（55 行）**零测试**；`ingest` 测试行数（179）反超源码（147）。

### 1.3 空目录（3 个，均为残留）

```
email-aggregator-go/src/sync/      ← 见上表
email-aggregator-web/src/pages/    ← 无路由设计，纯单页应用，该目录从未使用
.tmp_q/                            ← 根目录临时目录
```

---

## 二、系统能力清单（✅ 从源码提取，非文档转述）

### 2.1 HTTP 端点与 RBAC 映射（`src/api/server.go:116-154`）

| 端点 | 方法 | RBAC 动作 | 注册条件 |
|------|------|-----------|----------|
| `/api/health` | GET | **无鉴权** | 恒注册 |
| `/api/mails` · `/api/mails/{id}` · `/api/search` | GET | `mail.read` | 恒注册 |
| `/api/mails/{id}/read` | POST | `mail.write` | 恒注册 |
| `/api/accounts` | GET/POST/DELETE | `account.create` | 恒注册 |
| `/api/accounts/{id}` · `/credentials` · `/sync` | — | `account.manage` | 恒注册 |
| `/api/ai/chat` | POST | 无（在鉴权 Wrap 内） | `s.gw != nil` |
| `/ws` | WS | 无 | `s.wsHub != nil`；需 `?accountId=` |
| `/api/metrics` | GET | 无 | `s.observ != nil` |
| `/api/auth/login` · `/callback` | GET | **须在鉴权 Wrap 前** | `s.sso != nil` |
| `/` | GET | 无 | `webui` 构建（内嵌 SPA） |
| **`/api/demo/push`** | GET/POST | **🔴 无鉴权、无条件** | **恒注册（见 §6.1）** |

### 2.2 部署形态

| 形态 | 入口 | 端口 | 状态 |
|------|------|------|------|
| EdgeOne Makers 云上 | `deploy/edgeone-app/` + 云函数 | 平台托管 | ✅ **线上运行**，`demo:false` |
| Go 单二进制（内嵌 SPA） | `go build -tags webui` | 默认 8080 | ✅ 已验证 |
| Go 集成栈（真实中间件） | `build -tags integration` + Docker | 8082（`start-local.ps1`） | 🔴 **本机无 Docker，未验证** |
| 多可用区 / Citus | `deploy/docker-compose.multiaz.yml` 等 | — | 🔴 编译级通过，运行时未验证 |
| Render | `render.yaml` · `deploy/render-go-native.yaml` | — | ⚠️ 未授权部署 |

### 2.3 构建标签（✅ 实测分布）

`integration`(11 文件) > `sync_worker`/`ingest_worker`/`search_service`(各 3) > `sso`(2) · `webui`(2) · `reindex`(1)，
另有大量 `!xxx` 负向标签。**默认 `go build ./...` 零外部依赖**这一硬约束在 12 种标签组合下均成立。

---

## 三、命令速查（✅ 关键项已实测）

| 用途 | 命令 | 目录 | 前提 |
|------|------|------|------|
| 本地一键启动 | `powershell -ExecutionPolicy Bypass -File .\deploy\start-local.ps1` | `email-aggregator-go/` | Docker（中间件） |
| 本地停止 | `powershell ... -File .\deploy\stop-local.ps1` | 同上 | — |
| Go 默认构建 | `go build ./...` | `email-aggregator-go/` | Go 1.26 |
| Go 全量构建（含前端） | `powershell -File .\build.ps1` / `bash build.sh` | `email-aggregator-go/` | Node + Go，**会同步 webroot/dist** |
| 前端构建 | `npm run build`（= `tsc --noEmit && vite build`） | `email-aggregator-web/` | — |
| 本地全量 CI | `powershell -File .\scripts\ci.ps1` | 项目根 | `-Integration` 需 Docker |
| 线上部署 | `edgeone makers deploy -n email-aggregator-p003 --json` | `deploy/edgeone-app/` | EdgeOne CLI 已登录 |
| 云函数冒烟 | `node smoke.mjs` / `node smoke.mjs --prod` | 同上 | — |
| 线上全接口验证 | `bash shots/live-verify.sh "<带 eo_token 的 URL>"` | 项目根 | 新鲜 token（30 分钟寿命） |
| TS PoC 测试 | `node --test --experimental-strip-types tests/*.test.ts` | `email-aggregator/` | **Node ≥ 22.6** |

⚠️ **本机注意**：`8080` 端口由项目 002 占用，Go 后端需 `HTTP_PORT=8090` 启动，前端 `VITE_API_PORT=8090` 对接。

---

## 四、环境变量契约（✅ 已从代码核实读法）

| 变量 | 用途 | 默认 | 关键说明 |
|------|------|------|----------|
| `HTTP_PORT` | 服务监听端口 | `8080` | ✅ **确实接线**（`server_integration.go:93`、`cmd/server/main.go:131`）。⚠️ **只认 `HTTP_PORT`，设 `PORT` 无效** |
| `KAFKA_BROKERS` | broker 列表 | `kafka:9092` | 用于客户端元数据 |
| `KAFKA_DIAL_ADDR` | **实际拨号地址覆盖** | 空 | 🔴 **与上者并存、语义不同，非命名冲突**。宿主机场景必设 `127.0.0.1:9092`——Kafka advertised 通告容器内网名 `deploy-kafka-1`，宿主机解析不了；**缺它则 mail-ingested 事件生产失败 → 邮件永不落库，表现为「正文一直为空」** |
| `KMS_KEYRING_FILE` | 信封加密 KEK 持久化路径 | 空 | 🔴 缺省时退回**内存随机 KEK** → 进程重启后无法拆封历史凭据（`cipher: message authentication failed`） |
| `OPENSEARCH_ADDR` | 检索后端 | `https://opensearch:9200` | — |
| `OIDC_ISSUER` / `OIDC_CLIENT_ID` / `OIDC_CLIENT_SECRET` / `OIDC_REDIRECT_URI` | SSO | — | `OIDC_ISSUER` 为空时 SSORouter 返回 nil，不注册路由 |
| `ENABLE_DEMO` | **EdgeOne 云函数**演示开关 | **`true`** | 云函数 `:451` `demoEnabled = !(env.ENABLE_DEMO === 'false')`。线上实测 `demo:false` → 平台侧已设 `=false` |
| `VITE_DEMO_MODE` | **前端构建期**演示开关 | **`true`**（见 §6.2） | `vite.config.ts:17` `env.VITE_DEMO_MODE !== 'false'` |
| `VITE_API_PORT` | 开发代理目标端口 | `8090` | — |

---

## 五、历史踩坑精要（最高价值部分）

细节见 `.workbuddy/analysis/通读-验证与审计.md` §二，此处只列**改变了设计**的几条：

| # | 现象 | 根因 | 修复 |
|---|------|------|------|
| 1 | 删除账户「不生效」，刷新又回来 | Serverless 多实例**各自内存态缓存**，删除只作用于单实例 | 改单文档 `state.json`，每请求强一致读取（`getStore({consistency:'strong'})`） |
| 2 | 真实邮箱「看不到邮件内容」 | Kafka advertised 地址不可解析 → 事件生产失败 → 邮件未落库 | 引入 `KAFKA_DIAL_ADDR` 强制回环拨号 |
| 3 | 邮箱正文为空（全链路） | IMAP 只取信封未拉 BODY + 正文解析不完整 | 补 BODY 拉取 + 解析增强 + 存量回填 |
| 4 | TLS 握手失败（139 邮箱） | RSA 套件协商问题 | 显式配置套件 |
| 5 | 主题乱码 | 未解码 RFC2047 | 补 `=?UTF-8?B?...?=` 解码 |
| 6 | 凭据重启后失效 | KEK 为内存随机值 | `KMS_KEYRING_FILE` 落盘持久化 |
| 7 | 部署后仍有演示内容 | 构建期常量注入未生效 + CSS 未同步（只同步了 JS） | vite `define` 注入 `__DEMO_MODE__`；**产物同步必须双文件 SHA-256 校验** |
| 8 | 吸顶头部在部分浏览器变透明 | `color-mix()` 支持率 ~93%，不支持则整条声明丢弃 | 不透明兜底 + `@supports` 渐进增强 |
| 9 | 虚拟列表滚动错位 | `ROW_HEIGHT` 常量与 CSS 实际高度不同步 | 两处必须同步（现均 76px） |

**环境事故**：2026-09-03 Ubuntu WSL 磁盘丢失，中间件迁至 Docker Desktop，`start-local.ps1` 改为双引擎探测。

---

## 六、风险与未闭合项（分级）

### 6.1 🔴 Go 主干 `/api/demo/push` 无任何防护（✅ 已读源码确认）

```go
// server.go:144 —— 不在任何 if 块内，无条件注册，未包 s.require()
mux.HandleFunc("/api/demo/push", s.handleDemoPush)
```

`handleDemoPush`（`:616`）内部**没有 demo 模式判断、没有鉴权**，且：
- `accountId` 缺省时**默认写入 `acc_demo`**
- 硬编码中文演示邮件正文，标题「实时推送演示邮件」
- 直接 `metadata.UpsertMail` + `search.Index` + `notifier.Publish`（触发 WS 推送）

**影响**：Go 主干自托管部署（含 `webui` 单二进制）中，任何未鉴权请求即可**注入伪造邮件并推送给在线用户**。

**对照**：EdgeOne 云函数版**已做**此防护（`ENABLE_DEMO=false` 时该路由返回 404）。**Go 版与云函数版能力不对等**。
**建议**：给该路由加 demo 开关（复用 `__DEMO_MODE__` 同源配置），或整体下线该端点。

### 6.2 🔴 前端演示模式**默认开启**，而开关文件未入库（✅ 已读源码确认）

```ts
// vite.config.ts:17
const demoMode = env.VITE_DEMO_MODE !== 'false'   // ← 缺失时 undefined !== 'false' → true
```

`email-aggregator-web/.env.production`（内容仅 `VITE_DEMO_MODE=false`）**处于 untracked 状态**。

**影响**：全新克隆仓库执行 `npm run build` → **产出带演示模式的生产包**（预置种子数据、显示「模拟收信」入口、AI 伪造回复）。
这与 9-15「演示数据彻底清除」的成果直接冲突，是**该问题的制度性根因**。

**建议**：`.env.production` 只含非敏感构建开关，**应直接入库**。

### 6.3 🔴 `.gitignore` 规则漏掉 `.env.*`（✅ 已实测）

```
.gitignore:31-33
.env
**/.env
!**/.env.example        ← 只精确匹配名为 .env 的文件
```

`git check-ignore` 实测：以下两个文件**均未被忽略**——

| 文件 | 含敏感键（仅列键名） |
|------|---------------------|
| `email-aggregator-go/deploy/.env.keycloak-prod` | `KC_ADMIN_PASSWORD` · `KC_DB_PASSWORD` · `OIDC_CLIENT_SECRET` |
| `email-aggregator-web/.env.production` | `VITE_DEMO_MODE`（非敏感） |

**影响**：仓库为公开的 `github.com/afdk1991/email-aggregator`。任何一次 `git add .` 都会把 **Keycloak 管理员密码与 OIDC 客户端密钥**提交上去。
**建议**：加 `**/.env.*` 并将 `.env.production` 白名单化；已暴露的密钥**建议轮换**。

### 6.4 🔴 「半提交」会让仓库不可编译（✅ 已验证）

- `email-aggregator-go/src/api/auth_handler.go`（`SSORouter`，ADR-009 的 BFF 端点）**从未被提交**（`git log` 为空）
- 而**同批待提交**的 `cmd/oidc_assemble_nosso.go`（状态 `M`）当前版本已引用 `api.SSORouter`

**HEAD 状态本身可编译**（旧版 `oidc_assemble_nosso.go` 未引用它）——所以问题不在"现在克隆编译不过"，而在于：
**只要用 `git commit -am` 提交已跟踪文件的修改、却漏掉未跟踪的新文件，仓库立刻不可编译。**

**建议**：提交时用 `git add -A` 并先跑一次 `go build ./...` 验证。

### 6.5 🔴 EdgeOne 部署链路未入库（⚠️ 通读结论，路径已抽查确认存在）

```
?? .dockerignore
?? .github/workflows/deploy-edgeone.yml   ← CI 工作流本身
?? deploy/                                 ← 整个部署目录（含 deploy.ps1 / smoke.mjs / 云函数）
?? render.yaml
```

**影响**：全新克隆后 `deploy-edgeone.yml` 引用的路径全部不存在 → **自动部署 CI 必然失败**。

### 6.6 🟡 `webroot/dist` 同步机制（**本节更正了 9-16 早前的错误结论**）

早前判断「`webroot/dist` 无任何脚本负责同步」**不成立**。复核结果：

| 脚本 | 位置 | 同步逻辑 |
|------|------|----------|
| `build.ps1` | `:36-38` | ✅ `Remove-Item webroot/dist` + `Copy-Item $WEB/dist webroot/dist` |
| `build.sh` | `:46-48` | ✅ 同上 |

**真实成因**：同步**只在全量构建脚本里**（该脚本流程为 [1/4] 前端构建 → [2/4] 同步 → [3/4] 交叉编译 → [4/4] 输出 `release/`）。
直接 `go build -tags webui` 做快速验证时**不经过同步步骤**，旧文件照样编译通过 → 静默漂移。

**建议**：加 `sync-web.ps1`（只同步 + SHA-256 校验），或构建前置预检，把「静默漂移」变为「显式失败」。

### 6.7 🟡 `INTEGRATION.md` 陈述与代码不符（✅ 已核实为文档过时）

`INTEGRATION.md:170` 写道：*「监听端口当前在 `cmd/server_integration.go` 硬编码为 `8080`（无 `HTTP_PORT` 读取）」*

**实际**：`server_integration.go:93` 与 `cmd/server/main.go:131` 均已 `envPort("HTTP_PORT", 8080)`。
另外该行还把 Kafka 配置的权威位置指向了代码里已变化的位置。**该行需更新。**

### 6.8 🟡 其他

| 项 | 说明 |
|----|------|
| 集成栈运行时未验证 | 本机无 Docker。PG/Citus · MinIO · OpenSearch · Redpanda · Keycloak **仅完成编译级验证**（12 种标签组合 PASS）。补做：`scripts/ci.ps1 -Integration` |
| 线上 AI 助手不可用 | `POST /api/ai/chat` → `502 上游模型调用失败：fetch failed`。属 ADR-010 诚实降级（未伪造输出），但功能不可用，需配 `AI_GATEWAY_*` |
| 调试残留 11 个文件 | `email-aggregator-go/deploy/` 下 `bisect_*.py`(5) · `roundtrip*.py`(2) · `test_flat_groups.py` · `convert_and_import.py` · **`kc-token.json`（含凭据）** · `zz-test.json` |
| 未被忽略的 Keycloak 产物 | `deploy/.env.keycloak-prod` · `docker-compose.keycloak-prod.yml` · `keycloak/master-ref.json` · `keycloak/realm-export.v2.json` 等均 untracked 且未被忽略 |

### 6.9 🟢 文档引用质量（✅ 实测，结论与直觉相反）

对 644 处文件引用做了路径有效性检测：

| 分类 | 数量 | 判定 |
|------|------|------|
| 精确命中 | 426 | ✅ |
| 只写简名（如 `styles.css`、`App.tsx`），**文件实际存在** | 200 | 🟢 非缺陷，仅检索不便 |
| 项目内确实不存在 | 18 | 见下 |

18 处中，**真正需要修的仅 2 处**：

1. `DEPLOY.md` 引用 `edgeone.json` → 实际文件名为 `edgeone-project.json`
2. `email-aggregator-go/补全验证报告-20260818.md` 引用 `2026-08-18.md` → 实际为 `2026-08-19.md`

其余 16 处属正常：`state.json`/`state.v2.json` 是 **EdgeOne Blob 中的对象名**（非本地文件）；
`kafka-topics.sh` 是**容器内脚本**；`index_test.go`/`oidc_test.go` 是 Phase 2/3 **规划中待建**的文件；
`ws_check.mjs` 是已删的临时脚本。

> **结论：文档引用健康度良好。**（我第一版检测脚本报出 294 处「失效」，系检测逻辑缺陷导致的**假阳性**，已修正。）

---

## 七、版本控制现状（✅ 实测）

```
分支 master · 远程 github.com/afdk1991/email-aggregator
提交总数 27 · 首次 2026-08-29（基线）· 末次 2026-09-12
提交节奏：2026-08 → 21 次 · 2026-09 → 6 次
```

**🔴 关键事实：最后一次提交是 9-12，而 9-15 / 9-16 的全部成果均未入库。**

当前 **54 个未提交条目**（17 修改 + 37 未跟踪）：

| 类别 | 内容 |
|------|------|
| 已修改 17 个 | 前端 11（`App.tsx` + 9 组件 + `styles.css` + `vite.config.ts`，即本次 UI 重设计）· Go 6（`oidc_assemble_*.go` · `server_integration.go` · `api/server.go` · `auth/middleware.go` · keycloak `realm-export.json`） |
| 未跟踪：**新源码** | `email-aggregator-go/src/api/auth_handler.go`（🔴 见 §6.4）、5 个 SSO 集成脚本 |
| 未跟踪：部署链路 | `deploy/` · `.github/workflows/deploy-edgeone.yml` · `.dockerignore` · `render.yaml` |
| 未跟踪：**敏感** | `deploy/.env.keycloak-prod`（🔴 见 §6.3）、`deploy/kc-token.json` |
| 未跟踪：文档 6 份 | `DEPLOY.md` · `演示与模拟数据清理审计报告` · `项目通读报告` · `UI设计规范` · `UI重设计改动说明` · `全项目部署验证报告` |

---

## 八、对前序结论的更正（自我审计）

诚实记录本轮复核**推翻或修正**的既有说法：

| # | 原结论（出处） | 更正后 | 依据 |
|---|---------------|--------|------|
| 1 | 「`webroot/dist` 无任何脚本负责同步」（`项目文档与文件索引-20260916.md` §4.5） | ❌ **错误**。`build.ps1:36-38` 与 `build.sh:46-48` 均有完整同步逻辑；真实成因是「直接 `go build` 会绕过该脚本」 | 读脚本源码 |
| 2 | 「CI 用 `--experimental-strip-types` 调 Node 20，必然失败」（通读子代理） | ⚠️ **不成立**。TS job 用 **Node 22**（`ci.yml:59`）；Web job 用 Node 20（`:86`）但只跑 `tsc + vite build`，`vite ^5.4.11` 支持 Node 20。**CI Node 版本配置正确** | 读 `ci.yml` + `package.json` |
| 3 | 「`KAFKA_DIAL_ADDR` 与 `KAFKA_BROKERS` 命名冲突」（通读子代理） | ⚠️ **不准确**。两者**并存且语义不同**：Brokers 用于元数据，DialAddr 用于拨号地址覆盖（解决 advertised hostname 不可解析）。是协同关系，非冲突 | 读 `start-local.ps1:150-152` 注释 + 三处 `cmd/*.go` |
| 4 | 「端口硬编码 8080 / `PORT` 未接线」（通读子代理 + `INTEGRATION.md`） | ⚠️ **半对**。`HTTP_PORT` **确实已接线**（`server_integration.go:93`、`main.go:131`）；但确实只认 `HTTP_PORT`，`PORT` 无效。`INTEGRATION.md:170` 的说法**已过时** | 读源码 |
| 5 | 「文档引用 294 处失效」（本人第一版检测脚本） | ❌ **假阳性**。修正检测逻辑后，真实缺失 18 处，其中需修的仅 2 处 | 重跑 v2 检测 |

---

## 九、建议行动（按紧迫度）

**立即（安全）**

1. `.gitignore` 增加 `**/.env.*` 并白名单 `.env.production`；**轮换已暴露的 Keycloak 密钥与 `AI_GATEWAY_API_KEY`**
2. 删除 `email-aggregator-go/deploy/kc-token.json`（含凭据）
3. 给 Go 主干 `/api/demo/push` 加 demo 开关或下线

**尽快（交付完整性）**

4. 一次性提交：6 份文档 + `deploy/` + `deploy-edgeone.yml` + `.dockerignore` + `render.yaml` + `auth_handler.go`
   + 前端 11 个改动（**用 `git add -A`，勿用 `-am`**），提交后跑 `go build ./...` + `npm run build` 验证
5. 修正 `INTEGRATION.md:170`、`DEPLOY.md` 的 `edgeone.json` 引用
6. 加 `sync-web.ps1` 消除 `webroot/dist` 静默漂移

**择机**

7. 清理 11 个调试残留 + 3 个空目录（`src/sync/`、`src/pages/`、`.tmp_q/`）
8. 补 `model` / `tenant` 的单测（当前零覆盖）
9. 在有 Docker 的环境补做集成栈运行时验证（`scripts/ci.ps1 -Integration`）
10. 决策线上 AI 助手：配置 `AI_GATEWAY_*` 还是接受降级

---

## 附：四份通读明细（`.workbuddy/analysis/`）

| 文件 | 体积 | 内容 |
|------|------|------|
| `通读-架构与契约.md` | 19.6 KB | 系统定位与边界 · ADR 清单 · 分层与数据流 · 三套实现等价性 · 硬性约束 · 开放问题 · 文档矛盾 |
| `通读-验证与审计.md` | 32.0 KB | 12 份报告逐份结论 · **历史踩坑全集（7 类）** · 缺陷清单与修复状态 · 演示数据清理因果链 · 4 个阻断项 |
| `通读-部署运维与设计规范.md` | 24.0 KB | 部署形态总表 · 命令速查 · 环境变量契约 · **设计系统要点（Inbox Calm）** · Phase 2/3 规划 · MEMORY 铁律 · 过时点 |
| `通读-开发基础设施.md` | 24.5 KB | CI 解剖 · 容器编排拓扑 · 构建脚本逻辑 · EdgeOne 部署链路 · 环境变量 · 云函数入口分析 · 缺陷清单 |

*纪要生成：2026-09-16 · 关键结论均经源码/命令复核；标注 ⚠️ 者未经独立验证*
